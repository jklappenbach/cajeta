# Cajeta GPU — foundation spec

The **shared GPU foundation** that both compute and graphics build on. Anything that
is *not* specific to the kernel/compute execution model and *not* specific to the
graphics pipeline lives here: the codegen/device/driver plumbing, the memory & buffer
model, value types, math intrinsics, and textures.

```
cajeta-gpu        (this spec — the foundation)
   ▲                       ▲
   │ depends on            │ depends on
cajeta-xpu             cajeta-gfx
(compute:              (graphics:
 science/ML/stats)      pipeline + engine)
```

**Why a separate spec:** the value types (`Vector`/`Matrix`/`Quaternion`), math
intrinsics, the SPIR-V/cubin/hsaco emit machinery, the device/driver layer, the
memory/buffer model, and textures are needed *identically* by compute kernels and by
graphics shaders. Factoring them out keeps the compute spec (`cajeta-xpu`) and the
graphics spec (`cajeta-gfx`) from forking the substrate. *(Today this code lives in the
xpu tree because it was built for compute first; this spec is the seam along which it
becomes shared.)*

## Code-refactor strategy (gpu ⟂ xpu) — read before moving files

A survey of the tree (2026-06-02) found the physical split is **smaller and riskier
than it looks**, and should be staged, not big-banged:

- **Already foundation, no move needed.** The cleanest foundation — the value types
  `CajetaVector` / `CajetaConstantType` / `VectorOps` — already live in
  `src/cajeta/type/`, *not* under `src/cajeta/xpu/`. They are core language types
  (host-usable; the host JIT tests need no GPU). Forthcoming `Matrix`/`Quaternion`
  land there too. So "gpu value types" need **no physical move**.
- **Mixed prelude.** `runtime/src/cajeta/xpu/core/` holds both foundation
  (`Buffer`, `Texture2D`, `Sampler`, `AddressSpace`, `Capabilities`, `Stream`,
  `Event`, `Fence`) and compute (`Thread`, `Wave`, `Workgroup`, `Barrier`,
  `KernelArg`, `KernelError`). Splitting it changes fully-qualified package names
  (`cajeta.xpu.core.*` → `cajeta.gpu.core.*`).
- **Mixed ABI.** The `__cajeta_xpu_*` runtime symbols are split the same way —
  `__cajeta_xpu_{buffer,texture}_*` are foundation, `__cajeta_xpu_{launch,barrier,
  thread}_*` are compute. Renaming them touches the runtime, the codegen that emits
  the calls, and tests — a wide, hard-to-reverse ABI change.

**Decision: defer the physical package/ABI rename; do it as a dedicated, test-gated
effort sequenced with the *first* `cajeta-gfx` work** (the second consumer that
validates the seam). Until then: (1) the spec-level split is authoritative; (2)
foundation *features* advance in `src/cajeta/type/` where no rename is needed (the
value types — see Stage A3/B1); (3) a half-renamed ABI is worse than none, so the
rename happens all-at-once when it happens. This sequencing avoids speculative
factoring before graphics exists, exactly as a shared base should be extracted.

Checkbox legend: `[x]` landed+tested · `[~]` partial (sub-bullets) · `[ ]` not started.
Backends: **NV** NVPTX→cubin · **AMD** AMDGPU→hsaco · **VK** SPIR-V · **CPU** LLJIT.
**Working agreement:** one increment at a time, tests + docs + commit checkpoint;
never a miscompile; commit only when asked; **no attribution trailer**; stage files
explicitly. Companion docs (under `cajeta-docs/gpu/`): the capability matrix
`xpu/CajetaXPU-Matrix.md`, the CPU-backend reference `xpu/CajetaCPU.md`, the XPU
spec `xpu/CajetaXPU.md`, and the cross-backend discipline `xpu/CajetaXPU-Variance.md`.

---

## Part A — Foundation already built ✅

### Stage A0 — Codegen pipeline (triple · assembler · binary) ✅
- [x] Per-backend device triple + `*Backend` (`nvptx64-nvidia-cuda`, `amdgcn-amd-amdhsa`, `spirv64-unknown-vulkan1.3-compute`, CPU)
- [x] Lowered text + object emit (`addPassesToEmitFile`); NV `ptxas`→cubin, AMD `ld.lld`→hsaco, VK direct SPIR-V binary, CPU LLJIT
- [x] `decorateKernel`/entry-point seam (CC + `nvvm.annotations` / `amdgpu_kernel` / `OpEntryPoint`)
- [x] In-process module registration `__cajeta_xpu_register_module(name, bytes, len)` via `llvm.global_ctors`, name-keyed (byte format is the only per-backend difference)
- [x] LLVM 23 migration (API + semantic fixes)

### Stage A1 — Device & driver layer ✅
- [x] Driver acquisition via dlopen — `libcuda.so.1`, `libamdhip64.so` (+ pinned `libhsa`), `libvulkan.so.1` (entry points via `vkGetInstanceProcAddr`)
- [x] Backend availability detection + selection order `CUDA → HIP → Vulkan → CPU`
- [x] VK device pick: first physical device with a compute queue (RADV reaches Strix Halo)
- [x] Module load per backend (`cuModuleLoadData` / `hipModuleLoadData` / `vkCreateShaderModule`→pipeline)

### Stage A2 — Memory & buffer model ✅
- [x] `Buffer<T>` RAII (ctor-allocate / `~Buffer` free); device alloc/upload/download
- [x] Address-space model (`AddressSpace.h`) — Global/Shared/Constant/Private/Generic mapped per backend; alloca-AS seam (0/5/7)
- [x] VK buffer model = descriptor-set SSBOs (BDA has no IR path); scalars as single-element SSBOs

### Stage A3 — `Vector<T,N>` value type ✅ (S6 wrap-up open)
- [x] **S1** non-type template substrate — `CajetaConstantType : CajetaType`; grammar integer arm in `typeArgument`
- [x] **S2** `Numeric`/`Floating`/`Integral` marker bounds (rejects `bool`)
- [x] **S3** `CajetaVector` type + `fromContext` interception; `getLlvmType()` → `FixedVectorType::get(elem, N)`; by-value POD marshalling
- [x] **S4** host codegen — construct, `.xyzw`/`.rgba` + `v[i]` read/assign, element-wise arith + scalar broadcast, `dot`/`length`/`normalize` (shared `vecops` helpers)
- [x] **S5** device codegen — `DeviceLowerer` mirror reusing `vecops`; verified on CPU, VK, AMD
- [~] **S6** consolidation & diagnostics:
  - [x] distinct diagnostics — `CAJETA_ERROR_VECTOR_COMPONENT` (lane bounds), `_CONSTRUCT` (ctor arity), `_ELEMENT_TYPE` (non-bool numeric element), `_LENGTH` (positive N), `_METHOD` (incl. `length`/`normalize` on integral element). *(verified 2026-06-02 — already complete in the tree)*
  - [x] `Buffer<Vector<float32,4>>` end-to-end — `XpuVectorDeviceTests.bufferOfVectorRunsOnCpu` (16-byte element stride + whole-vector `out[i] = <4 x float>` store; CPU JIT). *(2026-06-02 — codegen already handled it; the gap was test coverage)*
  - [~] matrix/doc updates — *(2026-06-04)* `CajetaXPU-Matrix.md` refreshed: footer redated, NVIDIA column corrected to **emit-only (compute on-device pending B5 runner)**, coop-matrix/ray-query landings noted. **Still open:** the LLVM-22-vs-23 version-string sweep across `README.md`/companion docs is *blocked on a decision* — the stock distro build defaults to LLVM 22 (`setup.sh` `CAJETA_LLVM_VERSION:-22`) while the fork/GPU path is 23, so a blanket bump would desync the docs from the build default.
  - [x] **RGBA bridge — DONE 2026-06-07, device-verified (Vulkan/RADV + AMD/gfx1151 + CPU; bit-exact).** `Texture2D.sample` retyped `float32` → `Vector<float32,4>` (the 4 channels the hardware image-sample produces, instead of discarding 3). Each `sampleTexture` seam now returns the `<4 x float>`: **Vulkan/AMD** drop the lane-0 extract (the `samplelevel`/`__ockl_image_sample_2D` result is already `<4 x float>`); **NVPTX** repacks its `nvvm.tex` `{4×f32}` struct into a vector; **CPU** widens its single R channel to `(r,0,0,0)`. **Zero fork/runtime patch — all in-IR**, as predicted. Channels are read off a named local via the existing swizzle path (`Vector<float32,4> c = tex.sample(...); c.x`). Tests: `XpuVulkanEmitTests.lowersTextureSampleToSpirv` (spirv-val), `XpuNvptxEmitTests`/`XpuAmdgpuLoopEmitTests` (emit), `XpuCpuDispatchTests.textureSampleOnCpu`, and on-device `XpuVulkanDispatchDeviceTests.textureSampleOnDevice` + `XpuHipDispatchDeviceTests.textureSampleRoutesToHipOnDevice` — `.x` bit-exact vs the old scalar (7/7). v1 textures are single-channel R32f, so only `.r`/`.x` is contractual; G/B/A land with multi-channel formats (B3). **Follow-on:** chained `tex.sample(...).x` directly on the call needs `vectorComponentRead` to accept a general vector-typed base (today: named-local only). **This unblocks B3 "RGBA / multi-channel formats."**

### Stage A4 — Texture / Sampler types ✅
- [x] `Texture2D` + `Sampler` types; `tex.sample(sampler, u, v)` — 2-D, float32 texel, normalized coords, nearest/bilinear, clamp/wrap, LOD 0
- [x] `LoweringTarget::sampleTexture` seam — CPU (C bilinear), VK (`samplelevel`→`OpImageSampleExplicitLod`), AMD (`__ockl_image_sample_2D`); on-device
- [x] NV `llvm.nvvm.tex.unified.2d`→PTX `tex.2d` (emit-only); requires LLVM 23 (VK); AMD `ockl.bc` hybrid-linked only for sampling kernels

### Stage A5 — Multi-arch binary bundling 🟡
- [x] AMD fatbin — `--xpu-arch=gfx1100,gfx1151` → `clang-offload-bundler`; `hipModuleLoadData` selects device arch; on-device
- [ ] NVIDIA fatbin — `ptxas` per `sm_XX` → cubins → `fatbinary` (`ptxas`/`fatbinary` absent on this box)

---

## Part B — Foundation still to build (forward)

The substrate value types, math, and texture/memory completeness that the compute and
graphics specs both depend on. Finishing Part B is the **definition of done** for the
foundation.

### Stage B1 — Linear-algebra value types
- [x] `Matrix<T,R,C>` — lowers to `<R*C x T>`; reuses the non-type-param substrate from A3/S1 (`matrix-value-type-plan.md` S1–S8: host + CPU/VK/AMD on-device, methods, by-value params)
- [x] Matrix construction, element/row/column access, `m[i][j]`; `matmul`, transpose, identity, elementwise ops
- [x] `Matrix × Vector` / `Matrix × Matrix`; **determinant/inverse for 2/3/4-square** (2026-06-05, host + CPU/VK; recursive cofactor `matops::determinant`/`inverse`, float-only square, non-square/non-2-4 → `CAJETA_ERROR_MATRIX_METHOD`)
- [x] `Quaternion<T>` type + normalize/rotate/slerp (2026-06-05, host + CPU/VK/AMD). New `CajetaQuaternion` (`<4 x T>` = w,x,y,z; `QUATERNION_FLAG`), `quatops` helpers. `*` = Hamilton product (Q*Q) / vector rotation (Q*Vector<T,3>); `+ -` element-wise; methods normalize/conjugate/length/dot; interpolation via **nlerp** AND **slerp** (slerp uses acos/sin through the B2-inc2 transcendental seam — `quatops::slerp` is parametrized by a `TrigEmitter` so host emits `llvm.acos/sin` and device routes through `LoweringTarget::transcendental`; branchless with an automatic near-parallel nlerp fallback). Float element only (`CAJETA_ERROR_QUATERNION_ELEMENT_TYPE`).
- [x] Swizzles `.xyz`/`.xy`/`.xxyy` multi-component **reads** (2026-06-05, host + CPU/VK): `DotExpression` (host) + `vectorComponentRead` (device) emit a `shufflevector` via `vecops::swizzle`; xyzw + rgba letters, repeats allowed, out-of-range → `CAJETA_ERROR_VECTOR_COMPONENT`. **Swizzle writes (`v.xy = …`) deferred.**
- [x] Vector/Matrix comparisons → `<N x i1>` mask + `any`/`all`/`select` (2026-06-05, host + CPU/VK/AMD). **Rule: comparison operators on value types yield per-lane masks; equality is NOT special — `(a==b).all()` for whole-object eq. This OVERTURNED the B1 S4 `Matrix ==`→boolean.** Vector + Matrix get full `== != < <= > >=` with scalar-RHS broadcast. Masks are register-only (bool-element buffers/args stay ABI-rejected). `select` = the branchless primitive (ReLU/min/prune). **Docs: `cajeta-docs/MaskSelect.md` (walkthrough + alternatives) + `samples/Tour/xpu` `mask/select` demos (ReLU + weight-prune, bit-exact CPU/VK/AMD).** See the comparison-mask memo.
- [x] `cross`, `reflect`, `refract`, `distance`, `clamp`, `lerp`, `min`/`max` (2026-06-05, host + CPU/VK; float-only — **integer min/max/clamp deferred** pending device element-signedness tracking)

### Stage B2 — Device math intrinsics
- [x] **Increment 1 — native-lowering subset, in kernels (2026-06-02).** `Math.{sqrt,floor,ceil,trunc,round,abs,min,max,fma}` lower in the device `DeviceLowerer` (`KernelLowering.cpp::lowerMathCall`) to LLVM intrinsics that select natively on **all four backends** — no device math-library link. Operates in the argument's FP type (f32-native; not the host's forced f64). Transcendentals give a clean `XPU-N01` until increment 2. Tests: `XpuMathDeviceTests` (emit + CPU + clean-reject + Vulkan/RADV + AMD/gfx1151 on-device, 5/5 green).
- [x] **Increment 2 — transcendentals (2026-06-05)** `sin/cos/tan/asin/acos/atan/exp/exp2/log/log2/log10/pow/atan2/rsqrt` in kernels, via a new `LoweringTarget::transcendental` seam. **CPU libm + VK `OpExtInst GLSL.std.450` + AMD `ocml` all on-device green** (`XpuMathDeviceTests.transcendentals{LowerToIntrinsics,RunOnCpu,RunOnVulkanDevice,RunOnAmdDevice}`, 4/4). Default seam emits the `llvm.*` intrinsic (correct on CPU/Vulkan); **AMD overrides** to emit `__ocml_<fn>_f32` (AMDGPU mis-lowers `llvm.sin` w/o range reduction → NaN) and `AmdgpuBackend` links `ocml.bc` + the `oclc` control globals when `__ocml_*` is referenced; **NV overrides** to `__nv_<fn>f` (libdevice) — **emit-only until B5** (no NV HW). rsqrt = `amdgcn_rsq` / `nvvm_rsqrt_approx_f` / `1/sqrt`. f32-native.
- [x] **Vectorized forms (2026-06-05)** — `Math.<fn>(Vector<float32,N>)` elementwise over `<N x T>` (native ops + transcendentals). `asFp` is vector-aware; the default seam emits the vector intrinsic (CPU libm / Vulkan vector `OpExtInst`); AMD/NV scalarize per lane (ocml/libdevice are scalar). `pow`/`atan2` broadcast a scalar operand. `XpuMathDeviceTests.vectorMathRunsOn{Cpu,VulkanDevice,AmdDevice}` 3/3.
- [x] **fast/approx + fast-math flags (2026-06-05)** — a `@FastMath` annotation on a `@Kernel` sets the LLVM fast-math flags (`setFast()`: contract/reassoc/arcp/afn/nnan/ninf/nsz) on the IRBuilder for the whole body, so every FP op (and the transcendental seam's intrinsic calls) carries them — the backend may fuse to FMA, use reciprocals, and pick approximate transcendentals. Opt-in (precision-trading). `XpuMathDeviceTests.fastMath{FlagsEmitted,RunsOnCpu,RunsOnVulkanDevice}` 3/3 (the @FastMath body emits `fmul fast`/`fadd fast`; the plain kernel stays IEEE-strict).
- [x] `float16`/`bfloat16` element types (`<N x half>`/`<N x bfloat>`) — ML/graphics dtypes (2026-06-05). **Both portable: host + CPU + Vulkan + AMD.** **float16** (`<N x half>`) uses native f16 arithmetic (portable). **bfloat16** is a distinct registered type (`BFLOAT16_ID`/`BFLOAT16_TYPE_ID` → LLVM `bfloat`; `deviceScalarType` distinguishes it from float16 by the ID byte) lowered with the **storage + f32-compute** model — `applyBinOp` widens bfloat operands to f32, computes, narrows back (no native bfloat arithmetic, which is the Intel-only `SPV_INTEL_bfloat16_arithmetic`); Vulkan emits `SPV_KHR_bfloat16` for the type+conversions. `XpuVectorDeviceTests.{float16,bfloat16}RunsOn{Cpu,VulkanDevice,AmdDevice}` (6) + `VectorTests.halfAndBfloat16Elements`. (Follow-on: bf16 in the cooperative-matrix/WMMA path — the ML training lever.)

### Stage B3 — Texture & sampler completeness
- [x] RGBA / multi-channel formats (built on the A3/S6 RGBA bridge) — **DONE 2026-06-07, device-verified across CPU + Vulkan/RADV + AMD/gfx1151.** `TextureFormat` enum (`runtime/src/cajeta/xpu/core/TextureFormat.cajeta`) selects the storage format at construction: **`R32F` (default), `R8_UNORM`, `RGBA8_UNORM`, `RGBA32F`, `R16F`, `RGBA16F`** — all **float-sampled** (`sample()` stays `Vector<float32,4>`; the shader side is format-agnostic, so no lowering change beyond the bridge). The work was **runtime-side**: a format table (`cajeta_texfmt_*` in `cajeta_runtime.c`) threads channels/texel-bytes/encoding through `__cajeta_xpu_texture_alloc/upload` (now take a `format` ordinal) into all three device allocators — Vulkan `VK_FORMAT_*` (R32_SFLOAT / R8_UNORM / R8G8B8A8_UNORM / R32G32B32A32_SFLOAT / **R16_SFLOAT / R16G16B16A16_SFLOAT**), HIP `hipChannelFormatDesc` (8/16/32-bit channels, UNSIGNED for UNORM else FLOAT) + read mode (Element for float/half, **NormalizedFloat** for UNORM), and the CPU store; **upload encodes** float-interleaved host data per format (memcpy for f32, **float→unorm8 quantize** for UNORM, **float→binary16 pack** for half via `cajeta_f32_to_f16`). The CPU sampler became vec4 (`__cajeta_xpu_cpu_tex_sample_rgba` → `<4 x float>` via a `vector_size(16)` typedef; G/B = 0, A = 1 defaults for <4 channels), stores half formats round-tripped through f16 (`cajeta_f16_to_f32`) so the CPU matches the device's half rounding, and `CpuKernelLowering` calls it (dropping the bridge's lane-0 widen). `Texture2D` gains a 3-arg `(w, h, TextureFormat)` ctor (2-arg stays R32F). Tests (all formats × 3 backends): `Xpu{Cpu,Vulkan,Hip}…` `textureSample{Rgba32f,Rgba8Unorm,R16f,Rgba16f}…` (2×2 texture, all 4 channels per texel read back bit-exact / within unorm8 + f16 tol; linear) — **20/20 texture tests green**. Tour `runTexture` (RGBA8_UNORM 2×2, channel-sum `total = 6` on the CPU-portable path). **Finding:** a divergent `if/else-if` channel-select kernel *hung/crashed* the GPU — use branch-free multi-write. **Follow-ons (later):** integer (`texelFetch`, int-returning) formats; texture dims (1-D/3-D/cube/array); mipmaps/LOD/anisotropy/depth-compare; multiple samplers per texture.
- [ ] Integer & normalized-int (texelFetch, int-returning) formats; 1-D / 3-D / cube / array textures
- [ ] Mipmaps + explicit/implicit LOD; anisotropy; depth/compare samplers
- [x] **Writable images (`imageStore`)** — compute-generated render targets (the gfx bridge) — **DONE 2026-06-06, device-verified (Vulkan/RADV gfx1151, bit-exact).** Surfaced as **`Image2D`** (the writable twin of `Texture2D`): `heap Image2D(w, h)`, a device-only `img.store(uint32 x, uint32 y, float32 value)` (integer texel coords, exact — vs `Texture2D.sample`'s normalized filtered read), and host readback `img.download(float32[])`. **Fork (small, mirrors `store.typedbuffer`):** the in-tree `OpImageWrite` path only had a *typed-buffer* store (scalar `i32` coord), so added one intrinsic `llvm.spv.resource.store.2d` (a **`<2 x i32>`** coord) routed to the *existing* coordinate-agnostic `selectImageWriteIntrinsic` — 1 `.td` def + 1 selection case + `llc` rebuild on `cajeta-spirv` (no in-tree 2-D image-store producer → not an upstream PR). Lowering: a new `LoweringTarget::storeImage` seam — **default** = unsupported (XPU-N01); **Vulkan overrides** to bind a `STORAGE_IMAGE` (`vkStorageImageType`, Sampled=2) and emit a single `OpImageWrite`, splatting the scalar into `<value,0,0,0>`. **Probe finding (the load-bearing catch):** the image declares the **R32f** known format, NOT `Unknown` — `Unknown` would require `StorageImageWriteWithoutFormat`, which the SPIR-V backend only exposes for SPIR-V ≥ 1.6 in the Shader env, and cajeta's `spirv-unknown-vulkan1.3-compute` triple doesn't pin 1.6 → unsatisfiable ("Unable to meet SPIR-V requirements"); R32f matches the runtime `VK_FORMAT_R32_SFLOAT` and needs only `Shader`. Runtime: `CAJ_VKB_STORAGE_IMAGE` descriptor kind, `VK_IMAGE_USAGE_STORAGE_BIT|TRANSFER_SRC`, a pre-dispatch `→GENERAL` layout barrier recorded into the launch command buffer, and a **new image readback path** `vkCmdCopyImageToBuffer` (`__cajeta_xpu_image_download`; images previously had no host download at all). Tests: `XpuVulkanEmitTests.lowersImageStoreToSpirv` (emit: `OpImageWrite`+`R32f`, spirv-val), `XpuVulkanDispatchDeviceTests.imageStoreOnDevice` (RADV: one thread/texel writes its linear index, readback bit-exact), fork lit `hlsl-resources/StorageImage2DLoadStore.ll`, `cajeta-docs/WritableImages.md`. **NOT toured** — Vulkan-only (like `Texture2D`/AccelStruct/RayQuery, which are also absent from the CPU-portable Tour). Drive-by fix: added the missing CPU `@Native` stubs for `Bits.*` + `Wave.rotate` (a pre-existing host-JIT symbol gap that also broke `QuaternionTests.rotateVector90AboutZ`). **Image read (the twin) DONE 2026-06-06:** `Image2D.load(x, y) -> float32` (`OpImageRead`) via a sibling fork intrinsic `llvm.spv.resource.load.2d` (1 `.td` def + 1 selection case routed to the *existing* coordinate-agnostic `selectReadImageIntrinsic`, which reads a scalar result as a vec4 + extracts lane 0); a `LoweringTarget::loadImage` seam (Vulkan-only). Makes `Image2D` **read-modify-write** / image→image: the dispatch barrier now ALSO fires when the storage image is already GENERAL (a read/write-after-write memory dependency, not just a layout transition), so a dispatch reads what an earlier dispatch stored. Tests: `XpuVulkanEmitTests.lowersImageLoadToSpirv`, `XpuVulkanDispatchDeviceTests.imageLoadStoreRmwOnDevice` (RADV: fill then `2v+1` RMW, bit-exact). Follow-ons: `Image2D<T>` / multi-channel / integer formats, 3-D, AMD/NVIDIA/CPU `store`/`load`.
- [ ] Multiple samplers per texture; separate sampler objects

### Stage B4 — Memory & data ergonomics
- [ ] Pinned/host-mapped memory; unified memory where available
- [ ] Sub-buffer / strided views (`Buffer.slice`, non-contiguous gather) — *(note: `CooperativeMatrix.load/store` take `offset`/`layout`/`stride` as call-site params for sub-tile access, but that is not a persistent `Buffer.slice` view; the slice surface is still unbuilt.)*
- [ ] Async copies / transfer queues (the copy primitive; the *compute*-overlap `Stream` semantics live in `cajeta-xpu`)
- [ ] Bindless / multi-buffer descriptor sets (VK) beyond per-arg binding

### Stage B5 — NVIDIA codegen/device verification
- [ ] **NV runner = the x86-64 Windows + NVIDIA box, via WSL2** (Ubuntu + CUDA-on-WSL), reusing the **`linux-x64`** LLVM-23 artifact from C0 + the CUDA toolkit (`ptxas`/`fatbinary`). Registered as a second self-hosted **Linux** runner. This is the only NVIDIA hardware (the Strix Halo dev box is AMD-only); WSL2 avoids gating NV bring-up on the native-Windows/mingw toolchain (which is release-only — see `plans/c0/`). *(Verify CUDA-on-WSL exposes libcuda + ptxas on first setup.)*
- [ ] Unblocks the **5 currently-skipped NV exec tests** (`XpuSaxpy`, `XpuLoop`, `XpuShared`×2, `XpuHostLaunch` — CUDA-gated, verified 2026-06-04) + verifies the NV emit-only foundation on-device (textures A4, math B2); complete NV fatbin (A5)
- [ ] Promote the NVIDIA column of `CajetaXPU-Matrix.md` from "emit-only" to "on-device measured"
- [ ] **Full-stack NVIDIA re-evaluation — REQUIRED once the branch is pulled to an NVIDIA machine.** Everything NVIDIA-facing is *emit-only* today (NVPTX text/cubin emit verified, but **no NVPTX kernel has ever executed on real NVIDIA hardware** — there is no NV device on the Strix Halo dev box). When the `cajeta-spirv`/`cajeta-xpu` branch lands on the WSL2+CUDA box, run the **entire stack** end-to-end on NVIDIA, not just the 5 unblocked exec tests:
  - the full XPU device suite (SAXPY, loop/reduction, static + dynamic shared, host-launch, **wave/subgroup ops**, **vectors**, **math intrinsics**, **textures**) against the CUDA driver;
  - the **A5 NV fatbin** path (`ptxas` per `sm_XX` → `fatbinary`) — currently deferred for lack of `ptxas`;
  - re-confirm the **value-type / math / texture** foundation contract holds on NVIDIA (it is only AMD/Vulkan-measured today);
  - cooperative-matrix / ray-query are **Vulkan-flavor only** — note explicitly whether any NVIDIA-native MMA/RT path is in scope or remains out-of-scope after this pass.
  Treat the NVIDIA column as **unverified** until this full re-evaluation passes on real hardware; fix whatever the on-device run surfaces (NVPTX has only ever been exercised through emit + FileCheck, so live regressions are expected).

---

## Part C — Cutting-edge SPIR-V feature completeness (via a downstream LLVM)

**Goal (owner directive): expose *every* cutting-edge GPU call SPIR-V/Vulkan can
make — and for anything Vulkan exposes that LLVM can't yet lower, add it.** Cajeta
emits SPIR-V through LLVM's **in-tree** SPIR-V backend, so the ceiling on "what GPU
calls are possible" is set by that backend, not by Cajeta. Closing the gap is partly
Cajeta-side (surface the call) and partly **LLVM-side** (teach the backend to lower
an opcode it doesn't). LLVM-side changes live in `llvm/lib/Target/SPIRV/` — **never
in the Cajeta repo** — so they need a distribution strategy (C0) and an honest audit
of the gap (C2).

### Stage C0 — LLVM distribution: downstream fork + prebuilt artifact (**adopted**)
The Cajeta repo depends on an LLVM that has our backend patches; it does not contain
them. Today we already build **stock upstream `main`** (`cpp/llvm-project`, 23-git,
no local commits) — so the "get a custom LLVM to CI" problem already exists for the
graphics path; feature patches just turn "stock main build" into "patched main build."
- [x] Fork `llvm/llvm-project` on GitHub → rename to **`cajeta-llvm`** (fork relationship + upstream sync survive the rename); carry backend patches on a tracking branch (`cajeta-spirv`) over a **pinned** upstream base commit (`203c0668d`). Runbook + workflows in `plans/c0/`. *(2026-06-04: fork live — `origin git@github.com:jklappenbach/cajeta-llvm.git`, `upstream llvm/llvm-project`; branch `cajeta-spirv`, 10 patches above base.)*
- [x] A CI job **in the fork** builds LLVM **once** and publishes a **prebuilt artifact** (GitHub Release tarball / GHCR container image / build cache) *(2026-06-04: active fork workflows `build-cajeta-llvm.yml` + `upload-release-artifact`.)*
- [~] Cajeta CI + local dev **consume** that artifact via `LLVM_DIR` — Cajeta CI never builds LLVM and never vendors LLVM source (same pattern Rust/Swift/Zig use to ship custom LLVM). *(2026-06-04: **local dev consumes** the fork build via `LLVM_DIR` (`build-cajeta`); the **Cajeta-CI consume job is still a draft** in `plans/c0/cajeta-ci-consume.yml`, not yet wired into `.github/workflows/` — cajeta CI currently only has `release.yml`.)*
- [~] **Host-artifact matrix mirrors `release.yml`** (`x86_64-linux-gnu`, `aarch64-linux-gnu`, `aarch64-apple-darwin`, `x86_64-w64-mingw32`) — one toolchain per *host*; the `X86;NVPTX;AMDGPU;SPIRV` *targets* are all in each. **`release.yml` today pulls distro LLVM (apt/brew/MSYS2 ≤22)** — C0 must replace that with the LLVM-23 artifact per leg, or 23-only features (graphics SPIR-V, ray query) break in release builds. Build **`linux-x64` first** (unblocks all Part C dev + AMD *and* NVIDIA on-device testing — NV via WSL2 on the Windows box, see B5); native Windows/mingw leg is **release-only, deferred**. Runner topology + matrix detail in `plans/c0/README.md`.
- [~] **Upstream-first**: anything upstream will accept (ray query, cooperative-vector, …) goes up as a PR; the fork branch carries only not-yet-landed / in-review patches, minimizing rebase debt. *(2026-06-04: upstream PRs **deliberately deferred** — building a credible downstream body of work on the fork first; the branch carries all patches for now, so this is a held policy decision, not pending review.)*
- [~] Rebase cadence: re-pin to a newer upstream base periodically; drop patches that landed upstream; document the base commit + applied-patch list in the fork README. *(2026-06-04: base `203c0668d` pinned; `CAJETA-FORK.md` documents base + patches but is **stale** — lists 6, HEAD now carries 10 (adds coop-matrix CM1–CM2 + the `SPIRVFixupMergePlacement` fix); refresh due.)*

### Stage C1 — The two-flavor reality (read before auditing)
LLVM's SPIR-V backend has **two dialects**, and a feature wired in one is not
automatically reachable from the other:
- **OpenCL "Kernel"** — `spirv64-unknown-unknown`, `OpCapability Kernel`, Physical addressing. Driven by SYCL/oneAPI; **most cutting-edge compute lowerings land here first** (enabled with `--spirv-ext=+SPV_…`).
- **Vulkan "Shader"** — `spirv-unknown-vulkanN-stage`, `OpCapability Shader`, Logical GLSL450. **This is what Cajeta emits** (we target Vulkan, not an OpenCL runtime).

So a feature with a CodeGen test under `spirv64-unknown-unknown` is *lowerable* but may **not** be reachable from our Vulkan flavor — and "teaching the backend" is often **porting an existing OpenCL-flavor lowering to the Shader flavor** (smaller than writing it from scratch). Hence the three-tier audit below.

### Stage C2 — Feature audit (verified against LLVM 23-git `Target/SPIRV/`, 2026-06-02)
Tiering rule: *lowerable* = IR intrinsics/builtins **and** a `test/CodeGen/SPIRV` test. Symbolic-table-only (an enum name with no opcode/intrinsic) = **stub, not lowerable**.

**Tier 1 — already reachable from our Vulkan flavor → probe + surface in Cajeta (no LLVM patch):**
- [x] **Integer dot product** (`SPV_KHR_integer_dot_product`, DP4a) — **DONE 2026-06-06, device-verified (Vulkan/RADV + AMD/gfx1151 + CPU, bit-exact).** Surfaced as **`Vector<int8,4>` / `Vector<uint8,4>` `.dot(b)` → int32**, with the fused-accumulate overload `.dot(b, acc)`; signedness comes from the element type (OpSDot vs OpUDot). **Tier-1 confirmed: NO LLVM patch** — the `llvm.spv.dot4add.{i8,u8}packed` intrinsics + GlobalISel selection are stock upstream (HLSL path), `[EnvVulkan]`-reachable. Lowering: a new `LoweringTarget::integerDot4x8` seam — **default** = the portable widening reduce (`vecops::idotWiden`: sext/zext each lane→i32, mul, sum, +acc; correct on CPU/AMD/NVIDIA), **Vulkan overrides** to pack the four lanes into i32 and emit `dot4add` → `OpSDot/OpUDot ... PackedVectorFormat4x8Bit` + `OpIAdd` (the hardware dot-product unit; `SpirvBackend::enableSpirvExtensions` adds `+SPV_KHR_integer_dot_product`, else the backend falls back to the spirv-val-clean bit-field expansion). Host path mirrors via `idotWiden`. v1 scope: **4-lane 8-bit only** (the DP4a primitive; other integer shapes get a clean diagnostic). Tests: `VectorTests.{integerDotProduct,integerDotAccumulate,unsignedIntegerDotProduct}` (host), `XpuVulkanEmitTests.lowersIntegerDotToSpirv` (emit + spirv-val), `XpuVectorDeviceTests.integerDotRunsOn{Cpu,VulkanDevice,AmdDevice}` (bit-exact 1265: signed + unsigned + fused). **(Quantized int8 inference — the SPELA/Prism lever.)** Follow-ons: general-width integer dot, AMD-native `v_dot4` (the gfx seam already routes through the portable reduce), NVIDIA `dp4a`.
- [x] **Float atomics** (`SPV_EXT_shader_atomic_float_add` / `_min_max`) — **DONE 2026-06-06, device-verified (Vulkan/RADV + AMD/gfx1151 + CPU; concurrent reduction bit-exact).** Surfaced as **`Buffer<float32>.atomic{Add,Min,Max}(index, value)` → float32** (returns the old value; f32 v1). **Tier-1: NO LLVM patch** — generic `atomicrmw fadd/fmin/fmax` (flavor-agnostic). Lowering: `LoweringTarget::atomicFloatRMW` seam — **default** = relaxed (monotonic) system-scope `CreateAtomicRMW` (native global FP atomic on AMDGPU/NVPTX, lock/cmpxchg on CPU); **Vulkan overrides** to **Device scope + AcquireRelease** (spirv-val rejects CrossDevice scope + SequentiallyConsistent/relaxed-with-storage-class on `OpAtomicF*EXT` — *the probe finding*), emitting `OpAtomicFAddEXT/FMinEXT/FMaxEXT` + `AtomicFloat32{Add,MinMax}EXT` caps; `SpirvBackend` enables `+SPV_EXT_shader_atomic_float_add,+SPV_EXT_shader_atomic_float_min_max`. Tests: `XpuVulkanEmitTests.lowersFloatAtomicsToSpirv` (emit + spirv-val), `XpuAtomicDeviceTests.floatAtomicsRunOn{Cpu,VulkanDevice,AmdDevice}` (4096-thread parallel sum/max/min, exact). Tour `reduceAtomic` + `cajeta-docs/FloatAtomics.md`. Follow-ons: f16 (`SPV_EXT_shader_atomic_float16_add`) / f64 atomics, integer atomics (`atomicAdd/And/Or/Xor/Exchange/CompareExchange`), shared-memory atomics.
- [x] **Integer atomics** (core SPIR-V — no extension) — **DONE 2026-06-06, device-verified (CPU + Vulkan/RADV + AMD/gfx1151; concurrent, bit-exact).** Surfaced as **`Buffer<int32|uint32>.atomic{Add,Sub,Min,Max,And,Or,Xor,Exchange}(index, value)`** and **`.atomicCompareExchange(index, expected, desired)`** → old value. **NO fork patch for the ops** — generic `atomicrmw add/sub/and/or/xor/xchg/min/umin/max/umax` + `cmpxchg` map straight to `OpAtomicI*`/`OpAtomicCompareExchange`; `isSigned` (from the buffer element type) picks `SMin/SMax` vs `UMin/UMax`. New `LoweringTarget::atomicIntRMW` + `atomicCompareExchange` seams (default monotonic system-scope; **Vulkan overrides** to Device scope + AcquireRelease, the same memory-model constraint as the float path). The call-site dispatch (shared with float atomics) now routes int32/uint32 element buffers. **Surfaced a pre-existing UPSTREAM SPIR-V backend bug:** `atomicCompareExchange` was the first construct to hit `SPIRVLegalizePointerCast`'s `llvm_unreachable("Unsupported ptrcast user. Please fix.")` (Nathan Gauër, 2025-03-04) — a `cmpxchg` whose pointer flows through a `spv_ptrcast` (the descriptor-buffer element pointer) was unhandled → release-mode segfault. HLSL's `InterlockedCompareExchange` lowers its pointer differently, so upstream never hit it. Fixed on `cajeta-spirv` (`dd2ac00`) by handling the `spv_cmpxchg` ptrcast user like the existing `spv_gep` case (bypass the spurious cast); **upstream PR ready** — clean branch `pr/spirv-legalize-cmpxchg` off `origin/main` (fix + regression test in `passes/SPIRVLegalizePointerCast.ll`, lit-verified). Tests: `XpuVulkanEmitTests.lowersIntAtomicsToSpirv` (all 9 ops + spirv-val), `XpuAtomicDeviceTests.intAtomicsRunOn{Cpu,VulkanDevice,AmdDevice}` (4096-thread Add/Max/Min/Or + **uncontended** CAS, bit-exact). Tour `histogram` (verified `32 each`) + `cajeta-docs/IntegerAtomics.md`. **Test lesson:** a spin-CAS loop on one shared slot is a GPU livelock (lockstep subgroup starvation) — the device test does per-lane-distinct CAS instead. Follow-ons: 64-bit integer atomics (`Int64Atomics` cap).
- [x] **Shared-memory (LDS) atomics** — **DONE 2026-06-06, device-verified (CPU + Vulkan/RADV + AMD/gfx1151).** The same `Buffer` atomic surface works on a `shared T[]` array (LDS) — the fast per-block reduction/histogram path. **Tiny compiler change:** the atomic's memory scope is now derived from the pointer's address space (a `SpirvTarget::atomicScope` helper) — `shared` is Workgroup storage (addrspace 3) → **Workgroup** scope (`ds_add_u32` on AMD, `OpAtomicIAdd` Workgroup on Vulkan), global stays Device scope; AMD/CPU infer it from the addrspace so only the Vulkan overrides changed. No new seam (shared arrays already share the `bufferBases`/`bufferElems` maps, so the dispatch already routed them). **Surfaced a SECOND facet of the same upstream `SPIRVLegalizePointerCast` gap as `atomicCompareExchange`:** a shared atomic on element 0 folds the GEP to the aggregate base, so the `atomicrmw` flows through a `spv_ptrcast` whose *atomic* user was unhandled (load/store users *were* — which is why shared *reductions* always worked). Broadened the fork fix (`cajeta-spirv` e2a7067) to handle all atomic ptrcast users (`atomicrmw`/`cmpxchg`/`spv.cmpxchg`) and folded it into the upstream PR (#202098, retitled "Handle atomic users…", +57). Tests: `XpuVulkanEmitTests.lowersSharedAtomicsToSpirv` (both Workgroup + Device scope in one kernel, spirv-val), `Xpu{Shared,SharedAmd}*DeviceTests.sharedAtomicCounterRunsOnDevice` (per-block counter == block size, bit-exact). **Process lesson (cost a long detour):** left the fork on the `pr/spirv-*` branch after opening the PR → build-cajeta header desync → cajeta segfaulted on *every* lowering; recovery = `git switch cajeta-spirv` + rebuild build-cajeta LLVM (per `[[reference_fork_llvm_branch_desync]]`). Follow-ons: shared-memory `atomicCompareExchange` device test (the legalizer fix already covers it).
- [x] **Shader clock** (`SPV_KHR_shader_clock`) — **DONE 2026-06-06, device-verified (Vulkan/RADV + AMD/gfx1151 + CPU; clock live on every thread).** Surfaced as **`Thread.clock()` → uint64** (a free-running tick for in-kernel timing; diff two reads). **NOT no-patch — reclassified Tier-2/fork:** `OpReadClockKHR` exists in the backend but is only reachable via the OpenCL `__spirv_ReadClockKHR` builtin (`isShader()`-gated off), and there is **no in-tree producer** for a clock intrinsic (unlike `dot4add`, which clang HLSL emits) → **fork-only, not upstreamable** without also contributing the clang producer. Added fork intrinsic `llvm.spv.read.clock(scope)` + GlobalISel selection → `OpReadClockKHR` (Subgroup scope) on `cajeta-spirv`. Lowering seam `LoweringTarget::readClock` — **default** `llvm.readcyclecounter` (CPU rdtsc; AMDGPU lowers it to `s_getreg HW_REG_SHADER_CYCLES` on RDNA — the GCN `s_memrealtime`/`s_memtime` are not selectable on gfx11+); **Vulkan** → `llvm.spv.read.clock`; **NVPTX** → `clock64` (emit-only). `SpirvBackend` enables `+SPV_KHR_shader_clock`. Tests: fork lit `read_clock_vulkan.ll`, `XpuVulkanEmitTests.lowersShaderClockToSpirv`, `XpuClockDeviceTests.shaderClockRunsOn{Cpu,VulkanDevice,AmdDevice}`. Tour `clockProbe` + `cajeta-docs/ShaderClock.md`.
- [x] **Subgroup rotate** (`SPV_KHR_subgroup_rotate`), **uniform group instructions**, **bit instructions**, **maximal reconvergence** (`SPV_KHR_maximal_reconvergence`) — confirm + expose beyond today's wave ops. **DONE 2026-06-06 — all four surfaced + device-verified.** Probe finding (against the fork `Target/SPIRV/`): **3 of 4 needed no LLVM patch** (bit instructions = generic intrinsics; maximal reconvergence = a function attribute; wave reductions = the already-Shader-reachable `GroupNonUniformArithmetic` family whose intrinsics + selection already existed in-fork — the OpenCL-only `SPV_KHR_uniform_group_instructions` is the wrong path); **only subgroup rotate needed a fork intrinsic** (`llvm.spv.subgroup.rotate`, one selection case reusing `selectWaveOpInst`, one `llc` rebuild). All four are device-verified on RADV + gfx1151 (+ CPU). Sub-items:
  - [x] **Bit instructions — DONE 2026-06-06, device-verified (Vulkan/RADV + AMD/gfx1151 + CPU, bit-exact).** **NO LLVM patch, NO extension** — reach the Shader flavor as *core* SPIR-V via generic intrinsics. Surfaced as **`cajeta.xpu.core.Bits`** (per-invocation, scalar): `reverse(uint32)→OpBitReverse` (`llvm.bitreverse`), `count(uint32)→OpBitCount` (`llvm.ctpop`), `rotateLeft/rotateRight(uint32,uint32)`. **Probe finding:** `llvm.fshl/fshr` are NOT usable under Vulkan — the SPIR-V backend lowers them to a *generated external-linkage helper* (`spirv.llvm_fsh?_i32`) → pulls in `OpCapability Linkage`, which `spirv-val --target-env vulkan1.3` rejects; so rotate is **expanded inline** in `KernelLowering` as `(v<<s)|(v>>(32-s))` with `s` masked to `[0,31]` (the `s==0` case folds to a 0-shift, no undef shift-by-32). No seam (generic intrinsics select identically on every backend) — lowered once in `lowerMethodCall`. v1: **32-bit only** (Vulkan also restricts `OpBitCount` to 32-bit). Tests: `XpuVulkanEmitTests.lowersBitInstructionsToSpirv` (emit + spirv-val, asserts inline rotate / no `llvm.fsh*`), `XpuBitsDeviceTests.bitInstructionsRunOn{Cpu,VulkanDevice,AmdDevice}` (reverse/popcount/rotl/rotr bit-exact vs a C++ reference). Tour `runBitOps` (out[0]=132 on CPU + Vulkan) + `cajeta-docs/BitInstructions.md`. Stdlib structure count re-anchored to 104. Follow-ons: bitfield insert/extract (`OpBitFieldInsert/SExtract/UExtract` — OpenCL-builtin-only today → would need a fork intrinsic), 64-bit width, `leadingZeros`/`trailingZeros` (no core Shader op — `clz`/`ctz` are GLSL ext-inst `FindUMsb`/`FindILsb`, a separate surface).
  - [x] **Maximal reconvergence — DONE 2026-06-06, device-verified (Vulkan/RADV; 8 wave device tests green with the mode attached).** **NO LLVM patch, NO new opcode** — a function attribute the backend already turns into an execution mode. **Automatic + gated on use:** a kernel that lowers a *cross-lane* Wave op (`shuffleSync`/`ballotSync`/`reduceSum`) sets `"enable-maximal-reconvergence"="true"` → `OpExecutionMode <entry> MaximallyReconvergesKHR` (`SPV_KHR_maximal_reconvergence`, enabled in `SpirvBackend`); pure per-lane queries (`width`/`laneId`/`isFirstLane`) and non-wave kernels carry NO extra device requirement (no regression to the 185-test base). Implemented as `DeviceLowerer::usedSubgroupOp_` (set by the three cross-lane ops) read at finalization → a new `LoweringTarget::onSubgroupOpsUsed` seam (default no-op; NVIDIA/AMD/CPU model wave convergence through ISA + LLVM convergence, no module-level mode) which `SpirvKernelLowering` overrides to set the attr. The correctness companion to the existing `Wave.*` ops. Tests: `XpuWaveEmitTests.spirv{Wave,NonWave}Kernel{Requests,Omits}MaximalReconvergence` (positive + negative emit), and the existing `XpuWaveDeviceTests` (8) re-verified on real RADV with the mode now attached. Transparent (no user API), so no Tour item; documented in `CajetaXPU.md §6.3`.
  - [x] **Subgroup rotate — DONE 2026-06-06, device-verified (Vulkan/RADV native + AMD/gfx1151 ds_bpermute; both bit-identical, cross-checked).** Surfaced as **`Wave.rotate(value, delta) → uint32`** — read `value` from lane `(laneId + delta) mod width`. **Fork-carried** (the shader-clock pattern): the opcode `OpGroupNonUniformRotateKHR`, the `GroupNonUniformRotateKHR` capability, the `SPV_KHR_subgroup_rotate` extension, AND the `SPIRVModuleAnalysis` requirement **already existed** in the fork — the only gap was the Shader-flavor emission path (OpenCL `sub_group_rotate` builtin is `isShader()`-gated off). Added fork intrinsic `llvm.spv.subgroup.rotate(value, delta)` (`IntrinsicsSPIRV.td`) + one GlobalISel case (`SPIRVInstructionSelector.cpp`, reusing `selectWaveOpInst` which prepends the Subgroup scope) on `cajeta-spirv`; `SpirvBackend` enables `+SPV_KHR_subgroup_rotate`. **Lowering: a non-pure `LoweringTarget::waveRotate` seam with a portable default** — `waveShuffle(value, (laneId+delta) mod width)` built on the existing seams, so NVIDIA (shfl, divergent-index-capable) + CPU get rotate for FREE; **Vulkan overrides** to the native single-op intrinsic; **AMD overrides** to `ds_bpermute` (its `waveShuffle`=`readlane` requires a *uniform* source lane, but rotate's source is divergent — the probe finding). Cross-lane, so it sets `usedSubgroupOp_` → maximal reconvergence. Tests: fork lit `subgroup-rotate-vulkan.ll` (emit + spirv-val), `XpuWaveEmitTests.spirvLowersRotateToNativeOpAndValidates`, `XpuWaveDeviceTests.{amdgpu,vulkan}RotateRunsOnDevice` (`out[t]==t+1`, cross-checking the native op direction against the portable default). Tour `runWaveRotate` (`out[0..3]=1 2 3 4` on Vulkan) + `cajeta-docs/SubgroupRotate.md`. Follow-on: clustered rotate (the optional ClusterSize operand), signed/64-bit value.
  - [x] **Wave prefix scans — DONE 2026-06-06, device-verified (Vulkan/RADV native + AMD/gfx1151 ds_bpermute, bit-exact; CPU via Tour).** Surfaced as **`Wave.prefixSum`/`prefixProduct(uint32) → uint32`** (EXCLUSIVE scan; lane 0 = identity). Probe finding: **only Vulkan has a native subgroup scan** (`OpGroupNonUniform{IAdd,IMul}` ExclusiveScan, via the fork's pre-existing `spv_wave_prefix_{sum,product}` intrinsics — zero new fork work); AMD/NVPTX have NO scan intrinsic (only the shuffle primitives). So: a new non-pure `LoweringTarget::waveScan` seam with a **portable Hillis-Steele default** (log₂(width) steps) built on a new **`waveShuffleDivergent` seam** (default = `waveShuffle`; **AMD overrides to `ds_bpermute`** since its `readlane` shuffle is uniform-index-only — the rotate lesson generalized); **Vulkan overrides** `waveScan` to the native op; **CPU overrides** to a VFABI in-lane scan variant (`CpuRegistration` + 2 runtime stubs). The AMD device test validates the Hillis-Steele algorithm (AMD runs the default + ds_bpermute); Vulkan validates the native op; both agree (`prefixSum(1)→i`, `prefixProduct(2)→2^i`). Tests: `XpuWaveEmitTests.spirvLowersPrefixScanAndValidates`, `XpuWaveDeviceTests.{amdgpu,vulkan}PrefixScanRunsOnDevice`. Tour `runWavePrefix` (`0 1 2 3 4 5 6 7` on CPU + Vulkan) + `cajeta-docs/WavePrefixScan.md`. Remaining wave follow-ons: inclusive scans (= exclusive + own), `reduceProduct` (Vulkan `spv_wave_product` native, but AMD/NVPTX need a shuffle-tree), signed min/max (needs an int32 wave surface — today's wave ops are uint32).
  - [x] **Uniform group arithmetic / wave reductions — DONE 2026-06-06, device-verified (Vulkan/RADV + AMD/gfx1151 bit-exact; CPU via Tour).** **NO LLVM patch, NO extension** — the key probe finding: `SPV_KHR_uniform_group_instructions` (`OpGroupIMulKHR`…) is OpenCL-builtin-only (`isShader()`-gated off, never reaches the Vulkan flavor), but the **`GroupNonUniformArithmetic` Reduce family** gives the same reductions, is non-uniform-control-flow-safe, and is **already Shader-reachable** — AND the fork already has every reduce intrinsic + GlobalISel selection (`spv_wave_reduce_{umax,umin,and,or,xor}`, verified). So zero fork work. Surfaced as **`Wave.reduce{Max,Min,And,Or,Xor}(uint32) → uint32`** (unsigned min/max; joins the existing `reduceSum`). Lowering: a single enum-parameterized `LoweringTarget::waveReduce(op, value)` seam; **Vulkan** → `spv_wave_reduce_*` → `OpGroupNonUniform{UMax,UMin,BitwiseAnd,BitwiseOr,BitwiseXor}`; **AMD** → `amdgcn.wave.reduce.{umax,umin,and,or,xor}`; **NVPTX** → `nvvm.redux.sync.{...}` (sm_80+, emit-only); **CPU** → VFABI vector-variant reduce (5 new `__cajeta_xpu_wave_reduce_*_u32` stubs + `CpuRegistration` reduce builders with per-op masked-lane identity). Sets `usedSubgroupOp_` → maximal reconvergence. **Product intentionally excluded** (neither AMD `wave.reduce` nor NVPTX `redux.sync` has a multiply-reduce — a shuffle-tree follow-on). Tests: `XpuWaveEmitTests.spirvLowersReduceFamilyAndValidates` (5 ops + spirv-val), `XpuWaveDeviceTests.{amdgpu,vulkan}ReduceFamilyRunsOnDevice` (Max/Min/And/Or/Xor bit-exact, wave-width-agnostic inputs). Tour `runWaveReduceOps` (`100 5 3 3 1` on CPU + Vulkan — the CPU run exercises the VFABI path; note: per-lane store is required, since a wave op consumed only under divergence is predicated on CPU) + `cajeta-docs/WaveReductions.md`. Follow-ons: `reduceProduct` (shuffle-tree), signed min/max, scans (inclusive/exclusive prefix — the fork `spv_wave_prefix_*` intrinsics exist).
- [⊘] **Physical storage buffer / buffer device address** (`SPV_KHR_physical_storage_buffer`) — **EXCLUDED FROM THE SPEC BY DESIGN (decision 2026-06-06).** PSB is the *raw 64-bit device pointer* mechanism (addresses in data, `inttoptr` to `addrspace(7)`, pointer-chasing). It is fundamentally anti-cajeta: a pointer stored in untrusted bytes is a wild pointer — exactly what `view` forbids (`cajeta-docs/stdlib/Views.md`) and what the borrow model excludes. **It also unlocks no efficiency we lack:** on GPU hardware a pointer load and an indexed-arena load are the *same* memory instruction (`base + offset`), and index-into-arena (`view` over `Buffer<uint8>` + integer handles) is how fast CUDA/HLSL data structures are written anyway — 32-bit handles are *smaller* (bandwidth/cache), relocatable, and bounds-checkable. So trees / graphs / linked lists / hash tables / BVHs / ECS are all expressible at equal-or-better efficiency the cajeta way. PSB's only unique capabilities are (1) cross-*buffer* pointer addressing — covered by **bindless descriptors** (B4) with one cached indirection — and (2) consuming *external* raw device pointers across an **FFI boundary** (the lone genuine gap, an interop concern, not a data-structure one). **The aligned path for "arbitrary-structured device buffers"** (the data-science / game-engine need) is **device Views** (`view` overlay over `Buffer<uint8>`, Logical addressing, no fork, no raw pointers) + **bindless** for the many-buffers case. **Reconsider clause:** revisit ONLY if a compelling need appears (e.g. mandatory FFI with pointer-based external device code) AND a viable integration strategy exists that confines raw addresses to a narrow interop boundary — never as a general data-structure idiom.

**Tier 2 — lowerable but wired OpenCL-flavor only → port to the Shader flavor (medium: capability/addressing wiring, no new opcodes):**
- [x] **Cooperative matrix** (`SPV_KHR_cooperative_matrix`, `OpTypeCooperativeMatrixKHR`) — **tensor / matrix-core MMA; the single biggest ML lever (Prism / SPELA matmul).** **CM1–CM5 complete: cajeta source → SPIR-V → real RDNA3 WMMA cores, bit-exact (2026-06-04).** CM6 (LDS-staged load source) remains as a deferred throughput optimization. Tested under `spirv64 --spirv-ext`; needs Shader-capability reach. **Scoped 2026-06-04** — same shape as ray query (Tier-3 lesson reused): the KHR opcodes exist (`OpTypeCooperativeMatrixKHR` 4456, Load 4457, Store 4458, MulAdd 4459, Length 4460), the capability `CooperativeMatrixKHR` (6022) + extension are already `[EnvVulkan, EnvOpenCL]`, and the parameterized type-build helper (`SPIRVGlobalRegistry.cpp:1745`) is flavor-agnostic — but the only *op* emission path is the OpenCL `__spirv_*` builtin path (`isShader()`-gated off), so the Vulkan flavor cajeta emits needs **`llvm.spv.cooperative.matrix.*` intrinsics + GlobalISel selection** (the texture/ray-query path). RADV exposes `VK_KHR_cooperative_matrix` (`cooperativeMatrix=true`) → exec-validatable on the STRIX_HALO box. Increments:
  - [x] **CM1 — parameterized opaque type under the Vulkan flavor** (2026-06-04, fork, emit-only): `target("spirv.CooperativeMatrixKHR", elem, scope, rows, cols, use)` → `OpTypeCooperativeMatrixKHR` under `spirv-unknown-vulkan1.3-compute`, gated by `CooperativeMatrixKHR` cap + `SPV_KHR_cooperative_matrix` ext, clean LLVM ERROR without it. **Zero backend changes needed** — the existing `BuiltinType<"spirv.CooperativeMatrixKHR">` (SPIRVBuiltins.td:1812) + `getOrCreateOpTypeCoopMatr` (GlobalRegistry:1744) lower the type flavor-agnostically (same lesson as ray-query's opaque types). Test `cooperative_matrix_type_vulkan.ll` (both RUN lines pass via FileCheck; lit harness itself has a pre-existing base-pin config quirk).
  - [x] **CM2 — the operations via intrinsics** (2026-06-04, fork): added `llvm.spv.cooperative.matrix.{load,store,muladd,splat}` (`IntrinsicsSPIRV.td`, opaque matrix carried as `llvm_any_ty`) + GlobalISel selection (`SPIRVInstructionSelector.cpp`): load/muladd/splat via `selectOpWithSrcs`, store via a new void-op helper `selectCoopMatrixStore`. memory-layout + stride are `<id>` constant operands; float MulAdd omits the integer-only signedness literal (valid). `splat` = single-scalar `OpCompositeConstruct` (zero accumulator). **Deferred:** `OpCooperativeMatrixLengthKHR` (takes a *type* operand — only needed for the manual per-invocation element-fill path, not load/mma/store matmul). Test `cooperative_matrix_ops_vulkan.ll` asserts the full data flow (mul-add consumes both loads + the splat; store consumes the result); ray-query + OpenCL-coop paths regression-clean. The LLParser remangles the bare overloaded intrinsic names from operand types (same as ray query).
  - [x] **CM3 — spirv-val-clean tiled-matmul kernel** (2026-06-04, fork): a `GLCompute` `@main` loading A (use 0) + B (use 1) from descriptor-bound `VulkanBuffer` storage buffers (the same `handlefrombinding`+`getpointer.p11` path Cajeta's `Buffer<T>` uses), splat-zero accumulator C (use 2), `OpCooperativeMatrixMulAddKHR`, store C. Test `cooperative_matrix_kernel.ll` passes FileCheck **and `spirv-val --target-env vulkan1.3`**. **Key finding: cooperative matrix under Shader mandates the Vulkan memory model** — spirv-val rejects Shader + CooperativeMatrixKHR without `VulkanMemoryModel`. Fixed in `SPIRVModuleAnalysis.cpp::setBaseInfo`: detect the `llvm.spv.cooperative.matrix.*` intrinsics in the module → select `MemoryModel::VulkanKHR` (→ pulls `VulkanMemoryModelKHR` cap + `SPV_KHR_vulkan_memory_model` ext, emits `OpMemoryModel Logical VulkanKHR`); non-coop shaders stay `GLSL450` (regression-verified: ray-query kernel + plain shaders unchanged + still spirv-val-clean). The emitter must enable both `SPV_KHR_cooperative_matrix` + `SPV_KHR_vulkan_memory_model`. **Caveat noted:** mixing coop-matrix with workgroup barriers in one kernel would need Vulkan-model barrier scopes — revisit if CM5 kernels combine them.
  - [x] **CM4 — cajeta device-lowering surface (emit + spirv-val, no device)** (2026-06-04): `cajeta.xpu.core.CooperativeMatrix<T, Rows, Cols, Use>` — a real parameterized device-only class (`load`/`splat`/`mma`/`store` instance methods, RayQuery-style placeholder bodies intercepted at call sites). CM4a substrate (non-type param declarations) + CM4b: new `CooperativeMatrix.cajeta`; `KernelArgTrait::isCooperativeMatrixType`; `DeviceLowerer` `coopMatrixSlots` (alloca of the opaque tile) + `buildCoopMatrixType` (reads `<T,Rows,Cols,Use>` type args) + `lowerCoopMatrixMethod` (load→store-to-slot, store→load-slot, splat, mma=load a/b/c→muladd→store); `LoweringTarget` 5 seams (default XPU-N03); `SpirvKernelLowering` overrides emit the CM2 intrinsics under a `CAJETA_HAS_SPV_COOP_MATRIX` CMake probe (type builder unguarded); `SpirvBackend::enableSpirvExtensions` adds `+SPV_KHR_cooperative_matrix,+SPV_KHR_vulkan_memory_model`. Test `XpuVulkanEmitTests.lowersCooperativeMatrixToSpirv`: a cajeta @Kernel 16×16 f32 matmul tile → the intrinsics → `OpTypeCooperativeMatrixKHR` + ops + `OpMemoryModel Logical VulkanKHR`, **spirv-val-clean**. Stdlib structure count 100→101. **Design resolved 2026-06-04:** build on the **non-type template-param substrate — which already exists and is tested** (`CajetaConstantType::of/parseLiteral`, integer template args flow through `fromContext`/cache-keys; `CajetaVector` is the working precedent, 26 Vector/ConstantType tests green). So `CooperativeMatrix<T,Rows,Cols,Use>` mirrors `CajetaVector` for parameterization and `RayQuery` (inc 3a) for device-only kernel-local lowering — no new substrate work. Wire a new `CajetaCooperativeMatrix` type + `KernelArgTrait`/`DeviceLowerer`/`LoweringTarget`/`SpirvKernelLowering` (emit the CM2 intrinsics; enable `+SPV_KHR_cooperative_matrix,+SPV_KHR_vulkan_memory_model` in `SpirvBackend::enableSpirvExtensions`). Test: `XpuVulkanEmitTests` asserts the type + ops + spirv-val-clean.
    - **Surface finding 2026-06-04:** the grammar's `typeParameter` decl rule is type-params-only (`annotation* identifier (EXTENDS typeBound)?`) — **non-type template *parameter declarations* (`<T, uint32 Rows>`) are NOT supported** (only non-type *arguments* flow, via the existing substrate). A `fromContext`-synthesized type (Vector-style) has no class body, so its ops can only be name-intercepted free functions.
    - **Decision 2026-06-04 (option b — most cajeta-natural):** cajeta's dominant idiom is **instance methods on parameterized types** (`Buffer<T>.allocate()`, `RayQuery.proceed()`, `Vector<T,N>`, `Task<T>`); free functions (`dot`/`length`) are the Vector-only exception, not the rule. So make `CooperativeMatrix<T,Rows,Cols,Use>` a **real parameterized class with instance methods** `m.load(src, layout, stride)` / `m.mma(a, b)` / `m.store(dst, layout, stride)` (method bodies are resolution placeholders intercepted at call sites by the device lowerer, exactly like `RayQuery`). This requires the **deferred substrate the Vector plan flagged**: extend the grammar `typeParameter` rule to accept a non-type (integer) parameter (`primitiveType identifier`) + the substitution-stack plumbing so the class body substitutes integer params — a reusable language capability (any future user template, e.g. `Matrix<T,R,C>`, gets it). Strong type-level shape checking + idiomatic methods. Implement substrate first (test-first), then the `CooperativeMatrix` class + lowering seams.
    - [x] **CM4a — non-type template-parameter DECLARATION substrate** (2026-06-04): `class Foo<T, uint32 N>` now declares (the existing substrate only let types *consume* an integer arg, e.g. `Vector<T,N>`). Grammar `typeParameter` gains a `primitiveType identifier` alt; `TypeParameter` carries `isNonType`/`nonTypePrimitive`; both parse sites (`Compiler.cpp`, `CajetaLlvmVisitor.h`) populate it; `TemplateInstantiator` adds a parameter-kind check (non-type param ⇒ integer-constant arg / `CONSTANT_FLAG`; type param ⇒ not), `CAJETA_ERROR_TYPE_PARAMETER_KIND`. Test `NonTypeParamTests` (4): declare+instantiate `Tile<T, uint32 N>`, distinct-N distinct instantiations, both kind mismatches rejected. Template/Vector/Wildcard/Interface suites regression-checked. The integer params are carried in the type (read by the device lowerer) and need not appear in the class body — full body value-substitution of a non-type param stays deferred until a template needs `N` as a runtime value.
    - **load/store memory source — `Buffer<T>` (global StorageBuffer) for now.** A cooperative matrix value is *not* addressable memory: at `Subgroup` scope it lives **distributed across the wavefront's per-invocation registers** (RDNA3 WMMA register file), cooperatively owned — never pointed to. `load`/`store` move it to/from a *pointer whose storage class is the kernel's choice*; v1 uses the bound `Buffer<T>` (global VRAM, addrspace 11, the CM3-proven `handlefrombinding`+`getpointer.p11` path).
  - [ ] **CM6 — workgroup-shared (LDS) load source (DEFERRED)**: let `load`/`store` also target a **Workgroup / shared-memory** tile, not just a global `Buffer<T>`. **What it gains:** the canonical high-performance tiled-matmul staging — cooperatively load a global tile into LDS *once*, then each subgroup `CooperativeMatrixLoadKHR`s its sub-tiles from the fast on-chip shared memory instead of re-reading global VRAM per MMA step. Cuts global-memory traffic and bandwidth pressure on large GEMMs (the real SPELA/Prism matmul regime); also the natural place for cross-subgroup tile reuse. **Why deferred:** the matrix *type* and the load/mma/store ops are storage-class-agnostic — global `Buffer<T>` is sufficient to prove correctness end to end (CM4/CM5) and unblock matmul; LDS staging is a throughput optimization layered on top, and it interacts with the Vulkan-memory-model barrier-scope caveat from CM3 (workgroup barriers around the shared tile need explicit Vulkan-model scopes). Land after CM5 is green.
  - [x] **CM5 — exec on real RADV GPU** (2026-06-04): a single-tile cooperative-matrix matmul running on the STRIX_HALO WMMA cores, result bit-exact vs a CPU reference (the cooperative-matrix analog of `rayQuerySpatialIndexOnDevice`). **Key device finding (probed via `vkGetPhysicalDeviceCooperativeMatrixPropertiesKHR`): the RADV box exposes NO f32-input config** — only int8/int8→int32 and **f16/f16→{f16,f32}** (all M=N=K=16, Subgroup scope). So the CM4 f32 surface is spirv-val-clean but *not device-runnable*; CM5 targets the **mixed-precision f16/f16→f32** config (the SPELA/Prism regime). Increments:
    - [x] **CM5a — IEEE-half `float16`**: cajeta's `float16` mapped to LLVM `bfloat` (a latent misnomer — bf16 ≠ binary16); the device's `f16` configs are `VK_COMPONENT_TYPE_FLOAT16_KHR` (IEEE half) and it lists no bf16 config. Remapped `float16` → `llvm::Type::getHalfTy` so it emits `OpTypeFloat 16` (+ `Float16` capability) matching the device component type. Low-risk: only the registration, a reverse-map switch (already handled `HalfTyID`), the generic FPCast path, and one storage round-trip test touched `float16`. (`bfloat16` is reserved as a *future distinct* keyword → `bfloat`.) 31 FP/float tests green.
    - [x] **CM5b — mixed-type f16→f32 mma (no surface change)**: the `CooperativeMatrix<T,...>` surface needed **zero change** — device kernel bodies bypass front-end type resolution (a `@Kernel` with a `Buffer<T>` param gets a host stub + early return), and the `DeviceLowerer` keys each tile off its own *declared* element type while every seam (`load`/`store`/`muladd`) is independently overloaded on concrete types. So A/B `CooperativeMatrix<float16,16,16,0/1>` + accumulator `CooperativeMatrix<float32,16,16,2>` lowers correctly: `coopMatrixMulAdd` already overloads on `{result(f32), a(f16), b(f16), c(f32)}`. New emit test `lowersMixedPrecisionCooperativeMatrixToSpirv` asserts `OpCapability Float16` + `OpTypeFloat 16` + the f16/f16→f32 `OpCooperativeMatrixMulAddKHR`, **spirv-val-clean**.
    - [x] **CM5c — on-device exec**: `VulkanDriver::coopMatrixAvailable()` probe (checks the device lists the 16×16×16 Subgroup f16/f16→f32 config) gates a new `XpuCooperativeMatrixDeviceTests.mixedPrecisionMatmulOnDevice`. Mirrors the wave/saxpy device path (compile → `emitSpirv` → `VulkanDriver.launch` with raw half/float byte buffers, bypassing the cajeta `Buffer<T>` host marshalling). One 16×16×16 tile, inputs small integers exactly representable in half → **bit-exact** equality check vs CPU reference. Dispatch `localSize=64` (= 2 wave32 subgroups, each redundantly computing the identical tile). **Passed on the real device (1863 ms).**
  - [x] **GEMM — multi-tile f16→f32 matmul on device** (2026-06-04): the usable matmul on top of the CM5 single-tile primitive, the matmul SPELA/Prism can call. Two increments:
    - [x] **GEMM-1 — offset/strided tile load+store**: `load`/`store` gain an `offset` arg → `load(Buffer<T> src, uint32 offset, uint32 layout, uint32 stride)`, selecting a sub-tile of a larger row-major matrix (the GEP index into `bufferElementPtr`, previously hardwired to 0). New device test: `C[16×16] = A[16×32]·B[32×16]` via a `for`-loop over two K-tiles with runtime offsets, accumulating in place (`mma` is `C = A*B + C`). Bit-exact on device.
    - [x] **GEMM-2 — full tiled M×N×K GEMM**: a generic `@Kernel` — one 16×16 output tile per workgroup (decoded from `Workgroup.x()`), K-loop over the depth, `rows/cols/depth` as scalar args (lowered to single-element storage buffers on Vulkan). Dispatched as `(M/16)*(N/16)` workgroups; 64×64×64 runs **bit-exact** on the device. Tile offsets are written naturally inline (the source-level hoist workaround was removed once the backend bug below was fixed).
    - [x] **SPIR-V backend fix — `SPIRVFixupMergePlacement`** (2026-06-04, fork): GEMM-2 first **hung the GPU**. Root cause (bisected via `llc -print-after-all`): MachineCSE commons the loop-invariant tile offsets (`mi*16`, `nj*16`, computed redundantly in both the loop body and the post-loop store) and sinks the survivor into the loop header — placing it *after* the `OpLoopMerge`, between the merge and its branch. That violates SPIR-V's "merge immediately precedes the branch" rule (spirv-val rejects it; RADV consumes it and deadlocks). Fix: a new MachineFunction pass `SPIRVFixupMergePlacement` scheduled at the end of `addMachineSSAOptimization` (after MachineCSE) that re-seats any displaced `OpLoopMerge`/`OpSelectionMerge` to immediately before its block terminator — always safe, since merges carry only block-label/immediate operands (no SSA deps). Regression test `llvm/test/CodeGen/SPIRV/structurizer/fixup-merge-placement.mir` (`-run-pass`); structurizer + coop-matrix lit suites still pass. With this, the natural (un-hoisted) GEMM kernel runs bit-exact on device.
- [x] **bfloat16 arithmetic** (`SPV_KHR_bfloat16`) — **ALREADY DONE (checkbox was stale; verified 2026-06-06, device-verified on Vulkan/RADV + AMD/gfx1151 + CPU).** Landed incrementally via the B2 Vector-element work + CM7 cooperative matrix, not as a separate C2 task. State: `bfloat16` is a registered native type (`CajetaType.cpp` → `llvm::Type::getBFloatTy`, distinct from `float16`=binary16=`getHalfTy`); **device arithmetic** uses the standard portable "**bf16 storage, f32 compute**" model (`KernelLowering::applyBinOp` widens bfloat→f32, ops, narrows back — native bfloat arithmetic is Intel-only `SPV_INTEL_bfloat16_arithmetic`, absent on RADV/AMD, so the Shader flavor never emits it); **host arithmetic** is native LLVM `bfloat` (`BinaryOpExpression::emitFpBinOp`); `SpirvBackend` enables `+SPV_KHR_bfloat16` (the type + conversions). Tests (all passing on real hardware): `XpuVectorDeviceTests.bfloat16RunsOn{Cpu,VulkanDevice,AmdDevice}` (`Vector<bfloat16,4>` arithmetic), `XpuCooperativeMatrixAmdDeviceTests.bf16WmmaMatmulOnDevice` (native RDNA3 WMMA bf16→f32), `XpuCooperativeMatrixDeviceTests.bf16SoftwareMatmulOnDevice` (portable software tier). The only unimplemented bf16 surface is *native* bf16 arithmetic ops (Intel-only, no RADV/AMD config — correctly excluded).
- [ ] **fp16 vector atomics** (`SPV_NV_shader_atomic_fp16_vector`)

**Tier 3 — absent in BOTH flavors → full lowering (the real backend work, fork-carried until upstreamed):**
- [~] **Ray query + acceleration structures** (`SPV_KHR_ray_query`) — **in progress in the `cajeta-spirv` fork branch, TDD per increment.** Was only NV enum stubs. Findings: the zero-param opaque types use the backend's **default `getNonParameterizedType` lowering** (no custom helper needed, simpler than coop-matrix); ray query is **`[EnvVulkan]`-only** (rejected under the OpenCL `spirv64` triple) — so it's inherently the Vulkan/Shader flavor Cajeta emits. Tests live in `llvm/test/CodeGen/SPIRV/extensions/SPV_KHR_ray_query/`.
  - [x] **Increment 1 — `OpTypeRayQueryKHR` opaque type** (2026-06-03): `target("spirv.RayQueryKHR")` → `OpTypeRayQueryKHR` under `spirv-unknown-vulkan1.3-compute`, gated by `RayQueryKHR` capability + `SPV_KHR_ray_query` extension (clean fatal error without it). 5 backend edits — `SPIRVInstrInfo.td` (Op<4472>), `SPIRVBuiltins.td` (BuiltinType), `SPIRVSymbolicOperands.td` (RayQueryKHR cap<4472>), `SPIRVInstructionSelector.cpp` (isTypeInst), `SPIRVModuleAnalysis.cpp` (requirement+error). Test `ray_query_type.ll` green (text emission; spirv-val deferred to inc 2's real kernel). Incremental `llc` rebuild ≈ 14 s.
  - [x] **Increment 2a — `OpTypeAccelerationStructureKHR` opaque type** (2026-06-03): `target("spirv.AccelerationStructureKHR")` → `OpTypeAccelerationStructureKHR` (opcode 5341 — **tablegen accepts the duplicate Op<5341> alongside the existing NV def**, so the KHR name prints directly), same RayQueryKHR cap + extension gating. 4 edits (InstrInfo/Builtins/InstructionSelector/ModuleAnalysis). Test `acceleration_structure_type.ll` green. **Both opaque types now lower under the Vulkan flavor.**
  - [x] **Increment 2b — the operations** (2026-06-03, via `llvm.spv` intrinsics): `OpRayQueryInitializeKHR`/`ProceedKHR`/`GetIntersectionTypeKHR` all emit under the Vulkan flavor. Test `ray_query_ops.ll` green (FileCheck). **Key finding (cost a dead end first):** the `__spirv_*` builtin-call path is gated `if (!STI.isShader())` (`SPIRVPrepareFunctions.cpp:473`) — OpenCL/Kernel-only — while `SPV_KHR_ray_query` is `[EnvVulkan]`-only → mutually exclusive, so builtins can **never** lower ray query (verified: the calls stayed `LinkageAttributes Import`). Ops must (and now do) use **`llvm.spv.ray.query.*` intrinsics + GlobalISel selection** — the texture-style path that runs for every flavor. Implementation: 3 intrinsics (`IntrinsicsSPIRV.td`), 3 opcodes (`SPIRVInstrInfo.td`), and selection (`SPIRVInstructionSelector.cpp`) — `selectOpWithSrcs` for the valued Proceed/GetType, a small `selectRayQueryInitialize` for the void 8-operand init. Emitted shape verified: `OpVariable Function` rq + 8-operand init + valued proceed/get. (Unmangled intrinsic calls work — LLVM infers the overload from arg types.)
  - [x] **Increment 2c — spirv-val-clean kernel** (2026-06-03): a `GLCompute` entry with a **descriptor-bound** acceleration structure running the spatial-index pattern (init → `OpLoopMerge` proceed loop → inspect candidate type → read committed type). Test `ray_query_kernel.ll` passes both FileCheck and `spirv-val --target-env vulkan1.3`. **Key finding: zero new backend code needed.** The AS handle comes from the standard `llvm.spv.resource.handlefrombinding` intrinsic, and `OpTypeAccelerationStructureKHR` falls into the *generic non-image branch* of `loadHandleBeforePosition` (`SPIRVInstructionSelector.cpp`), which already emits exactly what's required: an `OpVariable UniformConstant` decorated `DescriptorSet 0`/`Binding 0`, then an `OpLoad` of the AS handle feeding `OpRayQueryInitializeKHR`. So Cajeta binds the AS exactly as it binds textures/buffers — no special path. (The by-value-param emission tests from 1/2a/2b are kept as minimal lowering checks; they intentionally pull in `Linkage` and are deliberately *not* run under `spirv-val` — 2c is the validity proof.) The type-level `SPIRVModuleAnalysis` requirements (cap `RayQueryKHR` + ext `SPV_KHR_ray_query`) were sufficient; no op-level requirements needed.
  - **Increment 3 — Cajeta surface + host AS build** (split into 3 sub-increments; the language/device surface is verifiable now, the host BVH build needs an RT-capable device):
    - [x] **3a — device-lowering surface (emit + spirv-val, no device needed)** (2026-06-03): new runtime types `cajeta.xpu.core.AccelerationStructure` (kernel-arg handle, bound like Texture2D) and `cajeta.xpu.core.RayQuery` (device-only kernel local; methods `initialize`/`proceed`/`committedType`/`candidateType`, origin/direction passed as scalar triples so no Vector<T,N> dep). Wired: `KernelArgTrait` (admit AS by canonical name; `isAccelStructType`/`isRayQueryType`), `KernelParam.isAccelStruct` + `collectParams`, `SpirvKernelLowering` (`vkAccelStructType()`/`vkRayQueryType()`, AS bound via the *existing* `handlefrombinding` path — proven in 2c; 4 ray-query seam overrides), `LoweringTarget` (4 new seam methods, default→XPU-N02 on non-Vulkan), and `DeviceLowerer` (`RayQuery` local → `alloca target("spirv.RayQueryKHR")`; method-call interception → `llvm.spv.ray.query.*`; `makeVec3`/`resolveAccelArg` helpers). Test `XpuVulkanEmitTests.lowersRayQueryToSpirv` green: IR has handlefrombinding + the 3 intrinsics; SPIR-V text has `OpCapability RayQueryKHR`/`OpTypeAccelerationStructureKHR`/`OpRayQueryInitializeKHR`/etc.; **binary passes `spirv-val --target-env vulkan1.3`**. No regressions (all Vulkan/NVPTX/AMD/CPU/Wave emit suites green). Two surface gotchas fixed: Cajeta's boolean primitive is `boolean` (not `bool`), and a zero-field class has a null LLVM type (RayQuery needed a placeholder field). **Build wiring (important):** the in-process SPIR-V emitter needs the extension enabled — added `enableSpirvExtensions()` in `SpirvBackend.cpp` that sets the `spirv-ext` cl::opt to `+SPV_KHR_ray_query` (the SPIRVSubtarget header isn't shipped in the artifact, so we drive the registered option). And cajeta's local build was repointed to the fork (`LLVM_DIR=cajeta-llvm/build-cajeta/lib/cmake/llvm`) — verified safe because `cajeta-llvm` HEAD~5 == the `llvm-project` checkout cajeta previously used (`203c0668d`), i.e. the fork is exactly that base + the 5 additive ray-query/CI commits.
    - [x] **3b — host BVH build + descriptor binding (EXEC-verified on real RT GPU)** (2026-06-03): the runtime Vulkan path (`cajeta_runtime.c`, which is the *actual* device path — the C++ `VulkanDriver.cpp` is compiler/test-only) gains the full BVH path. **Device bring-up** now probes for the RT triad (`VK_KHR_acceleration_structure` + `VK_KHR_ray_query` + `VK_KHR_deferred_host_operations` + `VK_KHR_buffer_device_address`) plus the matching feature bits via `vkEnumerateDeviceExtensionProperties`/`vkGetPhysicalDeviceFeatures2`, and **conditionally** enables them (chained `VkPhysicalDevice{RayQuery,AccelerationStructure,BufferDeviceAddress}Features` in `VkDeviceCreateInfo.pNext`) — a non-RT GPU is created exactly as before, `g_xpu_vk.rayQuery` stays 0, and the AS natives no-op, so the plain compute/texture path is untouched. **AS build** (`cajeta_xpu_vk_accel_build_aabbs`): device-address buffers (`VK_MEMORY_ALLOCATE_DEVICE_ADDRESS_BIT`) for AABB input / AS store / scratch; AABB geometry (`VkAabbPositionsKHR` is byte-identical to cajeta's 6-float box, so the `float32[]` uploads straight in); `vkGetAccelerationStructureBuildSizesKHR` → `vkCreateAccelerationStructureKHR` → `vkCmdBuildAccelerationStructuresKHR` (BOTTOM_LEVEL, PREFER_FAST_TRACE); AS+store kept in a table, AABB+scratch freed. **Descriptor bind**: new `CAJ_VKB_ACCEL` kind → `VK_DESCRIPTOR_TYPE_ACCELERATION_STRUCTURE_KHR` in the layout/pool/writes (write chains `VkWriteDescriptorSetAccelerationStructureKHR` via pNext); new `CAJETA_KP_ACCEL` param kind so `cajeta_xpu_launch_vulkan` routes the AS arg (and `KernelParamInfo::AccelStruct=4` + `collectKernelParamInfo` emit it). **Native host entries** `__cajeta_xpu_accel_build_aabbs`/`__cajeta_xpu_accel_free` (the `AccelerationStructure.cajeta` `@Native` forwards; floats at array-header offset 8, Vulkan-only — other backends return 0/no-op). C++ `VulkanDriver::rayQueryAvailable()` is a self-contained test gate (enumerates ext+features2, no logical device). Exec test `XpuVulkanDispatchDeviceTests.rayQuerySpatialIndexOnDevice`: a BLAS over one AABB `[0,1]³`, a kernel that casts a degenerate near-zero ray per query point and counts AABB candidates (RTNN pattern) — inside-box point → ≥1 candidate, outside → 0 — **ran green on a real AMD Radeon 8060S (RADV STRIX_HALO)** (777). Full XPU suite 185 passed / 7 skipped (CUDA/HIP — no device) / 0 failed; no regressions.
    - [x] **3c — Prism `SpatialIndex` (P1.0, EXEC-verified on real RT GPU)** (2026-06-03): the library primitive consuming 3a/3b lands as Prism's first real code — `cajeta-prism/src/prism/spatial/SpatialIndex.cajeta` (new `cajeta-prism` sibling repo + README). A `SpatialIndex` builds a bottom-level AS (BVH) over per-point AABBs (each datum wrapped in a half-extent-`r` box) and exposes the fixed-radius verb `countWithin(qx,qy,qz,out,n)`; the degenerate near-zero-ray / AABB-as-index / candidate-walk (RTNN pattern) is hidden inside an internal `@Kernel` — the user writes `idx.countWithin(...)`, never a ray. Exec test `PrismSpatialIndexDeviceTests.fixedRadiusCountOnDevice` (cajeta repo; reads the authoritative SpatialIndex source from the sibling repo, compiles it with a driver via the multi-source JIT, gated on `rayQueryAvailable()` + source presence) **ran green on the AMD Radeon 8060S (RADV STRIX_HALO)** (neighbour counts 1/0/1/1 → 777). Remaining P1 (knn/radius/contains/range verbs, custom-predicate exact-L2, refit/rebuild policy, compute fallback, precision safety, RT→tensor handoff) tracked in `cajeta-prism-plan.md` P1.1–P1.6.
  - [ ] Upstream-first PR once the lowering is complete; carry on `cajeta-spirv` until it lands.
  - [ ] Upstream-first PR once the lowering is complete; carry on `cajeta-spirv` until it lands.
- [ ] **fp8 / E4M3 / E5M2** float types — 0 backend files (only bfloat16 present). ML low-precision training/inference.
- [ ] **Cooperative vector** (`SPV_NV_cooperative_vector`) — 0 files — small-matrix / neural-shading MMA (newest)
- [ ] **Tensor addressing** (`SPV_NV_tensor_addressing` / `TensorNV`) — 0 files
- [ ] **Quad control** (`SPV_KHR_quad_control`), **replicated composites** (`SPV_EXT_replicated_composites`) — 0 files — minor

### Stage C3 — Sequencing (prioritized for compute · science · ML)
- [~] **1. C0 infra first** — the fork + prebuilt-artifact pipeline; in place (fork + artifact + local consumption); CI-consume + fork-README refresh pending
- [x] **2. Tier-1 probe sweep** — per-feature emit probes (the `GfxSpirvEmitProbe` pattern); surfaced: integer-dot, float + integer atomics, shader clock, bit instructions, subgroup rotate/reduce/prefix-scan, maximal reconvergence
- [x] **3. Ray query + acceleration structures** (Tier 3) — delivered, on-device (Inc 1–3c)
- [x] **4. Cooperative matrix → Shader flavor** (Tier 2) — delivered, on-device (CM1–CM5 + GEMM)
- [~] **5. bf16 (port) + fp8 (add)** — bf16 element type + arithmetic landed; **fp8 deferred** (no LLVM backend support yet)
- [ ] **6. Cooperative vector + tensor addressing** (Tier 3) — newest neural primitives
- [~] **7. Minor**: subgroup/bit/reconvergence confirmed + surfaced; quad control + replicated composites not started

### Placement: what is `cajeta-gpu` vs `cajeta-gfx`
By the shared-foundation rule (*if both xpu and gfx need it → gpu*):
- **`cajeta-gpu` (here):** ray query + acceleration structures, cooperative matrix, float atomics, integer dot, bf16/fp8, cooperative vector, tensor addressing, physical storage buffer — **all compute-callable from an ordinary kernel**, needed by both science/ML (`cajeta-xpu`) and inline-RT graphics (`cajeta-gfx`).
- **`cajeta-gfx` (NOT here):** the **full RT pipeline** (raygen / closest-hit / miss / any-hit / callable execution models, ray-payload & hit-attribute storage classes, `OpTraceRayKHR`, shader binding table) and mesh/task shaders — rendering-specific shading model. Cross-referenced in `cajeta-gfx-plan.md`.

**Why ray query is foundation, not graphics — prior art (compute uses of RT, not rendering):** Monte-Carlo radiation/particle transport (reactor & medical dosimetry); fixed-radius / k-NN search (RTNN, PPoPP 2022) → MD neighbor lists, point clouds, clustering; computational-geometry predicates (point-in-mesh, visibility, range queries); wave/field propagation (NVIDIA Sionna RT for 5G/6G, room acoustics, seismic tomography); CT reconstruction (line-integral projection); robotics motion planning; astrophysical radiative transfer. Scientific users want **`rayQuery` inside a compute kernel** over a BVH — *not* the rendering RT pipeline — which is exactly why the ray-query lowering belongs in the shared foundation. *(Domain list recalled from training; citations to be firmed up before this justifies the backend spend.)*

---

## Platforms — Apple / macOS (Metal)

**The crux: macOS has no native Vulkan/SPIR-V — Apple is Metal-only.** Apple deprecated
OpenGL/OpenCL and never shipped Vulkan; the GPU stack is **Metal** (API) + **MSL/AIR**
(shader language / IR, AIR being Apple's closed LLVM-bitcode dialect). Our SPIR-V-centric
design therefore needs a bridge to reach Apple GPUs. There are exactly two, and they form
a natural two-tier strategy. **Note:** this is independent of C0 — SPIR-V codegen is
host-agnostic, so the `aarch64-apple-darwin` *toolchain* artifact (deferred in C0) is a
build-out task; the items below are about *device execution* on Apple hardware.

### Tier 1 — MoltenVK (Vulkan→Metal). Works with ~no Cajeta backend changes.
MoltenVK is a Khronos (Apache-2.0) Vulkan implementation layered on Metal. The fit is exact:
our runtime **already loads Vulkan via `dlopen` with no link-time dependency**, so on macOS
it loads MoltenVK's `libvulkan.dylib` instead of a native ICD — the SPIR-V emission path is
unchanged. Baseline compute (`cajeta-xpu`) + graphics (`cajeta-gfx`) light up for ~the cost
of bundling MoltenVK + a dlopen name.
- The gap is **feature coverage** — MoltenVK is strong on core compute/graphics but weak/
  partial on **exactly the Part C features we care about most**:
  - **`VK_KHR_ray_query`** — only recently/partially mapped to Metal raytracing, M3+ only,
    experimental → the Prism RT-as-compute story is shaky here.
  - **`VK_KHR_cooperative_matrix`** — effectively **not** exposed (Metal's `simdgroup_matrix`
    MMA isn't surfaced through this Vulkan extension). The single biggest gap.
  - **`VK_KHR_buffer_device_address`** — partial, and our AS build relies on it.

### Tier 2 — a native Metal backend. For what MoltenVK can't do.
A new `metal` XPU backend alongside `cpu`/`spirv`/`cuda`/`hip`. The pragmatic path **reuses
our SPIR-V emission**: SPIR-V → **SPIRV-Cross** → **MSL** → Metal runtime compile
(`newLibraryWithSource` / precompiled `.metallib`), with a new `LoweringTarget` + a Metal
driver via **`metal-cpp`** (Apple's official C++ headers). There is **no public LLVM→AIR/Metal
backend** (Apple's Metal compiler is closed), so transpiling from SPIR-V is the realistic
route — not a direct LLVM target. This unlocks the Apple hardware that matters:
- **Metal raytracing** (M3/M4 hardware RT) → reliable ray query → Prism `SpatialIndex` on Apple.
- **`simdgroup_matrix`** → cooperative-matrix / MMA → **SPELA forward training + Prism tensor
  path on Apple Silicon's GPU**.

### Per-spec
| Spec | Tier 1 (MoltenVK, now) | Tier 2 (native Metal, later) |
|---|---|---|
| **cajeta-gpu** | value types, math, memory, textures, basic compute ✓ | ray query (Metal RT), cooperative matrix (`simdgroup_matrix`) |
| **cajeta-xpu** | a Vulkan-via-MoltenVK device, reusing the SPIR-V backend ✓ | a first-class `metal` backend (full perf + features) |
| **cajeta-gfx** | MoltenVK is mature for graphics (how most macOS games ship) ✓ | mesh shaders, native Metal RT pipeline, MetalFX upscaling |

### Roadmap impact (why Tier 2 matters for us specifically)
The two pillars of the ML/RT thesis — **Prism's RT-as-compute spatial index** and **SPELA
cooperative-matrix training** — are exactly the two Tier-1 gaps (ray query + coop matrix).
So MoltenVK makes Apple a *baseline* compute/graphics target but **not** a first-class home
for the ML/RT work; cooperative matrix in particular is **Tier-2-only**.

### Sequencing (mirrors the NVIDIA/AMD approach: Vulkan first, native later)
- [ ] **MV1 — MoltenVK bring-up.** Bundle/`dlopen` MoltenVK on macOS; run SAXPY / textures /
  Prism compute-fallback on Apple Silicon (Metal-via-Vulkan). ~zero backend work. The "it
  works on a Mac today" milestone. *(First concrete increment.)*
- [ ] **MV2 — MoltenVK capability probe** (same pattern as the Vulkan ray-query SPIR-V probe,
  `test/gpu/GpuRayQueryProbeTests.cpp`): pin down *exactly* what `VK_KHR_ray_query` /
  `cooperative_matrix` / `buffer_device_address` MoltenVK exposes on M3/M4, so Tier 2's scope
  is measured, not guessed.
- [ ] **MT1 — native Metal backend, cooperative matrix first** (the ML lever, Tier-2-only):
  `LoweringTarget` + Metal driver (`metal-cpp`), SPIRV-Cross→MSL, `simdgroup_matrix`.
- [ ] **MT2 — Metal raytracing** for ray query (improves on MoltenVK's partial support).

### Unknowns to verify (de-risk before committing Tier-2 spend)
- MoltenVK's **exact** ray-query + coop-matrix + BDA support on M3/M4 (→ probe MV2).
- SPIRV-Cross fidelity for our compute SPIR-V (workgroup/subgroup ops, shared memory,
  `<N×T>` vectors, atomics) → MSL — any constructs that don't round-trip cleanly.
- Whether a precompiled-`.metallib` path is needed (vs runtime MSL compile) for startup cost.
- Licensing/packaging: MoltenVK (Apache-2.0) + `metal-cpp` (Apple) are bundleable; confirm.

---

## Definition of done for the foundation

**Status checkpoint (2026-06-07):** the two early-exception blockers are **delivered** —
ray query (Inc 1–3c, on-device) and cooperative matrix (CM1–CM5 + GEMM, on-device).
**B1 (linear-algebra value types — Matrix/Quaternion/swizzles/comparison-masks/
cross-reflect-refract) and B2 (device math intrinsics) are landed**, and **B3 is partial**
(writable + readable storage images — `WritableImages.md`); B4/B5 remain. The capability
matrix (`xpu/CajetaXPU-Matrix.md`) now carries the CM/RQ rows + an honest
emit-only-vs-on-device NVIDIA column; the value-type contract stays unfrozen until the
remaining B3–B5 work lands. C0 infra is in place (fork + artifact + local consumption)
with CI-consume + fork-README refresh pending.

- [ ] A3/S6 closed (or deferred with a tracked reason); A5 NV fatbin done or deferred
- [ ] Part B Stages B1–B5 landed, each test-gated on CPU + on-device (AMD/VK; NV once B5 lands hardware)
- [ ] The capability matrix is honest and current (every cell `native`/`abstraction`/`forks`/`not possible`; emit-only vs on-device distinguished)
- [ ] The value-type / math / texture / memory surface is documented as a **frozen dependency contract** that `cajeta-xpu` and `cajeta-gfx` target
- [ ] **Part C is feature-completeness, largely parallel to the freeze** — B1–B5 define the *frozen contract*; Part C keeps extending the cutting-edge call surface afterward. The exceptions that *should* land early because downstream depends on them: **C0** (fork+artifact infra — gates every later backend patch) and the **ray-query** + **cooperative-matrix** items (science/ML blockers).

When checked, both dependent specs can build on a stable foundation.

---

*The foundation is the shared substrate; it does not by itself run a kernel or draw a
frame. The compute execution model is `cajeta-xpu`; the graphics pipeline is
`cajeta-gfx`. Both depend on this spec.*
