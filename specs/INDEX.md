# Active work

One row per in-flight spec. Rows are added when a spec is created (`draft`),
flipped to `active` on approval, and removed when the plan closes (spec →
`archive/`, plan → `agents/archive/`). See `td-project-workflow.md`.
`research-platform-roadmap-spec.md` holds the long-horizon view for the
**research-platform** axis (PyTorch/JAX table stakes);
`datascience-platform-roadmap-spec.md` does the same for the
**data-science** axis (sklearn/statsmodels/NetworkX/Spark/matplotlib roles)
and records the stdlib / `dev.cajeta.ml` / new-library grouping.

**Open questions closed 2026-08-01.** All 16 data-science-track specs had their
open-question sections resolved in one pass — 15 load-bearing decisions taken
directly, the remainder accepted on their stated recommendation, and 11 stale
items closed where a later decision had already answered them. What remains
open is listed per spec and is action rather than judgement (an audit, a
version pin, a packaging choice). Two consequences are structural and are
tracked below: `cajeta-text-shaping` is a **new spec not yet written**, and
`cajeta-rag` moves **last** in the build order.

| Spec | Plan | Status |
|------|------|--------|
| [plugin-output-protocol](plugin-output-protocol-spec.md) | [plan](../agents/plugin-output-protocol-plan.md) | **active** — spec approved 2026-08-23. The build tool ships a typed plugin API (`ActionContext`/`ActionResult`/`Finding`) and stops short of the OUTPUT channel, so every plugin hand-writes the wire format — `dev.cajeta.coverage` concatenates JSON with no escaping, and a `"` or newline in a message silently corrupts the record. This ships the emitter with the API (a plugin never spells a record), makes plugin output a first-class citizen of the diagnostic stream under `--diag-format=json` (findings/log/warn become diagnostics; output/result/write stay structural), and stamps every record with the plugin's **Olla library key** — provenance the plugin cannot forge, so a reader always knows compiler-vs-plugin and which plugin. An `error` finding fails its task; coco's coverage gate migrates onto findings and gains a non-failing `warn` level so drift is visible before it breaks the build. Published plugins keep working: bad records warn once and drop. |
| [notebook-olla-install](notebook-olla-install-spec.md) | [plan](../agents/notebook-olla-install-plan.md) | `active` (spec approved 2026-08-19) — `cajeta.session.Packages.install`/`installAndSave`: mid-session archive splice (Unit 1, the risk), resolver/cache/checksum reuse, opportunistic signatures + `require-signatures`, manifest graduation. Closes jupyter-kernel Unit 8 on delivery. |
| [stdlib-ownership-convention](stdlib-ownership-convention-spec.md) | [plan](../agents/stdlib-ownership-convention-plan.md) | **active 2026-08-14 — TOP OF STACK.** One convention for who may outlive the caller (producer returns owned / view returns borrow / only sinks take title, the ArrayList model), plus the two compiler checks that make it stick: reject `#x` on a borrow, reject capture of a plain param. From cajeta-llama U13: four ownership bugs, one root cause, only ONE caught at compile time — the rest were corruption and SIGABRT in unrelated frames. `JsonValue.setString(String)` borrows or copies depending on the argument's runtime representation, which no convention survives |
| [test-battery-restructure](test-battery-restructure-spec.md) | [plan](../agents/test-battery-restructure-plan.md) | **active — units 1–5 done, unit 6 deferred (Julian, 2026-08-16).** THE CORPUS IS THE GATE: `./cajeta_tests.sh` runs every test the binary has less 9 stress tests (3,608 in 3,043s); `FULL=1`, `CAJETA_ROUTINE_FILTER` and `routine_filter.txt` are gone. Corpus 6,060 → 3,617 (2,447 deleted, 172 attributed lines given up). light re-derived 35.88%→92.86% (~84s), release 41.25%→85.48% (~94s). Seven blind spots surfaced, each invisible to the last and all found by inspection, never by a failure — stale filter, coverage≠assertions, subprocess zero-attribution, test-to-test string references, a concurrent clone's fix, silently-inert dangling pins, and a JIT symbol colliding with libbsd. Six now fail loudly across six selftested tools. **Remaining: unit 6, coverage to 90%** (currently ~87.5% of indexed lines). |
| [external-debug](external-debug-spec.md) | [plan](../agents/external-debug-plan.md) | active — Units 1–6 delivered and green; one item (5.1.7 cycle-render test) is untestable, awaiting a call on whether to close |
| [ide-symbol-index](ide-symbol-index-spec.md) | [plan](../agents/ide-symbol-index-plan.md) | active — Units 1-9 implemented and green; 8.3.1 (dependency Ctrl-click) confirmed live 2026-07-18; remaining: live-IDE acceptance 8.3.2, 8.3.3, 9.3.1, 9.3.2 |
| [ide-features](ide-features-spec.md) | [plan](../agents/ide-features-plan.md) | active — refactoring, hierarchy, call graph, gutter nav; blocked until ide-symbol-index closes |
| [lint-server](lint-server-spec.md) | [plan](../agents/lint-server-plan.md) | active — Units 1–4 code delivered and green (reuse parity; NDJSON `--lint-server` daemon; warm sibling reuse; plugin LintServerCore/Client + routeLint + settings toggle, 885d200d); remaining 4.3.1/4.3.2 live per-edit latency + recorded numbers, batched with the plugin reinstall |
| [run-config-ergonomics](run-config-ergonomics-spec.md) | [plan](../agents/run-config-ergonomics-plan.md) | active — spec approved 2026-07-18; plan awaiting approval; Units 1-6 unstarted |
| [fast-debug-launch](fast-debug-launch-spec.md) | [plan](../agents/fast-debug-launch-plan.md) | active — Units 1-7 delivered 2026-07-21, tour warm launch 42.6s→0.45s; edit loop spun off to resident-debug-server; remaining: live passes 2.3.1/10.3.x |
| [resident-debug-server](resident-debug-server-spec.md) | [plan](../agents/resident-debug-server-plan.md) | active — Units 1-5+8 delivered 2026-07-21 (tour via one resident server: cold 41.3s / no-edit 0.65s / one-edit 34.8s); next: Unit 6 design review (stdlib digest churn first), Unit 7 live pass |
| [debugger-variable-inspection](debugger-variable-inspection-spec.md) | [plan](../agents/debugger-variable-inspection-plan.md) | active — Units 1–7 server-side delivered and green (leaf/String, array, object fields, DAP expansion, edit-through-refs, evaluate/hover, ArrayList+HashMap logical views; 912b5c57…e42a3285); only plugin-side items remain (4.2.5/4.3.3 page-size + presentation, 6.1.5/6.2.2/6.3.1 plugin evaluator + live hover) — all batched with the plugin reinstall; warm inspection unblocked (debug-type-sidecar closed 2026-07-28) |
| [cajeta-xgboost](cajeta-xgboost-spec.md) | [plan](../agents/cajeta-xgboost-plan.md) | active |
| [cajeta-ml-v3](cajeta-ml-v3-spec.md) | [§13 plan](../agents/cajeta-ml-v3-s13-plan.md) | active — base plan (12 units) archived complete: **dev.cajeta.ml 0.4.0 on Olla 2026-08-02**. Remaining scope is **§13** (positional encodings, encoder-decoder attention, augmentation, transfer learning, pretrained-weight import — verified unbuilt in 0.4.0), re-homed 2026-08-04 as cajeta-ml-v3-s13-plan (U1–U6, release 0.5.0). API doctrine torch-shaped ("We do torch"); keras façade parked |
| [buildtool-resources](buildtool-resources-spec.md) | [plan](../agents/buildtool-resources-plan.md) | **active** — approved 2026-08-01; two mechanisms: bundled resources (`src/main/resources/` → packaged in the `.cja`, readable with **no filesystem capability**) and resolved assets (declared in `cajeta.json`, hash-pinned in a lockfile, shared cache, vendoring bridge). Unblocks `cajeta-chart` §3.9/§3.10 fonts and `cajeta-ml-v3` §13.6 pretrained weights. ~~Depends on buildtool-dependency-classpath — `cajeta.json` dependencies are currently inert~~ **Corrected 2026-08-02: dependencies are NOT inert — they already resolve onto the compile classpath (that spec's U1 verdict), so this one is not blocked on it.** |
| [cajeta-chart](cajeta-chart-spec.md) | [c1](../agents/cajeta-chart-c1-plan.md) · [c2](../agents/cajeta-chart-c2-plan.md) · [c3](../agents/cajeta-chart-c3-plan.md) · [c4](../agents/cajeta-chart-c4-plan.md) | **active** — approved 2026-08-01; **phased into C1–C4** (§13.7): C1 core+SVG, C2 CPU raster+PNG, C3 frame binding + catalogue, C4 HTML5. Shaping moved out to `cajeta-text-shaping`. First-class charting/visualization: 5 layers (surface → D3-style primitives → matplotlib-style figure → plotly/seaborn-style declarative → interaction), 4 backends (SVG/PDF/PNG/HTML5), matplotlib+seaborn chart coverage, validated design system. **Largest spec in the set.** Critical path: §3 text/font metrics — nothing exists in cajeta and layout cannot resolve without it |
| [cajeta-rag](cajeta-rag-spec.md) | [plan](../agents/cajeta-rag-plan.md) | **active** — approved 2026-08-01; **external lib** `dev.cajeta.rag`: BM25 + inverted index, dense retrieval over `nucleo.frame`'s vector index, hybrid fusion (RRF), reranking, MMR, context assembly under a token budget, citation, retrieval evaluation. **Plus the structural half (§9–§11, added 2026-08-01):** a pluggable enhancement seam (filter/scorer/expander signals with per-signal score attribution); a **typed relation graph** — `imports`/`calls`/`links-to`/`cites` — with corpus-level reference resolution, PageRank ranking, and bounded neighbourhood expansion; and **code as a first-class corpus** — exact symbol index, syntactic chunking on definition boundaries, and default language processors for **8** languages including Cajeta itself (via the compiler front end). Requires new work in `cajeta-docs` (§4.7 code processors, §4.11 reference extraction, §10.2.1 code chunking) and `ml-graph-analytics` (§4.4.1 PageRank — added). **Does not generate** — produces ranked cited context; the caller decides what consumes it. **Sequenced LAST** (2026-08-01); indexes are **persistent from v1** (§12.3); language processors **hand-written, 8 languages** (§12.8) |
| [cajeta-ml-dist](cajeta-ml-dist-spec.md) | [plan](../agents/cajeta-ml-dist-plan.md) | **active** — approved 2026-08-01; **external lib**: distributed *execution strategy* for `dev.cajeta.ml`'s estimators over `cajeta-dqe` partitioned tables. Contract: **a distributed fit produces the model identical to the single-node fit**, invariant to partition count. Never a second implementation — the MLlib-vs-sklearn split Python never recovered from |
| [cajeta-cloud](cajeta-cloud-spec.md) | [plan](../agents/cajeta-cloud-plan.md) | **active** — approved 2026-08-01; **one platform-agnostic API for the services every cloud provides**. §1.2 enumerates the family with per-provider mappings: object store, queue, topic, stream, key-value, SQL, cache, secrets, notification, managed compute, compute, workflow — queue/topic/stream held **deliberately distinct** (delivery fan-out, replay, and ordering contracts differ). Features are **programmatically discoverable** (capability negotiation, never lowest-common-denominator); the interface library is **ports + in-memory drivers + conformance testkits only** — `capabilities: []`, no filesystem, no network, no SDK deps (§1.4); every real backend, provider *or* local, is a separate artifact. §1.5 records the criteria for a future `cajeta-cloud-local` (tier A achievable / B partial / C not) but **does not commit to one**. **Object store is the first vertical**, specified in full (§4–§8); stream port also specified (§12). Data plane only — provisioning/IAM stays out (§1.3). Adapters in separate artifacts (`-aws`/`-azure`/`-gcp`/`-local`). Replaces the `cajeta-cloud-objectstore` sample (`authors:["sample"]`, no adapters, no network capability). Unblocks `cajeta-dqe` §7 |
| [cajeta-docs](cajeta-docs-spec.md) | [plan](../agents/cajeta-docs-plan.md) | **active** — approved 2026-08-01; **external lib** `dev.cajeta.docs`, **the** text/document library. Structured document model (pages · sections · paragraphs · sentences · lines · tokens · **elements**: images/charts/tables/equations/footnotes), format readers, extraction quality, provenance at every granularity, normalization, tokenization + **sentence segmentation**, TF-IDF vectorization, chunking, subword tokenizers. **Supersedes and absorbs `cajeta-doc-ingest` and `cajeta-text`.** Prereqs: `codec` XML, `cajeta-font` (PDF), Unicode normalization on `String` |
| [cajeta-font](cajeta-font-spec.md) | [plan](../agents/cajeta-font-plan.md) | **active** — approved 2026-08-01; **external lib** `dev.cajeta.font`: sfnt container (TTF+OTF share it), `cmap`, metrics, **`GPOS` kerning**, `glyf` + CFF/Type2 outlines, PDF subsetting, reverse glyph→Unicode for text extraction, WOFF/WOFF2, TTC. Emits outlines + metrics only — **no rasterizer**. **Critical path for all charting** (`cajeta-chart` §1.7); supersedes that spec's §3. **§10.5 (2026-08-01): also gains the full text-shaping engine** — bidi, Arabic joining, Indic reordering, CJK, driven by GSUB/GPOS — specified separately as `cajeta-text-shaping` (**not yet written**), because three libraries consume it (chart §3, docs PDF, font's own rendering) |
| [cajeta-text-shaping](cajeta-text-shaping-spec.md) | [plan](../agents/cajeta-text-shaping-plan.md) | **active** — approved 2026-08-01; **new 2026-08-01**, split out of `cajeta-chart` §13.1. Unicode text → positioned glyphs, in `dev.cajeta.font`: itemization, **UAX #9 bidi**, GSUB/GPOS in full, GDEF mark filtering, and per-script shapers (default/Latin, Arabic joining, the Indic family with reordering, Khmer/Myanmar/Thai/Lao, Hangul, and the Universal Shaping Engine), vertical text, clusters, shaping-plan caching. **Oracle is HarfBuzz** — `hb-shape` emits a diffable glyph-buffer serialization, so fixtures are exact. Emits positions only; draws nothing. **Gates `cajeta-chart` C1.** Prereqs: `cajeta-font` sfnt+cmap, `stdlib-completion` §7 normalization + §8.4 UCD. Open: §13.5 **line breaking (UAX #14) is unowned** and charts need it |
| [nucleo-distributed-frame](nucleo-distributed-frame-spec.md) | [plan](../agents/nucleo-distributed-frame-plan.md) | **active** — approved 2026-08-01; partitioned tables, the **exchange/shuffle** operator, distribution-aware planning, distributed agg/join/sort, stage scheduling + fault tolerance, partitioned dataset storage; scoped against the HDFS/YARN/MapReduce/Hive/Spark stack. **Both halves already exist** — `nucleo.frame`'s lazy planner and `cajeta-cluster`/`cajeta-gossip`/`cajeta-cloud-objectstore` — this is the missing seam. Contract: distributed results bit-identical to single-node (§11.1). **§10 added 2026-08-01** on a second pass: Kafka/streaming split out as its own spec, RDD layer, distributed MLlib, dynamic allocation + graceful decommission, Hive external tables/nested types/views |
| [json-viewer](json-viewer-spec.md) | [plan](../agents/json-viewer-plan.md) | active — Units 1-4 delivered 2026-07-28 (doc model + lenient/mixed parsing; in-place console JSON view; diagnostic row navigation; .json/.jsonc structured editing), plugin installed to CLion2026.2; remaining: live passes 2.3.x/3.3.1/4.3.2 (Unit 6), Unit 5 binary JSON gated on Julian confirming the format |
| [silent-resolution-diagnostics](silent-resolution-diagnostics-spec.md) | [plan](../agents/silent-resolution-diagnostics-plan.md) | active |
| [docs-refactor](docs-refactor-spec.md) | [plan](../agents/docs-refactor-plan.md) | active |
| [cja-source-view](cja-source-view-spec.md) | [plan](../agents/cja-source-view-plan.md) | active |
| [embedded-targets](embedded-targets-spec.md) | [plan](../agents/embedded-targets-plan.md) | active |
| [memory-viewer](memory-viewer-spec.md) | [plan](../agents/memory-viewer-plan.md) | active |
| [diagnostic-engine](diagnostic-engine-spec.md) | — (engine + lint + full-compile collect-and-continue landed 2026-07-29; codegen-in-collect-mode is the open follow-up; remaining scope unverified) | active |
| [compile-cache](compile-cache-spec.md) | [plan](../agents/cajeta/compile-cache-plan.md) | blocked (parked after Unit 2, 2026-07-10). **D1 FIXED 2026-08-04**: the demand set was larger than the obligation set — a third obligation kind (`Owner::field`) records cross-module static-field demand and replays it; `TourSmokeBuildsIncrementally` re-enabled (the DoD), incremental suites 19/19. The re-open trigger is cleared; the perf units stay parked. **New defect datapoint 2026-08-24 (cajeta-profiler 6.7):** the IR cache key does not include the TOOLCHAIN's embedded runtime — after a runtime-only compiler change, `cajeta gpu` in samples/profile rebuilt to a byte-identical binary from `.cajeta/` cached objects (same sha256 through `rm -rf build`), silently reproducing with the OLD runtime until the project cache was purged. A toolchain-content (or version+runtime-digest) component belongs in the key. |
| [cajetadoc-model-fidelity](cajetadoc-model-fidelity-spec.md) | — (docs-refactor 15.1) | draft |
| [net-server-shutdown-wake](net-server-shutdown-wake-spec.md) | — (docs-refactor 15.6) | draft |
| [matrix-element-callarg](archive/matrix-element-callarg-spec.md) | — (docs-refactor 15.8) | **FIXED 2026-08-03.** A DOUBLE LOAD: array elements lower to a GEP (a pointer the caller loads), Matrix/Vector elements materialize the value, and the 2026-08-01 `f(arr[i])` fix applied `loadIfLValue` to both. Guard added — `loadIfLValue` returns non-pointers untouched — covering Matrix, plain arrays and `ArrayList.operator[]` at once, which is why one bug had three signatures. Tour workaround reverted; 491 self-checks. |
| [kernel-device-call-diagnostic](kernel-device-call-diagnostic-spec.md) | — (docs-refactor 15.9) | draft |
| [channel-ownership](channel-ownership-spec.md) | [plan](../agents/channel-ownership-plan.md) | draft |
| [view-element-arrays](view-element-arrays-spec.md) | plan: `agents/view-element-arrays-plan.md` | active (unblocks cajeta-gossip G1) |
| [profile](profile-spec.md) | — | draft |
| [cajeta-ir-phase-b](cajeta-ir-phase-b-spec.md) | — (§4/§5 resolved; §2 forwarding, §3 captures remain) | draft |
| [llm-kernel-scheduling](llm-kernel-scheduling-spec.md) | — | draft |
| [robotics-kernel-scheduling](robotics-kernel-scheduling-spec.md) | — | draft |
| [xpu-scan-primitive](xpu-scan-primitive-spec.md) | — | draft |
| [xpu-pipelined-gemm-primitives](xpu-pipelined-gemm-primitives-spec.md) | — | draft |
| [xpu-kernel-scheduling](xpu-kernel-scheduling-spec.md) | [plan](../agents/xpu-kernel-scheduling-plan.md) | **active** — approved 2026-08-20. Session-level **inter-kernel** GPU scheduler (distinct from `xpu-kernel-scheduling-hints`, which is instruction scheduling inside one `@Kernel`): co-runs complementary kernels so a compute-bound GEMM's idle bandwidth is not wasted. Offline classification (§3.2) reuses the shipped `DeviceProfile` roofline + occupancy budgets and has no unbuilt dependency; the online correction (§8.2) registers a live sink on `cajeta-profiler`'s §5.6 record seam, so it is gated on that spec's Unit 7 (§12.6) — **which landed 2026-08-21**: the record seam, the sink registry with per-sink delivery granularity, and the CPU-emulation backend are in, so §8.2's online correction is unblocked and can be built and tested without a GPU. Research: `research/xpu-scheduling/papers/` — 19 papers. |
| [xpu-kernel-scheduling-hints](xpu-kernel-scheduling-hints-spec.md) | — (U1 landed on origin: cajeta.xpu.Schedule surface + no-op seam; un-archived 2026-07-03) | active |
| [xpu-gfx-streaming-geometry](xpu-gfx-streaming-geometry-spec.md) | — | draft |
| [xpu-build-ergonomics](xpu-build-ergonomics-spec.md) | — | draft |
| [simd-numeric-kernels](simd-numeric-kernels-spec.md) | — | draft |
| [quaternion-vector-stdlib](quaternion-vector-stdlib-spec.md) | [plan](../agents/quaternion-vector-stdlib-plan.md) | draft |
| [ternary-int-codegen](ternary-int-codegen-spec.md) | — | draft |
| [placeholder-owned-field](placeholder-owned-field-spec.md) | — (defect; FIXED 2026-07-31 — cross-module vtable constant; pins live; ml co-location workaround stays until the fix releases in v0.13.0) | draft |
| [null-owned-interface-arg](null-owned-interface-arg-spec.md) | — (defect; FIXED 2026-07-31, pin live; noted follow-up: fat-aware interface `== null`) | draft |
| [cross-cja-exception-catch](cross-cja-exception-catch-spec.md) | — (defect, tour-quality unit review) | draft |
| [buildtool-dependency-classpath](buildtool-dependency-classpath-spec.md) | [plan](../agents/buildtool-dependency-classpath-plan.md) | **active** — plan written 2026-08-01; **U1 verdict 2026-08-02: the reported defect is STALE.** Manifest dependencies already resolve, cache, and thread onto the compile classpath with no `--classpath` (verified end to end against released v0.14.0), and an unresolvable dep fails naming package, constraint, and repositories tried. U2 as written describes no real work. What actually blocks `cajeta-unit`'s tour is (a) a stale `dev.cajeta.unit` 0.1.0 in `~/.olla` that the tour's `"0.1.*"` pin resolves onto, built from retired syntax, and (b) **buildtool-exe-package-name-collision**. Remaining here: U1.2.1's regression suite, then rescope U2/U4 |
| [buildtool-exe-package-name-collision](buildtool-exe-package-name-collision-spec.md) | — (plan not yet written) | draft — **defect, found 2026-08-02.** `--emit=exe` writes to `build/exe/<details.name>` while the project's own objects go to a package-mirroring tree under the same directory, so a `details.name` equal to a top-level package name makes the linker try to write a file over a directory (`cannot open output file build/exe/app: Is a directory`). A dependency's objects are flat dotted names; only the project's own are a tree — that asymmetry looks unintentional and removing it may fix the defect outright. Blocks `buildtool-dependency-classpath` §3.1 |
| [json-tobytes-string](json-tobytes-string-spec.md) | — (defect, tour-quality stdlib review) | — (defect) **CONFIRMED 2026-08-01 on 0.14.0:** String-field round trip still SIGSEGVs (fault addr nil) after `toBytes` returns 60 bytes. |
| [float64-tostring-roundtrip](float64-tostring-roundtrip-spec.md) | — (defect, tour-quality logging review) | — (defect) **PARTIAL 2026-08-01:** the pinned `0.987` case now renders correctly, but rendering is still not shortest-round-trip — `1.0/3.0` prints `0.333333`, which parses back to different bits. Spec 2.1 unmet. |
| [classpath-diag-duplication](classpath-diag-duplication-spec.md) | — (defect, cosmetic) | — (defect, cosmetic) **CONFIRMED 2026-08-01 on 0.14.0:** still doubled, second line has an empty subject. |
| [local-shadowing-field-member-lookup](local-shadowing-field-member-lookup-spec.md) | — (defect, plan not yet written) | draft — **defect, found 2026-08-14 in cajeta-llama U13.** A local shadowing a same-named field of a DIFFERENT type has member lookup validated against the FIELD's type, rejecting valid programs (`no member 'add' on TplBuf` for a line operating on a local `JsonArray`). Codegen is correct — 5 repros show the local always wins, incl. when both types carry the member, so there is no wrong-object dispatch; the bug is a false-positive diagnostic. Nesting is not a factor. Java shadows per JLS 6.4.1 |
| [jsonwriter-control-byte-escaping](jsonwriter-control-byte-escaping-spec.md) | — (defect, filed per cajeta-llama 13.7/13.8; plan not yet written) | draft — **defect, found 2026-08-07 planning cajeta-llama U12.** `JsonWriter.writeStringRaw` escapes only `"` and `\`; any byte 0x00–0x1F reaches the output raw, producing invalid JSON (RFC 8259 §7). Write-side only; the codec-wide verbatim-escape READ design is deliberate and untouched |
| [nested-class-type-resolution](archive/collections-overhaul-spec.md) | — (defects, found 2026-08-03 building the build-tool plugins) | — three signatures, one family: (a) an unqualified NESTED class name inside its outer class binds to a same-named nested class of ANOTHER outer (CoverageMap.FileEntry ctor resolved against FilteredCounts.FileEntry — shortname-rebind's nested variant); (b) `Optional<Outer.Nested>` resolves as raw Optional (no member isPresent); (c) chained `.get(k).get()` off a map whose V is a nested class resolves the second call against V. Plugins sidestep via renames/top-level classes/two-step locals |
| [static-string-array-initializer](archive/collections-overhaul-spec.md) | — (defect, found 2026-08-03) | — `static final String[] X = ["a", "b"]` fails codegen with INITIALIZER_TYPE_MISMATCH claiming String[] is not assignable to String[]; minimal single-file lint does NOT reproduce (needs the fuller class context). Sidestepped in ExcludeParser |
| [classpath-signature-shortname-rebind](archive/classpath-signature-shortname-rebind-spec.md) | — (defect) | **FIXED, closed 2026-08-04.** Not the resolver tiers — placeholder-fill short-name capture: an earlier import of `pkgA.Name` synthesizes a placeholder registered under both `pkgA.Name` AND bare `Name`; a same-shortname class from ANOTHER package then declared missed its own canonical, hit the bare key, and `fillFromDeclaration` mutated the shared shared_ptr — so `canonicalMap["pkgA.Name"]` BECAME the decoy and same-package signatures bound wrong. All three fill sites (class/view/interface) now accept a short-key hit only when the placeholder's canonical equals the declaring one. Deliberately-ordered two-archive repro pinned both orders: `ClasspathShortnameRebindTests`. |
| [owned-interface-return-fault](archive/owned-interface-return-fault-spec.md) | — (defect) | **FIXED 2026-08-03.** Scope was far wider than reported: EVERY `#Interface` return of a concrete class was miscompiled, container or not (15-line repro). ReturnStatement had no concrete->fat-body wrap, so the return fell into the `@ValueType` "load the aggregate from that address" arm and read 24 bytes out of a 16-byte object. `#<BaseClass>` control passes, answering spec 4.3: fat-value handling, not return-slot lifetime. Fat-aware `iface == null` (the null-owned-interface-arg follow-up) fixed alongside. |
| [typeparam-cast-of-paren](archive/typeparam-cast-of-paren-spec.md) | — (defect) | FIXED 2026-08-02 (5a3ba7fb): `(Name)(expr)` now lowers as a cast. Reinterpreted at Expression::fromContext, not in the grammar, so cast precedence is untouched — `(T) a.b()` never parses as a postfix call and never reaches the new arm. Shape checked before resolution, so kernel.launch(d)(a) is excluded and a non-type name still falls through to a call. Documented split: `(D)(x).f()` binds cast-first for a type name, whole-chain for a primitive. |
| [record-cross-type-return](record-cross-type-return-spec.md) | — (defect, found 2026-08-04 in stdlib-completion U6) | **FIXED 2026-08-05.** Root cause: the lazily-parsed stdlib prototypes methods while cross-file record types are still PLACEHOLDERS — the ABI (by-value aggregate vs ptr; sret) is decided against the placeholder and never revisited, and a filled record's `getLlvmType()` could return null into a FunctionType (the nil-fault). Fix: (a) a per-thread TYPE-FILL EPOCH bumped by `fillFromDeclaration`; `Method::ensureFreshPrototype` re-derives stale signatures (frozen methods exempt — primed fully-filled, and re-prototyping the shared frozen AST corrupts sessions); (b) declaration-only functions with changed types are replaced + RAUW'd; (c) on-demand opaque-then-body record struct build in `getLlvmType` (field-list-gated); (d) `ParameterField` names ABI mismatches instead of faulting; (e) the Math-intrinsic `loadArg` now uses the shared l-value coercion (field-GEP args). U6 unparked: OklabTests 5/5 |
| [owned-return-of-borrowed-this](owned-return-of-borrowed-this-spec.md) | — (defect, found 2026-08-05 in stdlib-completion U7) | **FIXED 2026-08-05** — rejection (spec §4's v1). `ReturnStatement::generateCode` rejects `return this` (and `return #this` via the MoveExpression unwrap) under a `#` return with `CAJETA_ERROR_OWNED_RETURN_OF_BORROWED_THIS`, beside the STACK_RETURN_ESCAPES check; the diagnostic names the method and both fixes (borrow window via `Cajeta.stringSliceBorrow`, or drop the `#`). Rejection over implicit retain: dynamic ride-out would tax every class-typed call result with a TLS read + conditional drop and demote three compile-time diagnostics to runtime. Pins: OwnedReturnOfBorrowedThisTests (4) — direct + fast-path shapes rejected, fluent-builder + fresh-`#heap` controls green; 48/48 acceptance sweep (SignatureAbiTests + UcdStringTests two-session chain). Stdlib scan: no `#`-returning method ships the shape; borrow-window workarounds stay (§5.3) |
| [instanceof-interface-lhs](instanceof-interface-lhs-spec.md) | — (defect, found 2026-08-04 in stdlib-completion U1) | **FIXED 2026-08-04.** Two halves: (a) an interface-typed lhs is a fat body `{data, vtable, kind}` — instanceof now strips to the data pointer (null-guarded branch+phi; same recipe as CastExpression's iface→class arm) before the RTTI call and the pattern binding; (b) RTTI `parentNames` now carries IMPLEMENTED INTERFACES beside superclasses, so interface targets answer truthfully from any lhs. Pins: InstanceofInterfaceTests (5) — iface→class both ways, iface→iface, class→iface via Object, guarded-form binding, null lhs. 163-test RTTI/capture/reflection sweep green. The `cajeta.math.distance` overload workaround stays (it's also a fine API) |
| [linkedlist-class-pop](archive/linkedlist-class-pop-spec.md) | — (defect) | **FIXED, closed 2026-08-04.** Fix shape (B) landed in two halves before v0.15.0: `39283f70` (String fields carry an ownership bit — exclusion removed from `fieldHasOwnershipBit`) + `c322461a` (the String field-store path now sets the bit; layout math shared via `locateFieldOwnershipBit`). Verified on `e57517e8`: LinkedListClassPop + SharedFieldDrop 22/22 under `MALLOC_PERTURB_=101`; tour LinkedListDemo un-gated to `LinkedList<String>`. The §1b double-sharp diagnostic gap remains a separate filing. |
| [allclasses-keep-all](archive/allclasses-keep-all-spec.md) | — (defect, focus-stack carry since 2026-07-31) | **FIXED, closed 2026-08-04.** Bounded enumerator `Class.classesWithMethodAnnotated` (+ `<@A>()` token) with `ReflSite::MethodAnnotated` keep-set resolution; `.cja` archives now carry `meta/reflection-keep.v1` (code-driven sites only) merged at classpath ingest — a dep-internal `allClasses()` previously never reached the consumer's classifier at all, so a lean link could silently strip what the dep enumerates. cajeta-unit Runner switched to the bounded form (committed locally, push rides the next cajeta release + CI pin bump). Pins: ReflectionTests + ReflectionKeepMethodAnnotatedTests. |
| [runtime-lost-wakeup-under-load](runtime-lost-wakeup-under-load-spec.md) | — (defect) | FIXED 2026-07-31 (18a78057 + 7d8cfd8c): closing a descriptor neither woke nor serialized against the fibers parked on it in the reactor — both interleavings fixed; http tour 0/6 -> 40/40. Open only for acceptance 2.2's >=100-run loop |
| [field-store-title-trap](archive/field-store-title-trap-spec.md) | — (NOT a defect) | CLOSED 2026-08-02: the compiler is correct. `heap X(...)` at a call site surrenders (the formal owns); `=` is a borrow and never inherits that contract, so the formal drops at return and the field dangles — the program asked to borrow from a value whose owner dies at the end of the call. Fix is `#=` at the STORE, formal stays plain (`#T` is a stronger, API-visible choice, not the remedy — the shipped language-ownership skill said otherwise and was corrected). Semantic proposal withdrawn. |
| [script-units](script-units-spec.md) | [plan](../agents/script-units-plan.md) | **active** — approved 2026-08-07 (top-level code as a language feature: implicit class, session-scoped bindings, generational redefinition; foundation for jupyter-kernel and `cajeta run <script>`); plan Units 1-6 unstarted |
| [jupyter-kernel](jupyter-kernel-spec.md) | [plan](../agents/jupyter-kernel-plan.md) | **active** — approved 2026-08-07 (`cajeta kernel` over Jupyter v5.3/ZMQ consuming script units); supersedes `agents/archive/jupyter-capability-plan.md`. **Units 1–6 COMPLETE 2026-08-14, running in Jupyter Lab**: persistent JIT session with cross-cell bindings, `Out[N]` rendering, `In[N]` tracebacks, five-channel ZMQ + HMAC, `cajeta kernel` verb, safepoint interrupt. **Unit 7 (startup latency) part-landed 2026-08-15/16**: two-stage SLL/LL parsing took the stdlib prime 36.4s → 4.9s and the first cell 54s → 19s; 7.2.5 closed (a session with any archive on its classpath failed codegen — a duplicate module in the delivery set, not the design call it was filed as), plus 7.1.1, 7.2.4, 7.2.6 (kernel adopts the project it launches in) and 7.2.7 (struct-name leak between sessions). **Remaining: 7.2.8** first-cell progress on IOPub (the wait is silent), **7.2.9** `[~]` compile-cache levers 2–5, **7.2.10** the classpath ingest re-derives dependency code the archive already ships compiled (+201s per dependency on the first cell), **7.2.11** a `notebook` project archetype, and **Unit 8** (Olla install from the prompt — design unit, needs a session with Julian; spec §1.4 lists "no magics" as a v1 non-goal, so it revisits that or picks a non-magic surface). |
| [cajeta-llama](cajeta-llama-spec.md) | [plan](../agents/cajeta-llama-plan.md) | **active — approved 2026-08-08** (decoder-only LLM inference engine; llama.cpp architecture rewritten against `cajeta.math.Tensor` + `cajeta.xpu`, not bound — `@Native` marshals no structs/callbacks). Spine: generalize the `Ewise` placement dispatch across the op surface and unpin f32, then a tape-free forward reusing only `Module`'s `state_dict` naming, then KV cache + incremental decode. **All 8 open questions resolved 2026-08-08** (§13 is now a decisions section); two reversed their filed recommendation — mixed host/device operands *reject* rather than migrate (`torch-facade-spec` §3.1.2-3.1.3 already made device type-level), and verbatim JSON escapes are a deliberate zero-copy design so §7.6 is additive, not a fix. Spawns three prerequisite defect specs (13.8): `writeallbytes-field-arg` (authored + fixed 2026-08-12, row below), `runtime-path-nul-termination` (authored 2026-08-12, closed as already-fixed-by-cstr, row below), `jsonwriter-control-byte-escaping` — not yet authored, due with the tokenizer/JSON work. **Quantization in v1** (§10): Q8_0, Q4_0 and the k-quant family Q2_K–Q6_K, per-tensor dispatch — `Q4_K_M`/`Q2_K_S` are `llama-quantize` recipes, not formats, so a real Q4_K_M file mixes Q6_K/Q5_K with Q4_K and the whole family is required to load one. No language or LLVM change needed (sub-byte is storage, not arithmetic). `IQ*` i-quants out — codebook, not bit-field. **Targets** Llama 3.x, Mistral, Qwen2/3, Gemma 3 (text). Attention config is **per layer** (window, RoPE base, n_kv_heads) — Gemma 3's 5:1 local/global with 10k/1M RoPE split makes a model-level `sliding_window` field unbuildable; per-layer ring-buffer KV cache is in v1 (Mistral v0.1 saves 8× at its declared 32k context). Gemma 3 also needs QK-norm (not Gemma 2 soft-capping, which it removed), 4 RMSNorms/block, `(1+weight)` zero-centered norm, GeGLU. **Second round: all 10 §14 questions closed 2026-08-08** (§13 now runs to 13.22), and four answers changed scope rather than filling a blank. **Batched from the start with a paged block cache** (13.13) — contiguous per-sequence slots rejected on worst-case allocation; windowed layers keep their ring; attention now takes ragged batches with cumulative-sequence-length indexing. **Continuous batching with preemption** (13.15) and **always-chunked prefill** where decode is the same path at N=1 (13.14). **Evicted blocks go to a file-backed, prefix-keyed store, not recompute** (13.16): KV is 128 KB/token against ~16 GFLOP to regenerate, so a 4096-token conversation is ~156 ms to reload vs ~8.2 s to recompute, with no crossover — and prefix-keying makes it a restart-surviving prompt cache. Built on **`LtmPager` + `LtmBPlusTree`** (13.17), which needs three stdlib gaps closed: no delete (tombstones, not page merge), no ordered scan, allocating `Encoder`. The index is *not* hot-path — admission/eviction/resume only. **Chat templates interpreted, not tabulated** (13.18): a Jinja subset over `JsonValue` with an ANTLR4 grammar, on the evidence that `llama.cpp` shipped a template table and replaced it with `--jinja`. SIMD `tableLookup`/`widen`/`narrow` pulled in from `Simd-plan.md` (13.19). **Gates now numeric**: decode ≥60% / prefill ≥50% of `llama.cpp`, memory ≤1.25×, on Llama-3.1-8B-Instruct Q4_K_M at gfx1151/ROCm 7.2.2 with the baseline measured before Unit 11 (13.20); parity metric pinned but threshold derived in Unit 11 (13.21); 99% greedy agreement with tie-only divergence (13.22). §1.5 reversed — paged cache and batching were non-goals and are now in v1. Plan built out to **19 units / 341 checkboxes** (284 leaf items), dependency-ordered: paged cache (8) → scheduler (9) → stdlib LTM gaps (10) → block store (11), with the scheduler landing first against recompute-on-resume since spec 6.32 makes the store optional; SIMD (17) precedes quantized execution (18); Unit 15 measures the `llama.cpp` baseline before asserting any fraction against it, and writes 13.21's derived thresholds back into the spec. Units 1–4, 10 and 17 are stdlib work in the `cajeta` repo, the rest build `dev.cajeta.llama`. Ready to implement |
| [nvptx-coop-bf16-fragment-abort](nvptx-coop-bf16-fragment-abort-spec.md) | — (defect, found 2026-08-10 in cajeta-llama Unit 2) | — (defect) **FIX IN PR #3:** a bf16-operand/f32-accumulator coop-matrix kernel (the standard WMMA recipe, all-Native on NVPTX) aborted the whole JIT-compiling process — `cast<FixedVectorType>` assert + SIGABRT — at *compile* time, launched or not, ptxas present or not. Root cause: three sites in `NvptxKernelLowering.cpp` assumed the f16 fragment shape `{<2 x half> x 8}`, but LLVM's bf16 WMMA fragments are `{i32 x 4}` (two bf16 per `.b32` register). *Filed first as `spirv-coop-mixed-tier-abort`* — the repro disproved that: SPIR-V skips mixed-tier kernels gracefully in both directions (now pinned by `XpuSpirvMixedTierTests`). Exit 3 records zero failed tests (same dishonest-reporting shape as windows-jit-coff-reloc). **MERGED to main 2026-08-11** (PR #3 merged; also validated on WSL/NVIDIA run 31452203287: matmulF16 lowered native through real ptxas — absent from the skip tally — with zero failures). |
| [windows-jit-coff-reloc](windows-jit-coff-reloc-spec.md) | — (defect, found by device-tests CI) | — (defect) **FIXED 2026-08-11** (`dcee8620` on main): LLJIT's auto-config deliberately avoids JITLink on COFF and falls back to RuntimeDyld, whose COFF x86-64 handler `report_fatal_error()`s on `IMAGE_REL_AMD64_ADDR32NB` without an ordered section layout — and the abort point wandered with the host module (three runs died at three different tests). Fix installs JITLink's `ObjectLinkingLayer` on COFF hosts, mirroring LLJIT's own JITLink setup + RTDyld-COFF responsibility flags. **Validated run 31452203287:** six hours of continuous Windows JIT, zero relocation aborts, 40 tests green (prior best: 28-then-abort); Linux/ELF untouched. **REOPENED 2026-08-11 — the abort survived both fixes.** Fix v1 covered only `CajetaJitHost`'s builder; the abort resurfaced through `KernelSession.cpp`'s bare `LLJITBuilder().create()` 48 tests later. Fix v2 (`ff060ccc`) routes both builder sites through the shared `JitCoffLinking.h` helper — and run 31551003125 (headSha verified to include both) still aborted at test #37. Static analysis exhausted: exactly two LLJITBuilder sites exist, no RTDyld/MCJIT/EngineBuilder users. A once-per-process stderr breadcrumb (`01600252`) now prints when the JITLink layer installs; its presence/absence in the next aborting run log discriminates mystery-RuntimeDyld-user vs stale-runner-build. Next step: a native Windows session — build main under MSYS2/MinGW64, run `XpuVulkanDispatchDeviceTests` under `gdb --batch -ex run -ex bt`; the backtrace names the RuntimeDyld instantiator. |
| [windows-vulkan-cpu-forced-hang](archive/windows-vulkan-cpu-forced-hang-spec.md) | — (defect, un-masked 2026-08-11 by the COFF fix) | — (defect) **FIXED 2026-08-11** (`3d595dd1` on main): the CPU kernel pool's mutex/condvars were plain zero-init statics — valid on glibc where `PTHREAD_*_INITIALIZER` is all-zero, INVALID on winpthreads (-1 sentinels) — so the first CPU-rung kernel launch on Windows parked forever in `pthread_cond_wait` on invalid primitives (return codes unchecked). The hanging test was simply the first CPU-rung launch in Windows history. Attribution settled by two bounded runner experiments (hangs identically pre- and post-JITLink → pre-existing; COFF fix exonerated). Fix: designated initializers on the pool struct, bit-identical on glibc. **Validated run 31536611067**: Windows passes the test; Linux controls green. **CLOSED 2026-08-20 — acceptance 2.2 met** by run 32404307651: first full Windows device sweep since the fix, **118 on hardware / 0 failures**, the formerly-hanging test passing in 65.6 s and the sweep finishing in ~105 min instead of hitting the ceiling (WSL: 43.5 s, run 32412895281). It had been unreachable Aug 18-20 for an unrelated reason — `41df9be7`'s active-msys2-prefix switch un-masked a missing `vim`/`xxd` in device-tests.yml, hard-failing configure; fixed in `81aad0e8`. |
| [writeallbytes-field-arg](writeallbytes-field-arg-spec.md) | — (defect, split from cajeta-llama 13.8) | — (defect) **FIXED 2026-08-12** (with cajeta-llama Units 3–4): `File.writeAllBytes(path, h.data, n)` with a struct-field (or array-element) data argument wrote adjacent struct memory — the File lowerings' array-argument helper unwrapped only `AllocaInst`, so a GEP-shaped l-value passed its slot address. Same defect class the String path fixed at `MethodCallExpression.cpp:386`; fix routes every File array argument through a shared `loadArrayArg` (loadIfLValue). Pinned by `FileIo64Tests.writeAllBytesFromStructField`. |
| [json-grow-element-uaf](json-grow-element-uaf-spec.md) | — (defect, found 2026-08-13 in cajeta-llama Unit 5) | — (defect) **FIXED 2026-08-13**: four grow loops (`JsonObject`, `JsonArray`, `Headers`, `QueryParams`) borrow-copied owned elements with plain `=` then displaced the old backing array with `#=` — whose element-drop walk freed everything the new array pointed at. Use-after-free past 8 entries; latent because no pre-llama workload exceeded 8 keys with reuse pressure. Missed by the title-stores 3.2.5 audit that respelled ArrayList/HashMap/Heap. Fix: forwarding moves (`dst[i] #= src[i]`) at all four sites; pinned by `JsonObjectHashTests` (growth to 20–40 entries). |
| [runtime-path-nul-termination](runtime-path-nul-termination-spec.md) | — (defect investigation, split from cajeta-llama 13.8) | — (defect) **CLOSED 2026-08-12 as already-fixed-by-cstr**: the filed concern (a mode-2 windowed substring path reaching `open(2)` without a NUL at its window end) cannot reproduce at the File seam — every String-taking File intrinsic routes through `loadStringArg` → `__cajeta_string_cstr`, which materializes windowed/inline forms into a NUL-terminated per-thread scratch. Regression-pinned by `FileIo64Tests.runtimeConstructedPathOpens` (concat + substring-with-trailing-garbage forms). The spec stays as the checklist for any future path-taking seam: route through cstr, never a bare `bytes + 8`. |
| [mingw-64bit-file-fixtures](mingw-64bit-file-fixtures-spec.md) | — (defect, found by the 2026-08-13 device-tests nightly) | — (defect) **FIXED 2026-08-13**: `FileIo64Tests`' sparse-fixture helper used `::pwrite` (absent on MinGW — the build error that took the whole `windows-nvidia` leg down before a single test ran) and `::ftruncate` with MinGW's 32-bit `off_t`, which would have wrapped the 2^31+16 length and silently unmade the bit-31 case the tests exist to pin. Helpers are now `markSparse` / `truncateTo` / `writeAt`, `#ifdef _WIN32` only; `FSCTL_SET_SPARSE` keeps the Windows fixture metadata-only as the file header claims. Verified with MSYS2 MINGW64 g++ on PHOENIX: `size=2147483664 onDisk=67895296`, tail marker read back. |
| [device-nightly-reboot-window](archive/device-nightly-reboot-window-spec.md) | — (defect, infrastructure, found by the 2026-08-13 device-tests nightly) | — (defect) **FIXED, closed 2026-08-20.** Two causes, one leg. (a) The 2026-08-13 death was Windows Update restarting PHOENIX at 04:53 local mid-test; the cron moved to `0 1 * * *` UTC (21:00 local) with the constraint documented in the workflow. (b) **The real one:** every attempt Aug 16-20 failed at exactly 600 s without completing `Set up job` — GitHub's grace period for a job assigned to a runner that never reports. `Start-WSL-Runner` fired only on interactive logon, so after any host restart the VM stayed down while GitHub kept assigning it work; the jobs were never running at all. PHOENIX now starts WSL at machine start (Acceptance 2.3, host config). Verified green 2026-08-20: standalone (32412895281, 65/0) and under the nightly filter with both legs concurrent (32416638186). Noted, not a defect: WSL executes 65 tests vs Windows' 118 on the same 4090 (§3.4). |
| [jitlink-coff-comdat-section-symbol](jitlink-coff-comdat-section-symbol-spec.md) | — (defect, LLVM upstream, found by device-tests CI 2026-08-14) | — (defect) **FIXED 2026-08-14** (`cajeta-llvm@86879380a`, shipped in `cajeta-llvm-23-r7`; verified on PHOENIX — the error is gone). Was: All 99 Windows device failures in run 31762622334 are one bug, and it is LLVM's: `COFFLinkGraphBuilder` assumes every COMDAT section has two symbols (section symbol + external leader) and back-fills the section symbol's graph entry only on the second. `.xdata$name` unwind COMDATs have no leader, so `GraphSymbols[13]` stays null and the `.pdata$name` ADDR32NB relocation naming it dies. Proven not ours: `lld-link` 23.0.0 links the identical object (RC=0). Not fixed upstream (builder is byte-identical to llvm main) and never reported. Fix shape in spec §2; reproduces on `BinaryOpTests.int128WidenNarrow`. |
| [jitlink-coff-imp-iat](jitlink-coff-imp-iat-spec.md) | — (defect, LLVM upstream, found by device-tests CI 2026-08-14) | — (defect) **FIXED 2026-08-14** by taking upstream `54a7896ac` ("[JITLink][COFF] Synthesize `__imp_` IAT entries", #203906) into the fork as `cajeta-llvm-23-r8`. Surfaced the moment r7 cleared the COMDAT crash: nothing defined the `__imp_X` slots a dllimport reference goes through, so every Windows API the runtime touches came back unresolved. The fork branched 2026-06-01, eighteen days before the upstream fix. Verified on PHOENIX: zero `__imp_` occurrences under r8. `eee7c2d22` (AutoImportGenerator) was cherry-picked and reverted — later ORC API, and bound to a single DLL so it cannot help host-executable symbols. |
| [jitlink-coff-external-call-range](jitlink-coff-external-call-range-spec.md) | — (defect, LLVM, found by device-tests CI 2026-08-14) | — (defect) **FIXED 2026-08-14, verified on PHOENIX 2026-08-15** (`cajeta-llvm-23-r9`) — `BinaryOpTests` green inside a 446/446 release subset. Third layer: a COFF REL32 reaches ±2 GB, but the JIT slab sits at `0x27d…` while loaded modules sit near `0x7ff8…`, so every call into the host CRT failed — 24 of 24 `BinaryOpTests` on `strcmp`/`__divti3`/`fmod`. JITLink's COFF backend had no stub mechanism; RuntimeDyld's always has. Fix adds `buildTables_COFF_x86_64` (the same `x86_64` PLT/GOT managers ELF uses) + `optimizeGOTAndStubAccesses`. Only CALL/JMP/Jcc sites are promoted — an out-of-range *data* reference must still error (`COFF_external_var.s`). Lit: 106 passed, 0 failed. |
| [jitlink-coff-seh-drop-uaf](jitlink-coff-seh-drop-uaf-spec.md) | — (defect, ours, found by the release subset on PHOENIX 2026-08-15) | — (defect) **FIXED 2026-08-15, verified on PHOENIX** — 13/13 crashers pass, and the release subset went 421/0/11/14 -> 446/0/0/0. Fourth and last layer, and this one is ours, not LLVM's: `DropSehFramesPlugin` removed `.pdata*` with a bare `LinkGraph::removeSection()`, which destroys the section's Symbols/Blocks without scrubbing edges INTO it. COFF models an associative COMDAT as an `Edge::KeepAlive` from `.text$fn` into the `.pdata$fn` symbol, so `jitlink::prune()` — which runs immediately after — reads *and writes* through hundreds of dangling `Symbol*`s. Usually benign over intact freed memory, which is why 421 of 434 release-subset tests passed and 13 died with a bare SIGSEGV (12 `HashMapStreamParallelTests` + `CastTests.roundToIntModes`; all pass under RuntimeDyld). Fix scrubs inbound edges first. Unit-tested on Linux via a hand-built `LinkGraph` (`DropSehFramesTests`, red before / green after). |
| [cajeta-profiler](cajeta-profiler-spec.md) | [plan](../agents/cajeta-profiler-plan.md) | **active** — authored 2026-08-17, approved 2026-08-20. Whole-run CPU sampling over the existing line-info shadow stack (no rebuild needed), exact instrumentation as an opt-in build mode, GPU device kernel timing, Perfetto `.pftrace` output, IntelliJ tool window. 13 units, AMD+CPU first then NVIDIA then Vulkan. Grounded in a six-track research pass with direct measurement on gfx1151; report at the artifact linked from the spec. Supersedes the `agents/cajeta/profiler/profiler-plan.md` scaffold (D1–D5 resolved in spec §14). Unit 2 fixes an existing fiber shadow-stack defect that stands on its own. **Unit 7's dispatch-record seam is dual-consumer (§5.6, decided 2026-08-20):** records publish to sinks, the Perfetto writer is one, and a live in-process consumer registers another without touching a backend — the second known consumer is `xpu-kernel-scheduling` §8's feedback loop, which needs per-launch achieved latency that `xpu-device-profile`'s counter tier does not emit. v1 ships the seam and the writer sink only. **Unit 7 landed 2026-08-21** — seam, sinks, CPU-emulation lane, device/context/queue tracks and launch→execution flows, with CI running `trace_processor` over the result. **2026-08-24: Units 6.7 + 11 essentially closed.** 6.7's reproduction on gfx1151 showed neither `rocm_records` nor the tier lying (the 6.6.3 contradiction was pre-6.6.2.d ordering); the real defect was §9.1.a flagging OUTSIDE_HOST on 144/144 honest async spans — fixed with a causal `[submit, resolved_ns]` bracket, tier enum renumbered (`UNKNOWN=0, DEVICE=1`) so the memset default is the weakest claim. Unit 11's viewer verified: the deferred test run caught three real viewer defects (all fixed), 11.1.d closed against a NEW vendored real fiber+GPU gfx1151 trace (`amdgpu.pftrace`), 11.3.b verified in the actual ui.perfetto.dev. Kotlin 119/119; C++ Profiler* 148/149 (1 by-design skip). **Remaining: 11.3.a** (live IDE pass — Julian) and Units 1 (1.3.d, unprivileged CUPTI run or spec §5.4.4 revision), 12 (gated on 1.3.d), 13 (Vulkan; likely workable on proton). |
| [memory-tooling](memory-tooling-spec.md) | [plan](../agents/memory-tooling-plan.md) | **draft** — authored 2026-08-17, awaiting approval. **Supersedes the never-approved `valgrind-interop` draft**, which made Valgrind primary and therefore fragmented: Valgrind has no Windows port and no Apple Silicon port. **AddressSanitizer is the single mechanism covering every target** (Linux/Windows/macOS incl. arm64), is ~2× vs Valgrind's 20–50×, and catches stack/global overflows and use-after-return that Memcheck cannot. Two phases. **Phase 1** (Units 1–7): implement the declared-but-empty `asan`/`ubsan` flavor keys, fiber annotation on **both** backends (ucontext + Win32 Fibers), arena poisoning, `--dwarf` flag, poison-pattern auto-init for the uninit-read gap, CI on all three platforms; Valgrind survives only as an optional Linux supplement (Unit 7) for uninitialized reads. **Phase 2** (Units 8–10): IDE report view + live findings-during-run, reading tool output directly (plugin targets `platformType = IC`; sanitizer/Valgrind integration is CLion-only). Sibling to `cajeta-profiler`; neither gates the other. Unit 3's arena annotation is also the hook site that reopens memory profiling in that spec's §14.5. |
| [vulkan-dispatch-serialization](cajeta-profiler-spec.md) | — (perf issue, filed per cajeta-profiler 13.3.c / spec §5.5.8) | **draft — filed 2026-08-24.** The Vulkan backend serializes every dispatch: one command buffer per launch, submitted and then `vkQueueWaitIdle`d under the submit mutex, so the host blocks for the full kernel duration on every launch and two streams can never overlap. The profiler now makes the cost visible instead of leaving a gap — an explicit "host blocked on GPU" span per submit (§14.13) — which is what turns this from a suspicion into a measurable filing. Fix direction: fences or timeline semaphores instead of WaitIdle, per-launch command buffers recycled through a pool, and only then real stream semantics. Not profiler work; the profiler is how you will see it pay off. |
| [moe-expert-cache](moe-expert-cache-spec.md) | _(not yet authored)_ | **draft** — opened 2026-08-21. Expert-granular residency over a mapped, quantized MoE checkpoint: top-k routing touches a small fraction of the model per token, so keep experts PACKED and manage admission/eviction/prefetch per expert. Baseline is LRU; the proposal is a continuously-learning policy on **SPELA(O)** (forward-only local per-layer losses — an update is a forward pass, so it fits an inference hot path, and it adapts to routing drift), which is ALREADY implemented in cajeta as `dev.cajeta.ml.train.SpelaTrainer` with the serving surface this needs (`observe`/`flush`/`freezeBackbone`/`confidenceOf`), so the policy runs in-process with nothing to port. **Weights are per-LLM**, keyed by checkpoint identity, never shared across models. The cache is latency-and-memory only: §7 requires identical output regardless of cache state, budget or policy, so a mispredicted prefetch costs time and never correctness. Acceptance is empirical — beat LRU on hit rate at equal budget on a held-out trace AND not lose the win to update cost, or it does not ship. Open: what model identity means, whether eviction is meaningful under mmap, and whether a per-model offline prior captures most of the win without hot-path cost. |
