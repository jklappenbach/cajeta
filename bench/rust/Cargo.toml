[package]
name = "rustjson-bench"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
simd-json = "0.13"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
