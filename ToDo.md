# ToDo

Working tracker for the next chunks of compiler work. Replaces the per-rollout status files now that the struct/view + memory-model rollouts are both complete (archived in `cajeta-docs/history/`).

Convention: each entry is a brief description, why it matters, where it bites today, and any pointer at where the design discussion lives. Mark `[x]` when complete; promote items into a session/rollout doc if the work warrants one.

---

## Current state (2026-05-18, end-of-session)

Tree at 1100+. **ZERO disabled tests.** Major tracks closed this session:
- **MultiClassing track FULLY CLOSED** (Phase 3 v4 via full vbase ABI rollout).
- **Lombok-mirror track FULLY CLOSED** — @Getter/@Setter (10) + @ToString (12) + @NoArgs/@AllArgs/@RequiredArgs (8) + @Data/@Value (6) + @NonNull (7) + @With (6) + @Builder (6) = 55 tests.
- **Static nested classes** shipped as @Builder prerequisite (5 tests).
- **Views-as-class-field doctrine enforced** — `CAJETA_ERROR_VIEW_AS_CLASS_FIELD` for direct view fields, view[], or templated containers with view K/V (9 tests; resolves Priority 2 § 6).
- **Method-template surface complete** — Form C is the sole call syntax (`Type.method<T>(args)`), two-layer naming for instantiations, lambda block-body return-type inference, NewExpression lambda-arg propagation.
- **`this`-passing convention fix** — `loadIfLValue` catch-all was loading class-typed `ptr` slots as inline structs; fixed (3 tests). Unblocks a latent class of "pass this to another class's ctor" cases.
- **Return-ownership enforcement** — `return heap/new/stack X` requires `#T` return type. New CAJETA_ERROR_FRESH_RETURN_NEEDS_TRANSFER check; ~14 stdlib sites swept; 7 test files updated.
- **`@Encoding(EncoderClass)` shipped end-to-end** — Phase A mutual-exclusion (5 tests) + Phase B real synthesizer (5 tests) for `T(byte[])` ctor calling `EncoderClass.decode` + `byte[] toBytes()` calling `EncoderClass.encode`. Decoded temp shell now correctly freed (no leak). Resolves Priority 2 § 10.
- **Templated interfaces in stdlib** — `visitInterfaceDeclaration` now processes typeParameters and skips body-walk for template interfaces (2 tests). Shipped `cajeta.wire.Encoder<T>` interface; @Encoding dispatch path still duck-typed.

Recently landed (Phase 7 + P6.5/P6.7 + memory-model gap pass + Priority 1 push + mode/debug-features push + P1.1 template-field codegen + P1.1 follow-ups + inline-MCE-as-ctor-arg + intermediate Stream combinators):
- `struct` is a transitional alias for `class`; CajetaAggregate retired.
- `(T) -> void` parses as a method-parameter type; lambda-arg expectedType inference works.
- `cajeta.lang.Stream.forEach` + `cajeta.collection.ArrayList<T>` landed.
- `Optional.get()` throws `CAJETA_ERROR_NONE_UNWRAP` on empty.
- **[GAP 1] Virtual dispatch on drop — done.** Vtable header carries `drop_fn` slot; `__cajeta_class_virtual_drop` loads through the dynamic type's vtable.
- **[GAP 2] Block-exit drops — verified done** (already worked).
- **[GAP 3] TLS / per-fiber drop chain — verified done** (already worked).
- **[GAP 4] Alias-mutation borrow check — done.** `Scope::liveBorrows`; assignment-site check throws on prefix overlap. Pinned by `test/parser/AliasMutationBorrowTests.cpp` (3 tests).
- **[GAP 5] Owned-string drops — done.** Owned-string shapes (binary `+`, routed string method intrinsics) register `__cajeta_free`. Pinned by `test/parser/OwnedStringDropTests.cpp` (4 tests).
- **Static class fields — done.** `CajetaClass::getOrCreateStaticFieldGlobal` emits LLVM globals named `<class.canonical>.<field>`; DotExpression resolves class-name LHS via `canonicalMap`. Literal initializers (`= 100`, `= -7`, floats) constant-fold into the global's initializer. Lambdas reading/writing statics work because statics resolve as globals, not captures. Pinned by `test/parser/StaticFieldTests.cpp` (9 tests) + `test/parser/LambdaStaticCaptureTests.cpp` (2 tests).
- **Automatic field drops — done.** Per-thread live-allocation set discriminates owner vs alias at drop time; auto-drop calls type-specific helpers that atomically claim out of the set so the second drop attempt on an aliased address (ArrayStream.data aliasing ArrayList.data, Optional<Hello>.value aliasing a local) silently no-ops. Doctrine in `cajeta-docs/stdlib/FieldOwnership.md`; spec at `cajeta-docs/stdlib/MemoryModel.md` § Destructors. Pinned by `test/parser/AutoFieldDropTests.cpp` (6 tests) + `test/parser/FieldOwnershipAliasingTests.cpp` (6 tests).
- **Compiler mode infrastructure — done.** `CompilerMode` enum (Debug, DebugRelease, Release, Fast, Minimal) + `CompilerFlags` struct with 13 toggle fields; CLI parses `--mode=`, flavor aliases, and per-feature overrides. Threaded through `Compiler` → `CajetaModule`. Doc: `cajeta-docs/CompilerModes.md`.
- **Source-tagged drop-chain entries — done.** When `flags.sourceTags` is on (default under `--debug`), every drop-chain push carries the alloc-site file + line via `cajeta_drop_entry_debug` (40 B vs 32 B base). Pinned by `test/parser/SourceTaggedDropTests.cpp` (6 tests).
- **SIGABRT handler with chain walk — done.** Runtime constructor installs the handler at load; on abort, dumps the per-thread drop chain (with source tags when available) and chains to the previous handler.
- **Template-instantiation field codegen — done.** All class-typed fields (templated or plain) now lay out as `ptr` rather than the inline class body; matches the `pass class by pointer` rule in Method.cpp. Also fixed companion issues: BinaryOpExpression loadIfLValue + BINARY_OP_ASSIGN slot-type unified to `ptr` for class refs, and MethodCallExpression receiver-load now load-throughs chained DotExpression field GEPs for class-ref receivers (`this.field.method()`). Unblocks the wrapper-stream pattern: `TakeStream<T>` landed as the first intermediate combinator with a `Stream<T> source;` field. Pinned by `test/parser/StreamIntermediateTests.cpp` (5 tests).
- **Subtype-aware ctor / method param matching — done.** `resolveMethod` now falls back from exact canonical-key lookup to a positional scan that walks each arg's `superClasses` via BFS, returning the most specific match. Lets `ArrayStream<int32>` args bind to `Stream<int32>` params (ctor or otherwise) without an explicit upcast local. Pinned by `test/parser/SubtypeArgLookupTests.cpp` (4 tests, both user-defined hierarchy and stdlib templates).
- **Intrinsic-result upcast JIT symbol resolution — done.** A user method body that triggers a stdlib template instantiation mid-codegen (e.g. `xs.stream()` lowers to `ArrayStream<int32>::ArrayStream(...)`) used to leave that instantiation's method bodies un-emitted because the Phase 2 loop had already swept stdlib. The Phase 2 (and Phase 1) walk now loops until the total method count across all modules stops growing — `Method::getLlvmFunctionType` and `Method::generateCode` are both idempotent, so revisiting already-emitted methods is a no-op. Applied to both `Compiler::compile` and the JIT test harness. Pinned by `test/parser/UpcastInitializerTests.cpp` (2 tests).
- **Inline-MCE-as-ctor-arg — done.** `heap T(xs.stream(), n)` or `heap T(Mk.make(xs), n)` used to crash because `MethodCallExpression` never set its own `resolvedType`. ClassCreatorRest fell back to `CajetaType::of(value)` which returns the generic `pointer` type for an opaque-pointer Value; `Method::buildGeneric` then built the wrong lookup key, the lookup failed, and downstream code crashed. Fix mirrors `NewExpression::resolveTypes`: MCE now pins `resolvedType` from the called method's return type (both the `.stream()` intrinsic and the general dispatch path), and `loadIfLValue` gets the same NewExpression-style carve-out — for MCE-typed expressions the returned pointer IS the language-level value and shouldn't be load-throughed. Pinned by `test/parser/InlineCtorArgTests.cpp` (3 tests covering intrinsic, user-method, and nested-heap forms).
- **Function-typed field invocation (`this.fn(args)`) — done.** MCE now detects when the receiver is a class and the called name resolves to a `CajetaFunctionType` field; loads the closure record (same `{ ptr fn, ptr captures, ptr drop_fn }` layout the local-lambda path uses) and indirect-calls. Previously the dispatch fell through to ordinary method lookup, found nothing, returned null, and downstream codegen silently substituted defaults (`i1 false` for boolean-returning lambdas, garbage for everything else). Unlocked by the wrapper-stream pattern across all intermediate combinators.
- **Intermediate Stream combinators (P2.2 second slice complete) — done.** Landed `SkipStream`, `FilterStream`, `MapStream<T, R>` (cross-type), `PeekStream` (side-effecting passthrough), and `FlatMapStream<T, R>` (nested-stream flattening with inter-pull state). All inherit from `Stream<T>` (or `Stream<R>` for type-changing variants) and use the wrapper-stream pattern unlocked by P1.1. Pinned by `test/parser/StreamIntermediateTests.cpp` (19 tests total: 5 take + 4 skip + 3 filter + 3 map + 2 peek + 2 flatMap).
- **MultiClassing Phase 1 (P-1 collision rejection) — done.** `CajetaClass::buildVirtualTable` gathers methods per suffix across the full ancestor closure and rejects sibling collisions (two declaring classes with no inheritance relationship between them, no self override) via `CAJETA_ERROR_AMBIGUOUS_METHOD_DISPATCH`. Same walk catches return-type collisions (`void fn()` vs `int32 fn()`) via `CAJETA_ERROR_RETURN_TYPE_COLLISION` — covariant overrides allowed when one return type is the ancestor of the other. `DotExpression::generateCode` gathers properties across all parents and rejects `c.field` when the name is declared on two sibling parents (no self shadow) via `CAJETA_ERROR_AMBIGUOUS_FIELD_ACCESS`. Common-ancestor cases (one declaration, multiple paths) and disjoint cases (different names) pass cleanly. Error messages point at remediation: override / self-declare / qualify via `super[Base].m()` or `this[Base].field` (Phase 2). Pinned by `test/parser/MultiClassingPhase1Tests.cpp` (7 enabled tests + 1 DISABLED_ for the pre-existing P-5 dispatch gap).
- **MultiClassing Phase 2 (P-2 bracketed parent-view selectors) — done.** Grammar extension: `primary` accepts `THIS '[' typeType ']'` and `SUPER '[' typeType ']'` (longer alternatives listed before bare `THIS` / `SUPER` so ANTLR's adaptive prediction picks the bracketed form when `[` follows). Visitor pulls the bracketed type name and stores it on `ThisExpression` / `SuperExpression`. `resolveTypes` validates the chosen ancestor against the current class's transitive parent set (via new static helper `resolveBracketedAncestor`) and pins `resolvedType` to the chosen ancestor — `CAJETA_ERROR_NOT_AN_ANCESTOR` fires for unrelated types. `generateCode` loads `this` and reuses `CajetaClass::adjustForUpcast` to shift to the chosen sub-object; `DotExpression`'s field-resolution path naturally lands on the ancestor's `propertyList` (and slot index) because `klass` becomes the ancestor. `MethodCallExpression`'s existing SuperExpression detection routes through `forceDirectCall=true` unchanged — the only new requirement is that `this` arrives pre-adjusted, which the new `generateCode` provides. Pinned by `test/parser/MultiClassingPhase2Tests.cpp` (7 tests: sibling-parent method combine, single-inheritance back-compat, no-infinite-loop guard, field read/write of independent slots, single-inheritance field back-compat, non-ancestor type rejection for both forms).
- **MultiClassing Phase 3 v1 (P-4 diamond ancestor canonicalization) — done.** `CajetaClass::generatePrototype`'s `embedSubObject` lambda now only records `subObjectSlotMap[ancestor]` on the FIRST encounter; subsequent walks through a diamond ancestor's other paths still emit storage but don't overwrite the canonical offset. Result: `getSubObjectByteOffset(A)` and therefore `adjustForUpcast(this, D, A)` return the canonical (first-emitted) position, so `this[A].x = N; return this.x;` in D's own code is a round-trip rather than a write-here-read-there (the pre-fix bug returned 0 because writes went to the C-chain inline-A and reads went to the B-chain inline-A). First-parent inherited methods touching A internally via `this.x` also reach the canonical A. Pinned by `test/parser/MultiClassingPhase3Tests.cpp` (5 enabled tests). Layout dedup (saving the duplicate-A space) deferred indefinitely — preserves dormant-A storage for inherited-method-on-non-first-parent compatibility (Phase 3 v3 case).
- **MultiClassing Phase 3 v2 (cross-path bracketed access) — done.** `DotExpression::generateCode` now detects diamond access at the use site by comparing two byte offsets on the diamond-anchor class (the receiver's static type): the canonical offset of the property's declaring class vs. the via-LHS-path offset. When they differ, `base` is shifted by `(canonical - via_lhs)` to recover the canonical position and the final GEP is redirected to use declaringClass's standalone struct type. The anchor is the structure-stack class when LHS is `This`/`Super` (those forms ARE the enclosing instance) and `klass` (the receiver's static type) for any other LHS — avoids false-firing when a caller in an unrelated method references a diamond-shaped instance. Also fixed two helper paths: `DotExpression`'s GEP load-through branch now skips for `This`/`Super` LHS (those primaries return ready-to-use pointers, not field-of-field GEPs), and `This`/`SuperExpression::generateCode` self-resolve when `resolvedType` is null so the adjustment fires even inside `LocalVariableDeclaration` initializers (whose pre-pass doesn't visit the RHS). Pinned by 4 new tests in `MultiClassingPhase3Tests.cpp` (read via non-first parent, write via non-first parent, both-paths-see-same-storage round trip, sanity for first-parent non-diamond access). The Phase 1 + v1 disabled test for `this[C].x` is now enabled and passing.
- **MultiClassing Phase 3 v3 (inherited-method re-adjustment) — done.** In `MethodCallExpression`'s super-dispatch path, when the resolved method's declaring class differs from the bracketed receiver class AND a diamond exists between the bracketed and declaring classes in the enclosing class, shift `thisValue` from the bracketed position to the declaring class's canonical position. Same offset formula as v2: `delta = enclosing.getSubObjectByteOffset(declaring) - (enclosing.getSubObjectByteOffset(bracketed) + bracketed.getSubObjectByteOffset(declaring))`. For `super[C].setX(88)` in Diamond where `setX` is inherited from A: bracketed=C (offset 16), declaring=A (canonical offset 0), delta=-16; the universal `setX` LLVM function (compiled for an A-pointer receiver) gets called with the canonical A-pointer instead of C-adjusted (which would hit dormant inline-A). Pinned by the previously-DISABLED_ `inheritedMethodOnNonFirstParentReachesSharedA` test — now enabled and passing. Out of scope (v4): non-first parent's OWN methods (declared on C, not inherited) that internally touch shared ancestor fields via `this.x`. Their declaring class equals the bracketed class, so no v3 re-adjust fires; C's IR runs with C-adjusted `this` and GEPs land on dormant inline-A. Fixing requires vbase ABI or per-descendant recompilation. No test pinned for this gap — it's an exotic pattern; composition is the recommended workaround.
- **MultiClassing P-5 dispatch fix (abstract+concrete sibling satisfaction) — done.** `CajetaClass::buildVirtualTable`'s alias walk no longer skips abstract methods. When `A` declares `abstract step()` and sibling `B` provides concrete `step()`, the alias walk now also re-keys A.step's canonical to point at B's concrete impl in the vtable. Pre-fix the abstract-skip kept A.step's canonical out of the vtable entirely; dispatch via that canonical (which is what method resolution picks when walking A's chain first) aborted because no entry existed for the looked-up hash. The abstract-obligation enforcement check below the alias walk already accepted the class shape — only the dispatch-time vtable lookup was broken. Re-enabled `MultiClassingPhase1Tests.abstractInOneParentConcreteInOtherSatisfies` (previously DISABLED_), which now passes. Tree dropped to ZERO disabled tests.
- **`adjustForUpcast` threading through assignment, return, and parameter-passing sites — done.** Pre-fix, the helper was wired only at `LocalVariableDeclaration` (Phase 1 of poly-MI); the other three upcast sites silently stored raw descendant pointers into ancestor-typed slots, breaking field access and method dispatch when the ancestor was a non-first parent (whose sub-object lives at non-zero offset in the descendant's layout). The threading: (1) `BinaryOpExpression::generateCode`'s BINARY_OP_ASSIGN path now calls `adjustForUpcast` on the RHS value before the store when LHS/RHS class types differ; (2) `ReturnStatement::generateCode` adjusts the returned value when the enclosing method's declared return type is a different class than the expression's resolved type; (3) `CajetaClass::invokeMethod`'s parameter-coercion loop adjusts each arg before binding to the formal parameter, using `method->getParameterList()`'s formal types as the destination. All three call the same `CajetaClass::adjustForUpcast` helper. Pinned by `test/parser/UpcastThreadingTests.cpp` (5 tests: three for the new sites + two sanity tests verifying first-parent and same-class paths still work without shift).
- **MultiClassing R-3 `@Override(from=Parent)` verification — done.** When a method on the class carries `@Override(from=Ancestor)` (or `from=Ancestor.class`), `CajetaClass::buildVirtualTable` walks the receiver class's ancestor chain to confirm the named class is actually an ancestor, then walks that ancestor's `methodList` for a same-suffix method (name + params). Raises `CAJETA_ERROR_OVERRIDE_FROM_MISMATCH` when either check fails — catches typos, misspelled ancestor names, and signature drift between parent and child declarations. Both identifier and `.class` literal forms accepted (visitor classifies them as String / ClassRef respectively; the verifier checks both kinds). Bare `@Override` without `from=` keeps its no-op behavior. Pinned by `test/parser/OverrideFromTests.cpp` (6 tests: 3 positive [matching ancestor, .class form, multi-inheritance pick-one], 2 negative [unrelated class, ancestor without matching method], 1 sanity [bare @Override still compiles]).
- **MultiClassing R-2 narrow implicit-ctor-skip warning — done.** In `Method::generateCode`'s implicit super-ctor emission loop, when the ctor body contains an explicit `super(args)` call AND a sibling (non-first) parent has BOTH a no-arg ctor and an args ctor, emit `warning: [implicit-ctor-skip]` to stderr naming the sibling. The warning is narrow on purpose: all three conditions (explicit super call exists, sibling has both flavors, no-arg was picked implicitly) must hold to avoid flooding the build log on intentional skips. AST walk uses `getMethods()` (the map, where ctors live) rather than `getMethodList()`; expression-statement bodies special-cased via `findSuperCtor` → `ExpressionStatement::getExpression()` since the wrapped expression isn't in `getChildren()`. Pinned by `test/parser/ImplicitCtorSkipWarningTests.cpp` (4 tests: 1 positive [the footgun shape], 3 negatives [sibling-only-noarg / no-explicit-super / sibling-only-args]).
- **HashSet stdlib collection — done.** Thin wrapper around `HashMap<T, int8>` in `runtime/src/cajeta/collection/HashSet.cajeta`. Methods: `add(T)`, `contains(T)`, `remove(T)`, `count()`. Uses int8 presence sentinel (value=1) rather than `HashMap<T, T>` to sidestep a latent HashMap bug: `get()`'s miss path `return 0` fails LLVM verifier when V is a class type (`ret i64 0` against a `ptr` return type). Set semantics get tested separately from that bug — HashSet's API doesn't expose the value side. Now works for both class T and primitive T (via the existing primitive-`.hash()` intrinsic). Pinned by `test/collections/HashSetTests.cpp` (9 tests). Bumped `STDLIB_STRUCTURE_COUNT` to 19.
- **HashMap class-typed V miss-path fix — done.** Replaced `return 0;` in `HashMap.get()` with `V[] missDefault = new V[1]; return missDefault[0];`. The array-element trick zero-inits with the right LLVM type for both primitive V (`i32 0` etc.) and class V (`ptr null`). Unblocked HashMap<class, class>; pinned by `HashMapTests.classTypedValueWorks`.
- **LinkedList stdlib collection — done.** Doubly-linked list at `runtime/src/cajeta/collection/LinkedList.cajeta` + `LinkedListNode.cajeta`. Methods: `add(T)` (append), `addFirst(T)` (prepend), `get(int64)` (walks from closer end), `remove(T)` (find by `==` and unlink), `contains(T)` (boolean find), `count()`. Works for primitive AND class T since `==` is built-in for primitives and identity-equal for classes — no `hash()` requirement. Pinned by `test/collections/LinkedListTests.cpp` (11 tests covering both T flavors + edge cases like empty list, out-of-range get, mid-list remove).
- **Templated-class-with-class-T-field load type fix — done.** `ReturnStatement::generateCode`'s DotExpression load branch was using `found->getType()->getLlvmType()` which for a class field returns the class's inline struct type instead of `ptr`. Applied the same pass-by-pointer rule that BinaryOpExpression's assignment-LHS already uses (class-typed / array-typed → `ptr`; views / interfaces keep inline storage). Unblocks any `return this.value;` where value is class-typed in a templated container — Optional<class T>, LinkedListNode<class T>, etc. Pinned indirectly by all the class-T collection tests.
- **HashMap.keys()/values()/entries() stream views — done.** Three new stream classes in `runtime/src/cajeta/lang/stream/`: `HashMapKeyStream<K, V>`, `HashMapValueStream<K, V>`, `HashMapEntryStream<K, V>` (entries yields `Pair<K, V>`, one heap-allocated per slot). All extend `Stream<T>` and walk the underlying HashMap's slot arrays skipping EMPTY/TOMBSTONE. Snapshot semantics — the stream captures array refs at construction; concurrent modification (resize / put / remove) leaves behavior undefined. Pinned by `test/collections/HashMapStreamTests.cpp` (6 tests). Bumped `STDLIB_STRUCTURE_COUNT` to 24.
- **Stream<class T> terminals with class-typed lambdas — done.** `MethodCallExpression`'s lambda-as-arg expectedType propagator now walks the parent chain when looking for the called method, so inherited methods like `Stream<T>::forEach` are found from receiver subclasses like `ArrayStream<T>`. Without this, the lambda's expectedType stayed null and expression-bodied lambdas inferred the wrong return type, causing the call to silently drop. Plus a stdlib fix in `Stream<T>::findFirst`: removed the `T v = o.get();` local that was registering a spurious drop entry for class-typed T (stream source owns the elements; the local's drop chain entry was freeing them prematurely and the returned Optional dangled). Replaced with two direct `o.get()` calls. Pinned by `test/parser/StreamClassTTests.cpp` (10 tests).
- **NewExpression lambda-arg expectedType propagation — done (2026-05-18).** `ClassCreatorRest::generateCode` now mirrors the `MethodCallExpression` pattern: when any ctor arg is a LambdaExpression without a resolved type, look up the matching ctor on the target class by arity, find the corresponding formal parameter, and call `lambda->setExpectedType(formal)`. Lets `heap T(seed, (acc, x) -> acc + x)` infer the lambda's parameter types from the ctor's formal, so users (and stdlib) can drop typed-param spellings in the common case. Stdlib uptake: `Collectors.toList` slimmed from `(ArrayList<T> acc, T x) -> ...` to `(acc, x) -> ...`. Pinned by `LambdaL15Tests.bareParamsInferFromCtorArgFormal` (1 new test) + the existing `CollectorTests.collectorsToList*` (still green after the stdlib slim).
- **Method-template Form C as sole call syntax — done (2026-05-18).** `Type.method<T>(args)` is the canonical and only spelling. Grammar updated: `methodCall` accepts an optional `('<' typeList '>')?` between identifier and `(`; `superSuffix` mirrors. Java-style Form A (`Type.<T>method(args)`) removed from the grammar entirely. Form B (`Type<T>.method(args)`) was rejected — one syntax is better than two, and Form C mirrors `Type<T>` at the type-use site. Docs updated (`MethodLevelTemplate.md`, `Features.md`, `Lambdas.md`). Pinned by `MethodTemplateExplicitArgsTests` (6 call sites rewritten + previously-disabled `staticExplicitWhenInferenceWouldFail` re-enabled).
- **Two-layer naming for method-template instantiations — done (2026-05-18).** `Method::getMapKey(bool labeled)` separates the map-lookup key (toCanonical only) from the LLVM symbol (toCanonical + method-arg suffix). Unblocked zero-value-param static factory templates like `Collectors.toList<int32>()` — pre-fix, the map key collided with any same-canonical instantiation and `addMethod` rejected the second one silently. Now distinct method-template instantiations share a key only when their value-param tuple matches; the LLVM symbol still encodes the suffix so the linker sees unique names. Pinned by `CollectorTests.collectorsToListSize` + `collectorsToListPreservesOrder` (both re-enabled).
- **Lambda block-body return-type inference — done (2026-05-18).** `Expression.cpp` gained `inferLambdaReturnFromBody` — walks the body for the first ReturnStatement, skipping nested lambdas. Inserted as the 3rd preference in `LambdaExpression::resolveTypes` (after expectedType and Expression-body resolvedType, before void fallback). The walker explicitly recurses into ExpressionStatement, ReturnStatement, IfStatement, LocalVariableDeclaration, MethodCallExpression payloads (Statement wrappers don't expose their payload via `getChildren()`). Fixed `Collectors.toList`'s synthesized accumulator lambda crashing JIT verify with "Found return instr that returns non-void in Function of void return type" — pre-fix the block-body lambda inferred void instead of `ArrayList<T>`. Pinned by `LambdaL2Tests.blockBodyReturnTypeInferredFromBodyUnderCtorArg`.
- **View-as-class-field doctrine enforcement — done (2026-05-18).** `CajetaClass::generatePrototype` walks the class's propertyList after parent resolution and rejects any field whose type is a view (directly) or an array-of-view. Raises `CAJETA_ERROR_VIEW_AS_CLASS_FIELD` with a clear message pointing at `cajeta-docs/stdlib/Views.md` and offering the byte[]+reconstruct workaround. The check is skipped for view types themselves so nested-view declarations (`view A { view B inner; }`) continue to work per Views.md § Nested views. Fires naturally for templated containers — `HashMap<*, view>` instantiates `V[] vals` as `view[]`, caught by the array branch; `Optional<view>` has `T value`, caught by the direct branch. Replaces the previously documented (but broken) JIT-verify cascade with a clean compile-time error. Pinned by `test/parser/ViewAsClassFieldRejectionTests.cpp` (9 tests: direct view field, array-of-view, HashMap V=view, HashMap K=view, ArrayList<view>, HashSet<view>, Optional<view>, plus sanity for nested-views-allowed and ordinary-class-fields-unaffected). Resolves Priority 2 § 6 (see entry for rationale).
- **MultiClassing Phase 3 v4 — full vbase ABI shipped (2026-05-18).** Four-layer change: (1) **Layout** — `CajetaClass::generatePrototype`'s `embedSubObject` lambda now appends one `ptr` vbase slot per transitive non-Object ancestor at the end of each sub-object slice; `vbaseSlotMap` records self's own; `getFieldLlvmIndex` and `getNonFirstSubObjects` advance their slot cursor past the vbase ptrs in their respective walks. (2) **Ctor init** — `Method::generateCode`'s ctor branch writes each vbase slot to `&self + getSubObjectByteOffset(ancestor)` BEFORE super-ctor calls so super-ctor bodies that read inherited-via-vbase find consistent pointers. (3) **Descendant fixup** — after super-ctor calls, non-first parents' vbase slots get overwritten to the descendant's canonical (first-encountered) ancestor positions so diamond ancestors are shared at the universal vbase indirection layer. (4) **DotExpression** — when the picked declaring class differs from the receiver's static type and the type isn't a view, GEP `vbase_slot = StructGEP(klass.struct, base, vbaseSlotIdx)`, LOAD `vbase_ptr`, and redirect base to `vbase_ptr` against `declaring.struct`. The earlier v2 cross-path canonical-base trick and v4 narrow MCE shift are both removed (universal vbase indirection subsumes both). Phase 3 v4 is no longer parked: methods declared on a non-first parent (not inherited) that touch BOTH own and inherited fields, or that call `this.foo()` where the helper recursively touches inherited state, both work. Pinned by 13 `MultiClassingPhase3Tests` including the two new full-v4 tests: `ownMethodOnNonFirstParentTouchesBothOwnAndInheritedFields` and `ownMethodOnNonFirstParentCallsThisHelper`.

Stack vs heap class instantiation is fully working: `heap T(args)`, `stack T(args)`, `heap T { … }`, `stack T { … }`, all with vtable init + ctor invocation + correct virtual destructor dispatch. See `test/parser/UnifiedClassSyntaxTests.cpp:44,137,184,249,317,576`, `test/parser/ClassDropTests.cpp`, and `test/parser/VirtualDropDispatchTests.cpp`.

---

## Remaining work — single prioritized list

Priority is rough effort × user-visible correctness impact.

### Top priorities for next session (2026-05-18, post-@Encoding)

With MultiClassing + Lombok-mirror + nested classes + view-as-class-field + @Encoding all closed, recommended next picks:

1. ✅ **Features.md S-1101 — `cajeta.codec.json` spec — SHIPPED 2026-05-18.** Spec landed at `cajeta-docs/stdlib/codec/Json.md`. Three-tier design (`Json.parse<T>` / `Json.toBytes` codegen / pull tokenizer / value tree), per-field annotation surface (`@JsonProperty`, `@JsonIgnore`, `@JsonRequired`, `@JsonAlias`, `@JsonInclude`, class-level `@JsonNamingStrategy` / `@JsonStrict`), lazy number parse with raw-byte storage, zero-copy string slices, performance-first goals (≥ 500 MB/s scalar tokenizer target; v2 SIMD direction noted), RFC 8259 strict-by-default with optional `relaxed=true` for trailing commas + `//`/`/* */` comments. Error model wired to `RecoverableException` per ErrorModel.md. JSON does NOT use `@Encoding` (that stays for binary formats only); the rationale section in Json.md spells out the divide. Next: S-1102 implementation.
2. ✅ **Features.md S-1102 — `cajeta.codec.json` — FULL TIER 1/2/3 SHIPPED.** `[mode-agnostic]` — Phase 1+2+3+4a (2026-05-18) landed reader/writer/value-tree + factory entry points. **Phase 4b shipped** through commits `8b7b3be` … `242abe5` (2026-05-18..19): Tier-1 `Json.parse<T>(int8[], int64)` / `Json.parse<T>(String)` / `Json.toBytes<T>(T)` direct-codegen synthesizer at `src/cajeta/codec/JsonSynthesizer.cpp`, with the full per-field annotation surface (`@JsonProperty`, `@JsonIgnore` w/ asymmetric `onRead`/`onWrite`, `@JsonRequired`, `@JsonAlias`, `@JsonInclude` w/ ALWAYS|NEVER|NON_NULL|NON_DEFAULT, `@JsonRaw`), class-level `@JsonNamingStrategy` (SNAKE_CASE | KEBAB_CASE | PASCAL_CASE | CAMEL_CASE | IDENTITY), `@JsonStrict` unknown-key rejection, `Optional<T>` field arms (present / absent / null distinction), nested classes via recursive `parseObjectFromReader<T>` / `toBytesObjectInto<T>` helpers, nested-class arrays. Pinned by `test/parser/JsonSynthesizerTests.cpp` (70 tests). `@ToString(format=TO_STRING_JSON)` synthesizer hook is now unblocked (currently still throws `CAJETA_ERROR_TOSTRING_JSON_NOT_IMPLEMENTED`; trivial follow-up to delegate to `Json.toBytes<T>(this)`).

   **All three Phase 4b compiler workarounds resolved 2026-05-21:**
     - ✅ `int8` method params — turned out to already work in the current compiler. Removed the int32-pass-then-narrow workaround on `JsonWriter.append`, restoring the natural `int8` signature.
     - ✅ `boolean[]` ICmp — fixed by routing `BinaryOpExpression`'s LOGAND/LOGOR LHS+RHS through `loadIfLValue` (was `loadIfAlloca`) so a boolean-array GEP load-throughs to its i1 value before the icmp-with-zero coercion. `JsonWriter.sibling` is now `boolean[]`.
     - ✅ Enum-typed class fields — `ArchivePrescanVisitor` now visits enum declarations and marks them in a new `g_enumArchive` side-set; `CajetaType::fromContext`'s placeholder-synthesis path consults this and synthesizes an i32-backed enum CajetaType (via the same shape as `visitEnumDeclaration`) instead of a class placeholder. `JsonReader` now stores `JsonToken current` / `JsonToken peekedToken` directly, and all `this.current = N; // X` ordinal stores became `this.current = JsonToken.X;`. The synthesizer's emitted `int32 t = r.next();` became `JsonToken t = r.next();`. Pinned indirectly by the existing JsonReader / JsonSynthesizer tests (all 106 passing).
3. **Implementing-class instantiation of templated-interface vtables.** ~1-2 sessions of compiler work. Would let `@Encoding` tighten to verify `encoder implements Encoder<T>`, plus unblock other templated-interface patterns. Currently @Encoding stays duck-typed.
4. **Priority 2 § 8 — Fluent stream chains.** ✅ SHIPPED 2026-05-18 (see Priority 2 § 8 below).
5. ✅ **Priority 2 § 12 — Implicit destructor chaining (C++ semantics) — SHIPPED 2026-05-21.** `[mode-agnostic]` — `CajetaClass::emitDropBodyInline` factors the per-class user-body + own-field-drops emission; `CajetaClass::collectDestructorChain` returns the deduped transitive ancestor list in reverse-DFS reverse-decl order (diamond ancestors run exactly once). Both `getOrCreateDropFunction` (heap) and `getOrCreateStackDropFunction` (stack) call self.emitDropBodyInline then walk the chain, adjusting `this` to each ancestor's canonical sub-object position via `getSubObjectByteOffset` for non-first parents. `__cajeta_free` runs once at the most-derived heap wrapper. Pinned by `test/parser/DestructorChainTests.cpp` — original 6 tests still pass plus 3 new ones: `multiInheritanceBothParentsRun`, `multiInheritanceOrderIsReverseDecl` (digit-encoded order = 321), `diamondSharedAncestorRunsOnce` (digit-encoded order = 4312, confirming A runs once not twice). Doc updated in MemoryModel.md § 140.
6. **Priority 6 § 5 — Restore lost test coverage from Phase 7.** Incremental, low-risk; pick up between bigger pieces.
7. **Priority 4 / 5 — Debug-mode + release-mode features.** Most haven't been touched; quality-of-life features (`--diag-hints`, `--bounds=trap`, `--null-checks` codegen).
8. **Small polish items.** `@Builder.Default` for per-field defaults; `@Getter(level="private")` visibility tightening. ~0.5 session each.

### Priority 1 — compiler infrastructure

**MultiClassing track FULLY CLOSED 2026-05-18.** P1.1 (compiler mode infrastructure), P1.2 (annotation argument capture), P1.1-template-field-codegen, P1.1-subtype-ctor-lookup, P1.1-intrinsic-upcast-jit, inline-MCE-as-ctor-arg, and all MultiClassing phases/rules (P-1..P-6, R-1..R-4) all shipped. Phase 3 v4 (the last open MultiClassing piece) landed via full vbase ABI rollout 2026-05-18. The annotation-arg machinery in `Annotatable` (typed `getString`/`getInt`/`getBool`/`getClassRef`/`getStringList`/`getIntList` accessors) + `CajetaLlvmVisitor::parseAnnotationInstance` populates args for every annotation site. Live consumers: `@Order(n)`, `@Component(name=...)`, `@Inject(name=..., allocate=...)`, `@SuppressLint(...)`, `@Native(value=...)`. New annotations that take args plug into the same machinery — no new infrastructure needed.

#### Open P1 items — none

All Priority 1 items shipped. Historical MultiClassing item log retained below for context.

#### Historical: MultiClassing implementation (from `cajeta-docs/stdlib/MultiClassing.md`)

1. ✅ **MultiClassing Phase 1 — collision rejection (P-1) — SHIPPED 2026-05-17.** `[mode-agnostic]` — landed in `CajetaClass::buildVirtualTable` (method ambiguity + return-type collision; covariant returns allowed) and `DotExpression::generateCode` (field ambiguity at access site). Pinned by `test/parser/MultiClassingPhase1Tests.cpp` (7 enabled + 1 DISABLED_ — the disabled test pins the pre-existing P-5 dispatch gap surfaced during Phase 1 work; see new item below).

2. ✅ **MultiClassing Phase 2 — `super[Base].method()` + `this[Base].field` (P-2, R-1) — SHIPPED 2026-05-17.** `[mode-agnostic]` — grammar `primary` extended; visitor wires chosen-ancestor name onto `ThisExpression` / `SuperExpression`; `resolveTypes` validates ancestor (`CAJETA_ERROR_NOT_AN_ANCESTOR` on miss); `generateCode` reuses `CajetaClass::adjustForUpcast`. `MethodCallExpression`'s SuperExpression force-direct path unchanged. Pinned by `test/parser/MultiClassingPhase2Tests.cpp` (7 tests).

3. ✅ **MultiClassing Phase 3 v4 — DONE (full vbase ABI shipped 2026-05-18).** `[mode-agnostic]` — v1, v2, v3, v4 narrow (2026-05-17/18) all superseded by the full vbase ABI rollout per the MultiClassing doctrine. Every class with at least one non-Object ancestor now reserves one `ptr` slot per transitive ancestor at the end of its layout; ctors initialize these to inline ancestor positions; diamond descendants' implicit auto-ctor overwrites non-first parents' vbase slots to canonical (first-encountered) positions. `DotExpression::generateCode` loads the vbase pointer for the declaring class instead of GEPing directly into the inherited block. The earlier v2 cross-path adjustment and v4 narrow MCE trick are removed (universal vbase indirection subsumes both). Pinned by all 13 `MultiClassingPhase3Tests`, including the two new full-v4 tests: `ownMethodOnNonFirstParentTouchesBothOwnAndInheritedFields` (method touches both own AND inherited fields) and `ownMethodOnNonFirstParentCallsThisHelper` (method calls `this.foo()` that recursively touches inherited fields). Both pass — Phase 3 v4 is no longer parked.

#### Open P1 items — other

4. ✅ **Thread `adjustForUpcast` through remaining upcast sites — SHIPPED 2026-05-17.** `[mode-agnostic]` — wired through BinaryOpExpression's assignment path, ReturnStatement's typed-return path, and CajetaClass::invokeMethod's parameter-coercion loop. Pinned by `test/parser/UpcastThreadingTests.cpp` (5 tests).

5. ✅ **MultiClassing R-3 — `@Override(from=Parent)` optional annotation — SHIPPED 2026-05-17.** `[mode-agnostic]` — `buildVirtualTable` verifies named ancestor exists in receiver's parent chain AND declares same-suffix method; raises `CAJETA_ERROR_OVERRIDE_FROM_MISMATCH` on miss. Both `from=B` and `from=B.class` forms accepted. Pinned by `test/parser/OverrideFromTests.cpp` (6 tests).

6. ✅ **MultiClassing R-2 — narrow implicit-ctor-skip warning — SHIPPED 2026-05-17.** `[mode-agnostic]` — `Method::generateCode` emits `warning: [implicit-ctor-skip]` when ctor body has explicit `super(args)` AND a sibling parent has both no-arg + args ctors AND the no-arg was picked implicitly. Pinned by `test/parser/ImplicitCtorSkipWarningTests.cpp` (4 tests).

7. **MultiClassing R-4 doc cross-reference — done.** `[mode-agnostic]` — aspect-only method-combination policy documented in `cajeta-docs/stdlib/MultiClassing.md` § R-4. No compiler change required; this is a "no, we don't ship CLOS-style combination" decision pinned in design docs.

8. ✅ **MultiClassing P-5 dispatch gap (abstract + concrete sibling) — SHIPPED 2026-05-17.** `[mode-agnostic]` — removed the abstract-skip from `buildVirtualTable`'s alias walk; abstract-declared canonicals now alias to the concrete sibling impl in the vtable. `MultiClassingPhase1Tests.abstractInOneParentConcreteInOtherSatisfies` re-enabled and passing. Tree at zero disabled tests.

### Priority 2 — language surface

1. **More collections — multi-session.** `[mode-agnostic]` — **HashSet, LinkedList, HashMap streams all shipped 2026-05-17.** HashSet is a thin wrapper around `HashMap<T, int8>` (1-byte presence sentinel sidesteps key-pointer duplication). LinkedList<T> is a doubly-linked list working for primitive and class T. HashMap.keys()/values()/entries() return Streams. Remaining: `Collector<T,R>` + `cajeta.lang.Collectors` (unblocks Stream.collect terminal).

2. ✅ **HashMap<K, V> with class-typed V — SHIPPED 2026-05-17.** `[mode-agnostic]` — `HashMap.get()`'s miss path replaced from `return 0;` to `V[] missDefault = new V[1]; return missDefault[0];`. Cajeta zero-inits arrays, so element 0 has the right LLVM type tag and the right zero value (null for class V, 0 for primitive V). Pinned by `HashMapTests.classTypedValueWorks`.

3. ✅ **Templated-class-with-class-T-field load type — SHIPPED 2026-05-17.** `[mode-agnostic]` — `ReturnStatement::generateCode`'s DotExpression load was using `found->getType()->getLlvmType()` which, for a class field, returns the class's inline struct type rather than `ptr`. Applied the same pass-by-pointer rule that BinaryOpExpression's assignment-LHS already uses: class-typed / array-typed fields load as `ptr`; views and interfaces keep inline storage. Unblocked `LinkedList<class T>::get`, `Optional<class T>::get`, `Stream<class T>::next`-wrapped-Optional access, HashMap.entries/keys/values for any K. Pinned indirectly by all the new collection tests for class T (LinkedListTests class-T cases, HashSet class-T cases, HashMapStreamTests).

4. ✅ **HashSet + HashMap support both primitive AND class K/T — SHIPPED 2026-05-17.** `[mode-agnostic]` — turns out the primitive `.hash()` intrinsic was already wired in `MethodCallExpression::generateCode` (primitive-receiver branch lowering to `__cajeta_hash_X` runtime helpers); the docs in HashMap.cajeta and HashSet.cajeta were lying about the "K must be class" constraint. Updated both docs to reflect that `HashSet<int32>`, `HashMap<int32, V>`, `HashMap<Tag, int32>`, etc., all work. Pinned by `HashSetTests.primitiveTypeWorks` + `HashSetTests.primitiveAddDuplicateCountsOnce` + `HashSetTests.primitiveContainsAndRemove` (existing `PrimitiveHashMapTests` also covers the HashMap side).

5. ✅ **Stream<class T> terminals with class-typed lambdas — SHIPPED 2026-05-18.** `[mode-agnostic]` — turned out to be TWO bugs interacting. (a) `MethodCallExpression`'s lambda-as-arg expectedType propagator walked only the receiver's own methods; for `ArrayStream<Counter>.forEach(...)` the forEach is inherited from Stream<T> so `getMethods()` returned nothing, the lambda's expectedType stayed null, an expression-bodied lambda's natural return type (e.g. int32 for an assignment expr) didn't match `(T) -> void`, and the call was silently dropped from the IR. Fix: walk the parent chain looking for the named method (first match wins; subclass overrides shadow). (b) `Stream<T>::findFirst` declared `T v = o.get();` — for class T that registered a drop entry for v, which freed the element at scope exit even though ownership stays with the stream source; the returned Optional held a dangling pointer. Fix: call `o.get()` twice (in predicate + return ctor) so both stay as immediate temporaries with no drop entries. Pinned by `test/parser/StreamClassTTests.cpp` (10 tests: forEach inline-expr / inline-block / pre-declared, findFirst match + no-match, anyMatch / allMatch / noneMatch, reduce return-arg + reduce-via-new). HashMap stream methods (keys/values/entries) over class K + reduce/forEach/findFirst lambda terminals now both work end-to-end.

6. ✅ **Views as K/V in HashMap — RESOLVED via doctrine enforcement (2026-05-18).** `[mode-agnostic]` — decided to enforce the existing `cajeta-docs/stdlib/Views.md` § Errors-caught-statically rule that views cannot be class fields, including via array elements or template instantiation. `CajetaClass::generatePrototype` now walks `propertyList` after parent resolution and rejects any field whose type is a view OR a `view[]` array — raises `CAJETA_ERROR_VIEW_AS_CLASS_FIELD` with a clear message pointing at Views.md and offering the byte[]+reconstruct workaround. Nested views (a `view A { view B inner; }` declaration) are explicitly allowed per Views.md § Nested views — the check skips when the enclosing type IS a view.

   Rationale: (a) the doctrine already said this; the compiler simply wasn't enforcing it. (b) The previously-documented "composition workaround" (`class { view header; }`) DOESN'T WORK end-to-end — probed 2026-05-18 and confirmed a write through the view field segfaults at runtime. (c) The four interlocking bugs the original report listed (return-type mismatch, lambda-arg coercion, hash/equality undefined, copy-vs-reference semantics) all stem from views being borrowed-lifetime handles that don't fit class-field semantics; making them work would require re-doctrine + multi-session implementation with several unresolved design questions. (d) Replacing JIT-verify crashes with a clear compile error is a strict UX win; lifting the rule later (e.g., for owning-form views with copy semantics) is forward-compatible from a strict baseline.

   Pinned by `test/parser/ViewAsClassFieldRejectionTests.cpp` (9 tests: direct view field, array-of-view field, HashMap V=view, HashMap K=view, ArrayList<view>, HashSet<view>, Optional<view>, plus sanity checks for nested-views-allowed and ordinary-class-fields-unaffected). Docs updated: `cajeta-docs/stdlib/Views.md` § Errors caught statically (added rationale paragraph + split the original "View used as a class field, or interface implementation" line into two entries), `runtime/src/cajeta/collection/HashMap.cajeta` (docstring spells out the K/V restriction with workaround).

7. ✅ **Collector<T, R> + Stream.collect terminal — DONE end-to-end (2026-05-18).** `[mode-agnostic]` — `cajeta.collection.Collector<T, R>` value class + `Stream<T>.collect<R>(Collector<T, R>)` shipped. `cajeta.collection.Collectors.toList<T>()` now compiles AND runs: two-layer naming cleared the addMethod same-canonical collision, and lambda-block-body return-type inference (LambdaExpression::resolveTypes walks the body for the first ReturnStatement when no expectedType is in play) cleared the JIT-verify rejection on `toList`'s synthesized accumulator lambda. Pinned by `test/parser/CollectorTests.cpp` (5 tests, all enabled) + `test/parser/LambdaL2Tests.cpp` (1 new test pinning the body-inference path under a ctor-arg with no LHS expectedType).

8. **Stream lambda combinators — terminals + intermediates landed; chained-form gap PROBED 2026-05-18.** `[mode-agnostic]` — Terminals **anyMatch, allMatch, noneMatch, findFirst, reduce, fold<R>, collect** all landed. Intermediate combinators **take, skip, filter, map, peek, flatMap** all landed as standalone wrapper classes (`heap FilterStream<int32>(xs.stream(), p)` works). **Fluent form blocked** — adding `Stream<T>.filter((T) -> boolean pred) { return heap FilterStream<T>(this, pred); }` to the base class breaks even `xs.stream().count()`. Root cause: when `Stream<T>` (a template) emits its method body, the body's heap-call to `FilterStream<T>(this, pred)` resolves to the FilterStream TEMPLATE form (T unresolved) AND loads `this` as the inline `Stream<T>` struct instead of passing as `ptr` — JIT verifier rejects with "Call parameter type does not match function signature!" (load type vs callee's class-pass-by-pointer ptr expectation). Two interacting issues: (a) template-body emission for class methods that construct other template types using the enclosing T parameter, (b) `this` value's pass-by-pointer convention not being honored when `this` is passed as an arg to another constructor. Stream.cajeta has a comment noting the gap. Fix requires either suppressing template-method-body emission entirely (so methods only emit per-instantiation) AND fixing this-passing convention, OR cascading T substitution through nested template heap-construction. Both are deep compiler changes; defer until there's a dedicated session for it.

9. ✅ **Templated-static-factory call syntax — DONE (Form C, 2026-05-18).** `[mode-agnostic]` — Phase 3 of MethodLevelTemplate.md shipped the post-identifier form (`receiver.method<TypeArgs>(args)`, including `TypeName.method<TypeArgs>(args)` for statics — pinned by `test/parser/MethodTemplateExplicitArgsTests.cpp`). Inference (`Util.identity(42)`) and explicit args (`Optional.Some<int32>(42)`) both work. **Form A** (`TypeName.<TypeArgs>method(args)`, Java-style dot-prefix) removed from the grammar 2026-05-18. **Form B** (`TypeName<TypeArgs>.method(args)`, Kotlin/Scala-style type-name-as-receiver) rejected at the design stage: Form C mirrors `Type<args>` at the type-use site (name + `<T>`), and one syntax is better than two.

10. ✅ **`@Encoding(EncoderClass)` for views — SHIPPED 2026-05-18.** `[mode-agnostic]` — Phase A (mutual exclusion with @BigEndian/@LittleEndian/@HostEndian/@Align, encoder-not-found / encode-missing / decode-missing errors) + Phase B (`SynthesizedEncodingCtor` + `SynthesizedEncodingToBytes`) ship together. Synthesizes `T(byte[])` calling `EncoderClass.decode(bytes)` + memcpying the decoded body into `this` + raw-freeing the decoded shell via `__cajeta_free`; synthesizes `#byte[] toBytes()` calling `EncoderClass.encode(this)`. Synthesis runs BEFORE writeVirtualTable so `toBytes` lands in the vtable. Encoder dispatch is **duck-typed** (looks up static `encode(T)`/`decode(byte[])` by name on the encoder class) since templated-interface vtable instantiation isn't yet wired — but `cajeta.wire.Encoder<T>` interface shipped alongside (templated-interface parse fix in `visitInterfaceDeclaration`) so the contract exists for documentation + future tightening. Pinned by `EncodingPhaseATests.cpp` (5) + `EncodingPhaseBTests.cpp` (5) + `TemplatedInterfaceTests.cpp` (2). Bumped STDLIB_STRUCTURE_COUNT to 34.

11. **Multiple inheritance gaps (L-03 follow-up) — 8 of 9 surface gaps shipped; remaining items rolled into MultiClassing implementation.** `[mode-agnostic]` — 8 of 9 surfaced gaps shipped + polymorphic dispatch through non-first-parent-typed bindings; pinned by `test/parser/MultipleInheritanceGapTests.cpp` (14 tests, all enabled). Tree at 899/899. Gap 9 (`super[Base].method()`) is superseded by `cajeta-docs/stdlib/MultiClassing.md` Phase 2 (Priority 1 § 2), which generalizes to BOTH method and field selectors. True-diamond / vbase handling moved to MultiClassing Phase 3 (Priority 1 § 3, with shared-by-default semantics per R-4). Other upcast sites moved to Priority 1 § 4. Features.md L-03 stays `partial`, L-19 `shipped`.
   1. ✅ **Implicit super-ctor chains ALL parents.** `Method.cpp` removed the `break;  // single inheritance chain` at the end of the implicit-super loop. Test: `twoParentsBothCtorsRun` (uses a static counter to avoid the Gap 8 field-slot issue).
   2. ✅ **Multi-parent field layout / lookup.** Rewrote `CajetaClass::getFieldLlvmIndex` to walk the layout in the same DFS order as `appendInherited`, rather than returning the standalone-struct index (which only worked for single inheritance). Test: `twoParentsFieldReadBackThroughChildWrite`.
   3. ✅ **Diamond field dedup.** Already worked by symmetry: `appendInherited` allocates duplicate slots but name resolution lands on the same slot for both casts, so writes and reads are self-consistent. Real layout-level dedup is a cleanup task but not user-visible. Test: `diamondCommonAncestorFieldShared`.
   4. ✅ **Abstract-method enforcement.** Three parts: (a) `visitMethodDeclaration` now accepts `methodBody : ';'` (was throwing `bad_any_cast`); (b) sets `method->setAbstract(true)` for body-less methods; (c) `buildVirtualTable` raises `CAJETA_ERROR_ABSTRACT_NOT_IMPLEMENTED` when this class has no abstract methods of its own (heuristic "self is concrete") and any inherited abstract lacks an override. Tests: `unimplementedAbstractMethodRejected`, `abstractMethodWithOverrideCompilesAndDispatches`.
   5. ✅ **`super.method()` upcall (L-19).** Replaced `UnsupportedExpression` with new `SuperExpression` (resolves to the first declared parent, code-gens the same `this` pointer). `MethodCallExpression` detects `SuperExpression` receivers and passes `forceDirectCall=true` through `CajetaClass::invokeMethod` to bypass vtable. Test: `superMethodCallReachesParent`. Stale `UnsupportedExpressionTests.superCallThrowsNotImplemented` removed.
   6. ✅ **Explicit `super(args)` in ctor.** `MethodCallExpression` ctor now handles the `methodCall : SUPER '(' parameterList? ')'` alternative (was null-derefing on `ctx->identifier()->getText()`); generateCode routes the call through the parent's matching ctor. Implicit no-arg super is naturally skipped when an args-only parent ctor exists. Test: `explicitSuperCtorWithArgs`.
   7. ✅ **Unsatisfied-interface enforcement.** `buildVirtualTable` raises `CAJETA_ERROR_INTERFACE_NOT_IMPLEMENTED` when an interface method has no matching concrete impl (was silently omitting the vtable entry). Test: `unimplementedInterfaceMethodRejected`.
   8. ✅ **Multi-parent ctor/method field-slot baking — SHIPPED via per-parent sub-object layout.** `CajetaClass::generatePrototype` now lays each class out as `{ vtable_primary, first-parent-sub-object-content (shares vptr), vtable_secondary_for_next_parent, ..., own-fields }`. Every non-first parent gets its own vtable slot at the start of its sub-object so a pointer adjusted to that offset looks structurally identical to that parent standalone — the parent's pre-compiled ctor/method IR uses its own slot indices and lands on the right fields. A `subObjectSlotMap` keyed by ancestor class records where each ancestor's sub-object begins; `getSubObjectByteOffset` computes the byte offset via `DataLayout`. Call-site `this` adjustments now fire in three places: Method.cpp's implicit super-ctor emission, MethodCallExpression's explicit `super(args)` path, and `CajetaClass::invokeMethod` for any method whose resolved declaring class differs from the receiver class. `getFieldLlvmIndex` mirrors the same sub-object walk so own/inherited field GEPs match the layout exactly. Tests: `twoParentsBothInstanceFieldsFromParentCtors`, `parentMethodReadsOwnFieldOnSubclassInstance`, `twoParentsMultipleFieldsEach`. Remaining caveat: polymorphic dispatch through a non-first-parent-typed binding (e.g. `B b = c` then `b.foo()` where C extends A, B) needs secondary-vtable thunks for cross-class override correction; the secondary vptr slots are currently zeroed and that path will null-deref until it lands. True diamond / virtual-base machinery (A duplicated when it's a non-first-parent on either side) is the other piece — both are now the L-03-polymorphism follow-up.
   9. ⏸ **`super[Base].method()` disambiguation — SUPERSEDED by MultiClassing Phase 2 (Priority 1 § 2).** The `cajeta-docs/stdlib/MultiClassing.md` design generalizes this to a bracket-form parent-view selector for BOTH methods (`super[Base].method()`) and fields (`this[Base].field`); grammar + dispatch land together.
   10. ✅ **Polymorphic dispatch through a non-first-parent-typed binding — SHIPPED.** Three pieces:
       - **Upcast adjustment at `LocalVariableDeclaration`.** After `HeapField::getOrCreateAllocation` stores the RHS pointer, we reload, shift by `srcClass->getSubObjectByteOffset(dstClass)`, and store back. The static helper lives at `CajetaClass::adjustForUpcast` so future call sites (assignment expressions, return statements, parameter passing) can reuse it.
       - **Secondary vtables.** `CajetaClass::getOrCreateSecondaryVTable(parent)` lazily synthesizes a vtable whose LLVM type matches the parent's standalone vtable (same hash entries / sort order). For each parent vtable entry: if this class overrides the method (declaring class differs), the entry points at an offset thunk; otherwise it points at the parent's own function directly (the receiver is already a parent-sub-pointer and is what the parent's function expects). `ClassCreatorRest::generateCode` initializes each non-first-parent vptr slot with the matching secondary vtable global at `new` time, via `klass->getNonFirstSubObjects()`.
       - **Cross-class override thunks.** `CajetaClass::synthesizeOffsetThunk(parent, impl, parentOffsetInThis)` emits a tiny private function: receives `this` as the parent-sub-pointer, GEPs `this - parentOffsetInThis` to recover the most-derived pointer, then tail-calls the override. The thunk's signature matches the override exactly so the dispatch site doesn't need to know it's a thunk. Tests: `dispatchThroughNonFirstParentTypedBinding`, `overrideThroughNonFirstParentTypedBinding`, `dispatchThroughFirstParentTypedBindingUnchanged`. `getNonFirstSubObjects` walks the layout deterministically (not the `subObjectSlotMap`, which collapses diamond ancestors) so each non-first-vptr slot is enumerated exactly once.
   - Unblocked stdlib rows (now including the multi-parent-with-instance-fields case after Gap 8): **S-107** `Optional<T>` multi-inherits `Stream<T>`, **S-302** `ArrayList<T>` IS-A `Stream<T>`, **S-305** `HashMap<K,V>` IS-A `Stream<Pair<K,V>>`. **S-313** for-loop desugar through `.next()` is a separate language item — not strictly L-03, but reads as the same feature surface for users.

12. **Implicit destructor chaining (C++ semantics) — design locked 2026-05-18, implementation pending.** `[mode-agnostic]` — Compiler-emitted destructor runs (1) user-written `~Derived()` body, (2) owned fields in reverse declaration order (already done via auto-field-drop), (3) direct parent destructors in reverse declaration order. **Automatic and non-suppressible** — no opt-out syntax; each destructor cleans only its own fields. Diamond / multi-inheritance walks the layout, so shared ancestors (deduped by the vbase ABI) destruct exactly once. `super<Base>.~Base()` may be written explicitly for documentation; it does not change codegen. Implementation site: extend `CajetaClass::getOrCreateDropFunction` (the same synthesizer that emits the auto-field-drop loop) to append direct-parent drop-fn calls — for each parent in reverse declaration order, GEP the receiver to the parent's sub-object offset and call the parent's `__cajeta_<Class>_drop` directly (static dispatch — the chain is walking statically up the hierarchy from inside the most-derived drop body). Tests: derived chains to base in single inheritance, three-level chain runs in correct reverse order, multi-inheritance runs each parent once in reverse declaration order, diamond runs the shared ancestor exactly once. Doc: `cajeta-docs/stdlib/MemoryModel.md` § destructor limitations.

### Priority 3 — Lombok-mirror annotations (all `[mode-agnostic]`) — TRACK CLOSED 2026-05-18

All shipped:

1. ✅ **`@Getter` / `@Setter`** — shipped. Field-walk synthesizers (`SynthesizedGetter/SetterMethod`). Class-level applies to every qualifying field; field-level applies to one. Setter skips final fields. Pinned by `test/parser/GetterSetterTests.cpp` (10).
2. ✅ **`@ToString`** — shipped with `format=TO_STRING_PROPERTIES` (default). **`TO_STRING_JSON` deferred** to `cajeta.codec.json` (Features.md S-1101 / S-1102) — requesting it throws `CAJETA_ERROR_TOSTRING_JSON_NOT_IMPLEMENTED`. `@ToString.Exclude` on individual fields omits them. Pinned by `test/parser/ToStringTests.cpp` (12).
3. ❌ **`@EqualsAndHashCode`** — Rejected. We have `@AutoHash` for hashing; there's no `equals()` on Object (equality goes through `operator==` overloads).
4. ✅ **`@NoArgsConstructor` / `@AllArgsConstructor` / `@RequiredArgsConstructor`** — shipped via single `SynthesizedConstructorMethod` with per-annotation field selection. Pinned by `test/parser/ConstructorAnnotationTests.cpp` (8).
5. ✅ **`@Data` / `@Value`** — shipped as bundle expansions. `@Data` = G+S+TS+AH+RAC; `@Value` = G+TS+AH+AAC (no setters — immutability). Implemented by ORing in the bundle annotations at each per-annotation synthesizer's gate. Pinned by `test/parser/DataValueAnnotationTests.cpp` (6).
6. ✅ **`@NonNull`** — shipped on parameters. `Method::emitNonNullParamChecks` walks params at body entry, emits `if (arg == null) throw 2` (`CAJETA_ERROR_NULL_PARAM_ARG`). `@RequiredArgsConstructor` already includes `@NonNull` fields alongside `final`. Return-type @NonNull deferred. Pinned by `test/parser/NonNullTests.cpp` (7). Also surfaced + fixed a pre-existing `Annotatable(set&)` ctor bug where parameter annotations weren't being mirrored to `annotationList`, breaking RTTI emission.
7. ✅ **`@Builder`** — shipped. Synthesizes a real nested `Outer.Builder` class (uses the static-nested-class infrastructure that landed alongside) with mirror private fields, chained setters returning `this`, and `build()` that allocates Outer and calls its all-args ctor. Adds `static Outer.Builder builder()` to Outer. `@Builder` implicitly triggers `@AllArgsConstructor` on Outer. **`@Builder.Default` for per-field defaults deferred** (relies on field-initializer parsing; v2). Pinned by `test/parser/BuilderAnnotationTests.cpp` (6).
8. ✅ **`@With`** — shipped. `SynthesizedWithMethod` per-field `withX(T v)` mutators (Lombok-style camelCase naming to avoid setter collision). Body: `__cajeta_alloc` + memcpy + slot overwrite + return new ptr. Pinned by `test/parser/WithAnnotationTests.cpp` (6).
9. ❌ **`@Cleanup("method"="close")`** — Rejected. Cajeta has destructors + the auto-field-drop machinery; the `@Cleanup` Java-side use case doesn't translate.

**Lombok test total: 55 new tests across 7 files (10 + 12 + 8 + 6 + 7 + 6 + 6). All green.**

**Side benefit: static nested classes** landed as the @Builder prerequisite. The grammar already accepted them; missing pieces were the visitor cast (now wrapped in `NestedClassDeclaration`) and the structureStack `.front()` vs `.back()` choice in two Method::create sites. Pinned by `test/parser/NestedClassTests.cpp` (5).

### Priority 4 — debug-mode features (CompilerModes.md phasing order)

All `[debug]`. P4.1 source-tagged drop-chain entries and P4.2 SIGABRT chain-walk handler landed; remaining items below renumbered.

1. **`--live-set=strict` — ~0.5 session.** Unbounded growth + rehash; assert on duplicate-add (catches compiler-codegen bugs in the live-set hook path). Doc: `CompilerModes.md` § `--live-set`.
2. **`--poison-free=on` — ~0.5 session.** memset freed body with sentinel pattern before glibc free. Doc: `CompilerModes.md` § `--poison-free`.
3. **`--drop-chain-validate=on` — ~0.5 session.** Per-push/pop linked-list integrity checks; assert + diagnostic on corruption. Doc: `CompilerModes.md` § `--drop-chain-validate`.
4. **`--diag-hints=on` (compile-time) — ~1 session.** "Did you mean..." for typo'd identifiers; recommend `#`-transfer when a borrow violates lifetime; suggest `@SuppressLint(...)` for noisy lints. Doc: `CompilerModes.md` § `--diag-hints`.
5. **Stack-trace capture on throw — ~1 session.** `backtrace(3)` + DWARF + source-map symbolization in the exception payload. Doc: `CompilerModes.md` § `--stack-trace-capture`.
6. **`--use-after-move-rt=on` — ~0.5 session.** Sentinel in moved slot header; trap on read. Backs up the static use-after-move tracker. Doc: `CompilerModes.md` § `--use-after-move-rt`.
7. **`--ub-traps=on` — ~0.5 session.** Trap instructions for signed overflow, divide-by-zero, oversized shift, unaligned atomic. Catches "compiler made my code do something weird" early. Doc: `CompilerModes.md` § `--ub-traps`.

### Priority 5 — release-mode features

All `[release]`.

1. **`--bounds=trap` codegen — ~0.5 session.** Skip the exception throw; emit `@llvm.trap` for the fastest bail. Doc: `CompilerModes.md` § `--bounds`.
2. **`--overflow-checks=wrapping`/`off` codegen — ~1 session.** Today integer arithmetic is implicit wrapping; make the choice explicit via the flag and wire `--overflow-checks=off` so the compiler can assume no overflow and optimize accordingly. Doc: `CompilerModes.md` § `--overflow-checks`.
3. **`--null-checks=on/off/trap` codegen — ~0.5 session.** Today null-check generation is implicit; expose the flag so users can opt out at high `--release` confidence. `@NonNull` (P3.6) integrates here. Doc: `CompilerModes.md` § `--null-checks`.
4. **`--profile-counters=on` — ~1 session.** Per-method invocation counter + wall-time tally for PGO collection. Default on under `--debug-release`. Doc: `CompilerModes.md` § `--profile-counters`.

### Priority 6 — completeness / cleanup

All `[mode-agnostic]`.

1. ✅ **P6.6 chained-form completion (basic case)** — verified shipped. `xs.stream().count()` and `xs.stream().forEach(λ)` direct chains work today, likely incidental to the earlier inline-MCE-as-ctor-arg + function-typed-field-invocation landings. Pinned by `test/parser/ChainedFormTests.cpp` (3 tests). Improved `ReturnStatement::generateCode` diagnostic for null-lowered return expressions (was hitting `dyn_cast<AllocaInst>(nullptr)` assert; now emits "[cajeta] return value lowered to null" warning). Out of scope here: longer chains like `xs.stream().filter(p).count()` fail because `Stream<T>.filter()` isn't defined — combinators must be constructed via `heap FilterStream<T>(source, pred)`. That's a stdlib-completeness gap (Priority 2 § 2), not chained-form.

2. ✅ **Non-literal static field initializers — SHIPPED.** `CajetaClass::generateStaticInitializers` emits a per-class `__cajeta_clinit_<class.canonical>` function in the class's home module that evaluates any initializer beyond the constant-folder's reach (`= 1 + 2`, `= a + 5`, `= (2*3) + (10/2)`, etc.) and stores the result into the global. Registered with `llvm.global_ctors` at default priority 65535 via `llvm::appendToGlobalCtors`. Compiler.cpp + JitTestHelper.cpp call it once after the Phase 1/2 quiescence loop completes, so expression codegen sees the final method set. JitTestHelper now calls `LLJIT::initialize(mainDylib)` explicitly so the JIT actually runs the ctors (the host-linked runtime ctors fire via process startup regardless, but pure-JIT clinits need this). IdentifierExpression gained a static-shorthand path so a bare `a` inside a static initializer (e.g. `b = a + 5`) resolves to the class's static field global. Pinned by 3 new tests in `StaticFieldTests`. Out of scope: string literals (still need global-string emission), method calls / references to other classes' statics (need ordering between cross-module clinits), aggregate / array literal initializers.

3. ✅ **P3c switch/loops/try-catch DA merging — SHIPPED.** Extended the P3a/P3b snapshot/restore/merge pattern: `WhileStatement` and `ForStatement` snapshot pre-body NYA and restore after the body so conditional assignments inside don't leak as DA-after (do-while body always runs, so the no-restore default preserves its DA escape). `SwitchStatement` snapshots pre-switch, resets per group, snapshots post-group, then unions across arms; when no `default:` exists it also unions pre-switch back in for the "no-case-matched" path. `TryStatement` snapshots pre-try, snapshots post-try, restores for catch, snapshots post-catch, then unions — but only when catch clauses exist (try-with-resources / `try {…}` with no catch propagates throws out of the method, so post-after === post-try). Pinned by `test/parser/DefiniteAssignmentTests.cpp` (8 tests covering false-negative loops/switch/try-no-catch + positive do-while/switch-with-default/try-with-catch cases).

4. ✅ **Phase 7 cleanup — SHIPPED.** Removed every `dynamic_pointer_cast<CajetaStruct>` dead branch from `src/` (LocalVariableDeclaration, Statement, Method, CajetaClass, AggregateInitializerExpression, MethodCallExpression) and deleted `src/cajeta/type/CajetaStruct.h`. All `#include` references trimmed. Comments referencing CajetaStruct as historical context were preserved or rewritten where they crossed the unified-class boundary. Tree stays at 899/899.

5. **Restore lost test coverage from Phase 7 — incremental.** The 9 deleted struct test files contained ~105 tests. Many exercised happy-path behavior still valid under the unified model.

### Memory-model carry-overs (lower priority, design-doc only)

- ✅ **Automatic field drops.** Landed via FieldOwnership.md § Solution B: the per-thread live-allocation set lets every heap allocation track its liveness; auto-drop calls the type-specific helper (`__cajeta_class_virtual_drop` / `__cajeta_free_array`) which atomically claims out of the set, so aliased fields no-op safely. Spec at `MemoryModel.md:138` updated; doctrine at `cajeta-docs/stdlib/FieldOwnership.md`; tests at `test/parser/AutoFieldDropTests.cpp` and `test/parser/FieldOwnershipAliasingTests.cpp`. Trade-off: use-after-free of an aliased field whose source has dropped is now the programmer's responsibility (Phase 6+ lifetime tracker can re-tighten).
- **No `super.~Class()` chaining.** Now reachable via virtual dispatch (Gap 1) — needs design pass for whether the base destructor should chain implicitly. Doc: `MemoryModel.md:139`.
- **Multi-parameter borrow-return needs lifetime annotations.  [I don't like those.  They're cumbersome and confusing.   Find other ways to solve the initial problems] ** Multi-input free functions can't return a borrow. Rust-style explicit lifetimes would lift it. Doc: `MemoryModel.md:307`.

---

## Build / LLVM-version migration (2026-05-20 session)

Context: development moved to an AMD Ryzen AI Max+ 395 (Zen 5 / Strix Halo). LLVM 18
predates Zen 5 — `getHostCPUName()` returns `(unknown)`, znver* table tops out at
znver4. LLVM 19.1+ has znver5 back-ported; LLVM 20+ has it native. Tried LLVM 21 first;
it introduced its own wave of regressions (vtable/drop/chained-form/with-annotation
codegen). Landed on **LLVM 20 as the new baseline**.

### Shipped this session

- ✅ **Build parameterized by `CAJETA_LLVM_VERSION` (default 20).**
  - `setup.sh` derives `LLVM_DIR` + the apt package set (`clang-${LLVM_VER}`,
    `llvm-${LLVM_VER}-dev`, `libllvm${LLVM_VER}`, `lld-${LLVM_VER}-dev`) from the
    env var; probes each via apt-cache and silently skips ones the local archive
    doesn't advertise (Ubuntu 26.04 dropped `lld-N-dev` for all N; `libllvm$N`
    folded into `llvm-$N`).
  - `src/CMakeLists.txt` uses `${LLVM_VERSION_MAJOR}` (set by `find_package(LLVM
    CONFIG)`) for `clang-N` and the lld header paths, so discovery automatically
    tracks whichever LLVM was found.
  - `Compiler.cpp`'s "install lld-N-dev" error message uses `LLVM_VERSION_MAJOR`.
- ✅ **LLVM 21 API-shift shims (preprocessor-guarded so we still build on 18/20):**
  - `CajetaModule.cpp` — LLVM 21 narrowed `Module::setTargetTriple` to take
    `llvm::Triple` instead of `StringRef`; `#if LLVM_VERSION_MAJOR >= 21` wraps
    the string in `llvm::Triple(...)`.
  - `JitTestHelper.cpp` — LLVM 21 removed `ThreadSafeContext::getContext()` in
    favor of `withContextDo(...)`; `#if` branch covers both paths.
- ✅ **Cross-platform xxhash install scripts** (`scripts/install-xxhash-{linux,
  macos}.sh`, `scripts/install-xxhash-windows.ps1`) — `runtime/native/cajeta_runtime.c`
  pulls `<xxhash.h>` via `XXH_INLINE_ALL` so headers-only is sufficient. Scripts
  cover apt/dnf/yum/pacman/zypper/apk on Linux, Homebrew/MacPorts on macOS,
  vcpkg/winget/choco on Windows, with a GitHub-release source-fallback in each.
- ✅ **`run_tests.sh` hardening.**
  - Bash signal-death diagnostics (`Segmentation fault (core dumped)` etc.) no
    longer leak from `wait`-time job reaping. The fix is `{ ...; } 2>/dev/null &`
    (group with stderr-redirect) rather than `( ... ) 2>/dev/null &` — bash
    prints the diagnostic at the *parent*'s reap step, not the child's, so the
    redirect has to apply to the parent shell.
  - Crash classifier surfaces signal codes by name (SIGSEGV/SIGFPE/SIGABRT/etc.)
    and now handles empty / unrecorded `exit_code` (subshell killed before it
    could write the .exit file) without throwing `[: : integer expected`.

### Outstanding cajeta-compiler bugs — ALL FIXED (2026-05-20 session)

All four LLVM-version-independent bugs called out previously are fixed. Tree at
1210/1340 passing, 0 explicit failures, 6 shard crashes that all pass individually
(pure state-poisoning carry-over from the still-flaky parallel runner).

1. ✅ **`StringMethodsTests.{replaceNoMatch, replaceSingleMatch, replaceMultipleMatches,
   substringSliceMatches, toUpperCaseAscii, toLowerCaseAscii, trimBothSides}` — double-free of
   the freshly-allocated `int8[] out` buffer.** Root cause was NOT a JIT-linker quirk or
   alloca aliasing — `out` got freed by the function's own `__cajeta_drop_pop_run` BEFORE
   the function returned, because `return new String(out, n)` stored `out` as the new
   String's `bytes` field without marking the local's drop entry inactive. The new
   String's bytes field then dangled, and the next read tripped a bounds-check on
   freed (re-zeroed) memory. Fix: switched all five sites in `runtime/src/cajeta/lang/
   String.cajeta` from `new String(out, …)` to `heap String(#out, …)` so the
   `#`-transfer marker fires `__cajeta_drop_mark_inactive` on `out`'s drop entry, leaving
   ownership entirely with the returned String. Other stdlib files (Path, MD5,
   FileReader) already used this pattern; String.cajeta was the lone holdout.

2. ✅ **`ErrorModelTests.{asyncFnThrowReraisedAtAwait, asyncFnSuccessAwaitsValueThroughBranches}` —
   `__cajeta_throw` walked the drop chain (freeing the awaited task) BEFORE
   `__cajeta_scope_exit_to` walked the scope_register list (which pointed INTO the
   freed task struct).** The scope-register entry's `exception_addr` read garbage from
   freed memory, treated non-NULL as a sibling throw, and re-raised the garbage
   pointer. Fix: added `__cajeta_scope_deregister_task(void*, uint64_t)` runtime
   helper that walks every active scope frame and NULLs out entries whose
   `done_addr` falls inside the given task struct; `CajetaTask::generateDropFunction`
   now calls it (with the task's allocation size from `DataLayout::getTypeAllocSize`)
   immediately before `__cajeta_free(task)`. Subsequent `scope_exit_to` sees clean
   NULL entries and bypasses them via the existing `if (done_addr)` guard.

3. ✅ **`DetachTests.freshAllocatorCaptureDetachAccepted`** — fell out of #2's fix. The
   detach path doesn't register a drop entry for the task (the detached fiber outlives
   the spawn scope), but the same task-struct lifetime invariant applied. Once the
   scope-deregister hook landed in the task drop function the symptom disappeared.

4. ✅ **`WithAnnotationTests.fieldLevelWith` — JIT bitcode reparse rejected the module
   with "Invalid number of elements in struct initializer".** Two interacting bugs:
   (a) `SynthesizedWithMethod::generateCode` was non-idempotent — the Phase 2
   codegen quiescence loop visits methods until the method set stops growing, and
   the second visit appended a duplicate `entry` basic block to `withAge`'s function,
   leaving the IR with two entry blocks (`entry` + `entry1`). Fix: early-return when
   `llvmFunction` already has a body. (b) `StructureMetadata::createPropertyConstant`
   walked `getAnnotations()` (a `set<QualifiedNamePtr>` that dedupes by pointer) for
   the constant initializer, while `createPropertyType` walked `getAnnotationList()`
   (a `list` that preserves duplicates) for the struct's slot layout. When `@With`
   got recorded twice (once at the field, once at the class via synthesizer expansion)
   the list had 2 entries but the set had 1, producing a struct type with 2 annotation
   slots and an initializer with 1 element. Fix: both `createPropertyConstant` and
   `createParameterConstant` now walk `getAnnotationList()` so the constant arity
   matches the type's slot count.

5. ✅ **`JsonSynthesizerTests.jsonReaderCurrentString`** — same `#`-transfer pattern as #1.
   `JsonReader.currentString` did `return heap String(bytes, (int32) bytes.count())`
   where `bytes` came from a `#int8[]`-returning helper. Fix: pre-compute the count,
   then `return heap String(#bytes, n)`.

6. ✅ **`ViewOwningTests.owningFormViewMayEscape` — owning views returned from a
   function double-freed their underlying buffer.** `ReturnStatement::generateCode`
   had a transfer-on-return rule for class-instance locals (drop-mark-inactive so the
   function-scope exit doesn't reclaim what the caller now owns), but explicitly
   EXCLUDED `CajetaView` types — under the assumption that "structs live inline (no
   drop)". Owning views violate that assumption: their constructor pushes a drop entry
   paired with `__cajeta_view_drop_owned` that frees the underlying buffer. Fix:
   widened the transfer condition to also fire for `CajetaView` types whose field has
   a drop entry — generic `f->getDropEntry()` check covers both classes and owning
   views. All four ViewOwningTests now pass.

---

## Done

(See "Current state" above for the running list; older entries below.)
