# PDF is gitignored (not tracked in git); fetch the original from the url below.
name: FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness
description: Tiles the attention computation to keep Q/K/V blocks in SRAM and avoid materializing the N^2 score matrix in HBM — the IO-aware fused attention kernel that removes attention's memory bottleneck.
url: https://arxiv.org/abs/2205.14135
